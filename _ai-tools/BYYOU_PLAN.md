# BYYOU_PLAN — активний потік /byyou

> **Що це:** жива оперативна памʼять одного потоку `/byyou`. Переживає обрив чату — `/byyou` без аргументу читає цей файл і продовжує з місця.
> **Чим НЕ є:** не документація і не лог рішень. Довгі «чому» рішення → `docs/adr/`. Хроніка сесії → `_ai-tools/SESSION_STATE.md`.

---

**Статус:** active
**Ціль:** Звірка плану Supabase з реальністю → виправити застарілий план + закрити реальну дірку (Notes без конверта) щоб НЕ переробляти при Фазі 2. Усе верифікується з хмари. `send_chips` — окремий наступний захід (потребує iPhone-смоуку).
**Гілка:** claude/new-session-v3pexs
**Мітка відкату:** byyou-start-supacheck
**Оновлено:** 2026-06-27

> **Council 3 погляди (Sonnet) звірили план vs код vs ROADMAP.** Голова верифікувала 2 критичні проти коду: (1) Notes БЕЗ stampEntity — підтверджено (makeNote нема, 6 фабрик не 7); (2) tombstones(план) vs hard DELETE(ROADMAP) — суперечність підтверджена. **Рішення Романа: позначити → hard DELETE через 30 днів.**
> **Статуси:** `idle` · `active` (push заблоковано до «деплой») · `paused` · `done`.

---

## Кроки

| # | Крок | Маркер | Статус | Коміт |
|---|------|--------|--------|-------|
| 1 | **Зафіксувати рішення про видалення** (tombstone + hard DELETE через 30 днів) у `SUPABASE_MIGRATION_PLAN.md` (рядок 43) ТА `ROADMAP.md` (рядок 887) — зняти суперечність. | 🟢 | ⬜ | — |
| 2 | **Синк застарілого плану:** Ворота 1 → закрито; Ворота 2 → «30 dispatch-точок (не 9 listeners)» + лінк «= Architecture Refactor Сесія 7»; send_chips → «0/8, усі парсять з тексту»; додати **Notes** у «лишилось Фази 1» (7-ма сутність без конверта). | 🟢 | ⬜ | — |
| 3 | **Синк ROADMAP:** SyncEngine → «ВИРIШЕНО у плані (DIY action-log+HLC+IndexedDB)» (не «відкрите»); EU-регіон → лінк на Фазу 2 плану; статуси security (escapeHtml ✅/CSP ⏳). | 🟢 | ⬜ | — |
| 4 | **Notes-конверт (реальна дірка).** `makeNote({text, folder, source})` у `entity-factories.js` + stampEntity; спрямувати 4 точки створення у `notes.js` (123/142/202/1045) на фабрику. node --check + контракт-тест (розширити check-factories). | 🟢 | ⬜ | — |
| 5 | **Pre-flight** (усі check-*.js + node --check) + bump CACHE + реліз-нотатки (смоук: нотатки створюються/видаляються/виживають reload). | 🟢 | ⬜ | — |

> **Маркери:** 🟢 GO. Кроки 1-3 — doc-only (нуль ризику). Крок 4 — чистий рефактор створення нотаток (форма не міняється, лише додається конверт) → верифікується node+E2E. Схему БД не чіпаємо (Фаза 2).

---

## Наступне ПIСЛЯ цього потоку (окремо)

- **send_chips** (структуровані чіпи) — tool + 1 handler у `dispatchChatToolCalls` + текст-fallback на 2-3 тиж (стара PWA) + перенести CHIP_PROMPT_RULES у tool-опис. 🟡 Зачіпає поведінку AI → потребує iPhone-смоуку, тому окремим заходом.
- **bareNoun node-тест** (борг із попереднього потоку) — винести чисту логіку shouldClarify у src/data/.
- **Ворота 2** (структурний nm-data-changed, 30 точок) — 🔴 потребує iPhone (зламаний слухач = мертве табло).

---

## Зупинки і рішення Романа

| Крок | Питання | Рішення | Дата |
|------|---------|---------|------|
| старт | Видалення: tombstone назавжди чи hard DELETE? | **позначити → hard DELETE через 30 днів** | 27.06 |
| старт | План 5 кроків — стартуємо? | чекаю ОК | 27.06 |

---

## Де зупинились

**Поточний крок:** 0 — брама старту (план показано, чекаю ОК)
**Причина:** звірка зроблена (Council 3 + верифікація Голови), рішення про видалення прийнято.
**Наступна дія:** після ОК — мітка `byyou-start-supacheck` + крок 1. Деплою немає до «деплой».
