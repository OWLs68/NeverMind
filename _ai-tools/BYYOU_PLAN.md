# BYYOU_PLAN — активний потік /byyou

> **Що це:** жива оперативна памʼять одного потоку `/byyou`. Переживає обрив чату — `/byyou` без аргументу читає цей файл і продовжує з місця.
> **Чим НЕ є:** не документація і не лог рішень. Довгі «чому» рішення → `docs/adr/`. Хроніка сесії → `_ai-tools/SESSION_STATE.md`.
> **Тримати маленьким.** Після завершення потоку — архівувати у `_archive/BYYOU_PLANS_archive.md`, цей файл скинути у шаблон (статус `idle`).

---

**Статус:** done
**Ціль:** контракт-тести (детермінований конвеєр текст→сутність) + Golden Journey (наскрізний E2E через екрани) — ✅ ЗАВЕРШЕНО, E2E #17 зелений
**Гілка:** claude/new-session-gfrvu5
**Оновлено:** 2026-06-22

> **Статуси:** `idle` · `active` (push заблоковано до «деплой») · `paused` · `done`.

---

## Кроки

| # | Крок | Маркер | Статус | Коміт |
|---|------|--------|--------|-------|
| 1 | Розширити `scripts/check-guards.js` — покрити всі 7 guards + `applyAllGuards` порядок (35/35) + **фікс баґу `\b`-кирилиця** (момент-guard + дні тижня) | 🟢 | ✅ | step1 |
| 2 | Новий `scripts/check-intent-router.js` — `parseExplicitIntent` (routine/reminder, дата, bail) — 17/17 | 🟢 | ✅ | step2 |
| 3 | Зареєструвати `check-intent-router.js` у `pre-push-check.js` UNIT_GUARD_SCRIPTS | 🟢 | ✅ | step3 |
| 4 | `tests/e2e/contract.spec.js` — текст→mockAI save_task→Tasks+persist (finance/notes — наступним) | 🟡 | ✅ | step4 |
| 5 | `tests/e2e/golden-journey.spec.js` — задача+нотатка через екрани+reload+0 помилок | 🟡 | ✅ | step5 |
| 6 | Додати у `.github/workflows/e2e.yml` крок «node scripts/check-*.js» (контракт-тести як CI-гейт, не лише pre-push) | 🟡 | ⏳ | — |
| 7 | Локальна перевірка: 201 node-перевірка зелена + `playwright --list` 16/16 парситься | 🟢 | ✅ | verify |
| 8 | Оновити `docs/FILE_STRUCTURE.md` (нові тест-файли) | 🟢 | ✅ | step8 |
| 9 | Реліз-нотатки + тест-батч → брама деплою | 🟡 | 🔄 | — |

> **Маркери:** 🟢 GO · 🟡 ТВІЙ ХІД (E2E #4/#5 реально йдуть лише у CI — локально немає браузерів; #6 чіпає CI) · 🔴 СТОП.
> **Статуси кроку:** ⏳ ще не почато · 🔄 в процесі · ✅ зроблено · 🚫 скасовано.
> **Примітка:** жоден крок не чіпає схему даних / storage-ключі / payload — лише тести + один additive CI-крок. Тому 🔴 немає (безпечна обкатка).

---

## Реліз-нотатки (перед «деплой»)

**ЩО ЗМІНИЛОСЬ:**
- Контракт-тести: `check-guards` 2→35 (всі 7 вартових), новий `check-intent-router` 17, E2E `contract.spec` + `golden-journey`.
- Баг-фікс: вартовий «момент» + дні тижня пн/вт… (були мертві через `\b`-кирилицю).
- CI: контракт-тести тепер гейт у `e2e.yml`. Скіл `/byyou` + хук-замок.

**ЩО МОЖЕ ЗЛАМАТИСЬ (міні pre-mortem):**
- Фікс «момент»/днів змінює класифікацію у ВСІХ 8 чатах: «запиши момент» тепер реально НЕ робить подію; «нагадай … пн» тепер ловить день. Стара поведінка була баг → малоймовірно що хтось на неї покладався.
- E2E `contract`/`golden` локально НЕ ганялись (браузери недоступні) — перший реальний прогін у CI; можливі дрібні правки селекторів якщо CI червоний.
- `/момент/i` тепер матчить і «моментально» (рідкісний edge, низький ризик).

**ЩО ПЕРЕВІРИТИ НА IPHONE (≤5):**
- [ ] Inbox: «запиши момент про каву» → зберігається як Момент (НЕ подія в календарі).
- [ ] Inbox: «нагадай подзвонити мамі пн о 10» → нагадування на понеділок.
- [ ] «купити молоко» → у Tasks (звичайна задача не зламалась).
- [ ] «купив хліб 3 євро» → у Finance (guard конверсії живий).
- [ ] Створи нотатку → видно після перезавантаження.

---

## Зупинки і рішення Романа

| Крок | Питання | Рішення | Дата |
|------|---------|---------|------|
| 1 | Контракт-тест знайшов баг: `\b` не працює з кирилицею → вартовий «момент» мертвий + дні тижня `пн/вт…` мертві (`dispatcher-guards.js:32`, `inbox.js:1211,1217-1223`). Фіксити зараз? | Варіант 1 — фікс обох зараз (Роман) | 21.06 |
| хук | push-замок теж мав `\bдеплой\b` → не пускав «Деплой» (той самий баг). Фікс `/деплой/i`. | авто | 21.06 |
| E2E#16 | Playwright 1 fail+2 flaky: update-тур (#slides-tour) перехоплював кліки на чистому тест-профілі (`nm_seen_update≠v065`, відкривається +500мс після boot). | Фікс: глушити тур за `__NM_TEST_SEED__` (onboarding.js + helpers boot) — version-proof | 21.06 |

---

## Де зупинились

**Поточний крок:** 9 — брама деплою (чекаю слово «деплой» від Романа)
**Причина:** кроки 1-8 готові, локально все зелене
**Наступна дія:** після «деплой» — push → петля E2E через GitHub MCP → звіт
