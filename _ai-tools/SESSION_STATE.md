# Стан сесії

> **Правило ротації:** у файлі детально описані **2 останні активні сесії**.
> При виклику `/finish` у новій сесії — найстарша з 2 переноситься у [`_archive/SESSION_STATE_archive.md`](../_archive/SESSION_STATE_archive.md).
> Попередні сесії до JvzDi — в архіві (6GoDe, dIooU, QV1n2, NFtzw, uDZmz, rSTLV, w3ISi, VJF2M, Vydqm, FMykK, 14zLe, KTQZA, gHCOh, cnTkD, hHIlZ, W6MDn, VAP6z, acZEu, E5O3I, 3229b, 6v2eR, jMR6m).

**Оновлено:** 2026-04-20 (сесія **EY55M** — **"Один мозок" #1 + #2A**: 17 комітів, 7 чатів отримали UI tools (#1), потім повний INBOX_TOOLS (#2A). Виявлено 2 регресії на iPhone-тесті v311: switch_tab замість save_finance/save_task у Фінансах/Проектах + галюцинація звички у Задачах. Фінанси і Задачі виправлено промптом, Проекти/Здоров'я — відкриті баги B-84/B-85.)

---

## ⚠️ ДЛЯ НОВОГО ЧАТУ — найважливіше

**1. 🧭 СЕСІЯ EY55M — "Один мозок" #1 + #2A зроблено, 2 регресії відкриті.** 17 комітів. Усі 7 tab-чатів (Вечір, Я, Задачі, Нотатки, Фінанси, Здоров'я, Проекти) переведено на повний INBOX_TOOLS — навігаційні tools (`switch_tab`/`open_memory`/`open_settings`/`open_finance_analytics`/`set_owl_mode`/`export_health_card`) + CRUD (`save_task`/`save_finance`/`complete_task` тощо) тепер працюють з будь-якого чату. Створено спільний `dispatchChatToolCalls` + мапу `_toolCallToUniversalAction` у `habits.js`. Промпти скоротились на ~30 рядків кожен (прибрано JSON-приклади, tool schemas покривають). CACHE_NAME: `nm-20260420-0441`.

**2. 🚨 ВІДКРИТІ РЕГРЕСІЇ #2A на iPhone-тесті v311+:**
  - **B-85 Проекти/Здоров'я** — AI обирає `switch_tab` замість CRUD. Приклади: "створи задачу по проекту — зробити презентацію" → "Відкрив Проекти" замість save_task. У Фінансах це виправлено через жорстке правило промпту (коміт `5078a21`) — потрібно те саме у `projects.js` і `health.js`.
  - **B-84 Здоров'я промпт** — OWL "вибачається" при болі: "Сьогодні болить голова" → "Вибач, що ти відчуваєш дискомфорт, що могло викликати цю біль?" Патернова шаблонність, не у стилі OWL. Потрібно переписати тон (прямий, без сантиментів).

**3. 📋 ПЛАН НАСТУПНОЇ СЕСІЇ — закрити B-84/B-85 (~30 хв):**
  - У `src/tabs/projects.js` промпт додати ЖОРСТКЕ ПРАВИЛО: "якщо юзер каже 'створи задачу/подію/нотатку' → save_task/create_event/save_note ОБОВ'ЯЗКОВО, НЕ switch_tab".
  - У `src/tabs/health.js` промпт: те саме + переробити тон OWL (прибрати "Вибач/мені шкода/розумію" — заборонені шаблони).
  - Перетестувати на iPhone — обидва баги.

**4. 🔜 ПІСЛЯ B-84/B-85 — наступні напрямки з ROADMAP:**
  - **👤 Я 65→100%** (1-2 сесії) — теплова карта, автопатерни тижня
  - **📁 Проекти 65→100%** (2 сесії) — глибоке інтерв'ю, стагнація, синк Витрати→Фінанси
  - **"Один мозок #2 B повний"** — додати у INBOX_TOOLS специфічні tools що зараз у text-JSON fallback: `complete_step`/`undo_step` (Задачі), `delete_transaction`/`set_budget`/`create_category` (Фінанси), `update_health_progress`/`add_medication` (Здоров'я), `complete_project_step`/`add_project_step`/`update_project_progress` (Проекти).

**5. CACHE_NAME АКТУАЛЬНЕ:** `nm-20260420-0441`.

**6. ТЕСТУВАННЯ НА iPhone v312+ підтвердило (з шпаргалки EY55M):** UI навігація працює (switch_tab/open_memory/open_settings/open_finance_analytics/set_owl_mode/export_health_card). CRUD у Фінансах і Задачах працює після фіксу промпту. Регресії — Проекти/Здоров'я (див. п.2).

**7. Workflow Романа:** "Роби" → один таск → звіт → пропозиція наступного → чекати. **Файли >250 рядків — skeleton+Edit, checkpoint-коміт після кожної фази.**

---

## 🔧 Сесія EY55M — "Один мозок" #1 + #2A: 17 комітів, 7 чатів на INBOX_TOOLS (20.04.2026)

### Зроблено

**Один мозок #1 (8 фаз, коміти `abbeafd → c4ed025`):** UI tools (навігація) у всі 7 tab-чатів.
- **Фаза 1 `abbeafd`** — extract `UI_TOOLS_RULES` константа у `src/ai/prompts.js`. Спільні правила навігації (switch_tab/open_memory/open_settings/open_finance_analytics/set_owl_mode/export_health_card) для імпорту в усі чати без дублювання.
- **Фаза 2 `13d9605`** — Вечір (`evening-chat.js`): `handleUITool` dispatch перед `dispatchEveningTool`. Вже був на INBOX_TOOLS, додав UI tools dispatch.
- **Фази 3-8** — Я/Задачі/Нотатки/Фінанси/Здоров'я/Проекти: переключив з `fetch()` на `callAIWithTools(UI_TOOLS)`. CRUD залишався у text-JSON fallback.
- **CACHE_NAME bump:** `nm-20260419-1951` → `nm-20260419-2027`.

**Один мозок #2 варіант A (7 фаз, коміти `f4da60c → fc2e707`):** повний INBOX_TOOLS у всі 7 чатів. Виправлення регресій #1.
- **Фаза 1 `f4da60c`** — створено `_toolCallToUniversalAction(name, args)` у `habits.js` — мапа 20 INBOX_TOOLS → формат action для processUniversalAction. Bulk tools (complete_task/complete_habit з масивами IDs) розбиваються на окремі actions. Спільний dispatcher `dispatchChatToolCalls(toolCalls, addMsg, originalText)` — UI tool через handleUITool, CRUD через universal action.
- **Фази 2-7** — me/habits/notes/finance-chat/health/projects: UI_TOOLS → INBOX_TOOLS. Промпти скоротились на ~30 рядків кожен (прибрано JSON-приклади, tool schemas покривають). Локальні fallback залишено для специфічних actions: `complete_step`/`undo_step` (Задачі), `delete_transaction`/`set_budget`/`create_category` (Фінанси), `update_health_progress`/`add_medication` (Здоров'я), `complete_project_step`/`add_project_step` (Проекти), `search_notes`/`open_folder`/`open_note` (Нотатки). Finance чат — кастомний цикл бо `update_transaction` не в processUniversalAction.
- **CACHE_NAME bump:** `nm-20260419-2027` → `nm-20260420-0430`.

**Hook context-warning recalibration (коміт `a3721d5`):** старі пороги 80%/90% від 1M застаріли — Opus 4.7 у розширеному режимі [1m] має реальний ліміт ~1.6M. Нові пороги у `.claude/hooks/context-warning.sh`: WARN 75% (1.2M) жовте з трикутниками 🟡⚠️, CRITICAL 85% (1.36M) червоне 🔴⚠️. Помітніший візуал (рамка з емодзі-рядків, bordered block, action hint з ⚡). Hook спрацьовує у `UserPromptSubmit` — попередження з'являється у КОЖНОМУ наступному повідомленні після першого переходу порога.

**Регресії #2A виправлені (коміт `5078a21`, CACHE_NAME `nm-20260420-0441`):**
- **Фінанси** — "50 на каву" → AI обирав `switch_tab("finance")` замість save_finance. Корінь: промпт мав застарілі JSON приклади `save_expense`/`save_income` яких НЕМА як tools — AI плутався. Замінив на жорстке правило з конкретними прикладами: "50 на каву" → save_finance(fin_type=expense, amount=50, category=Їжа, fin_comment=кава).
- **Задачі** — "Пити воду щодня" → AI галюцинував "у тебе вже є звичка Пити воду". Корінь: правило "edit_habit замість save_habit нову" AI трактував занадто широко. Переписав: ПЕРЕВІР список "ЗВИЧКИ СЬОГОДНІ" — якщо назви немає → save_habit. Якщо ТОЧНО є з ID → edit_habit.

### Обговорено (без виконання)

- **Варіант B повний для #2** (2-3 сесії) — додати у INBOX_TOOLS специфічні tools (complete_step/undo_step/delete_transaction/set_budget/create_category/update_health_progress/add_medication/complete_project_step/add_project_step/update_project_progress) щоб усі 7 чатів мали повний CRUD без text-JSON fallback. Відкладено — варіант A покриває 90% кейсів.
- **Тестова шпаргалка v311+ (6 сценаріїв навігації + 1 регресія CRUD)** — Роман пройшов на iPhone. UI навігація працює всюди (підтверджено). CRUD у Фінансах і Задачах працює після промпт-фіксу. Регресії в Проектах і Здоров'ї залишились — нові баги B-84/B-85.

### Ключові рішення

- **Варіант A vs B для #2.** Я рекомендував B (поетапно з тестом, без регресій). Роман обрав A (швидко, ризик). Виявились регресії — ціна швидкості. Підтверджено правило: "поетапно з тестом" безпечніше за "одним пушем".
- **Спільна мапа замість 6 копій dispatch.** Створив `_toolCallToUniversalAction` у habits.js — інші чати імпортують. Якби кожен мав свій dispatch — 6 копій підтримувати, баг у одному не помітиш у решти.
- **Промпт-фікс замість зміни tools.** Регресії "switch_tab замість CRUD" вирішую жорсткими правилами у промптах кожного чату, а не зміною описів tools (бо інші чати тоді теж будуть інакше реагувати на навігацію).
- **OWL вибачається — НЕ баг tools, а тон промпту.** Здоров'я-промпт треба переписати у стилі OWL (прямий, без "Вибач/мені шкода").
- **Залишити локальні fallback _processOne у 6 чатах.** Прибрати їх повністю = #2B окрема сесія. Зараз вони покривають специфічні actions без яких чат би деградував.

### Інциденти

- Без reset/force push. 17 чистих комітів `abbeafd → 5078a21`.
- 1 проблема build-time на старті (esbuild не встановлено локально) → npm install → зелено далі.
- 2 регресії від мене на iPhone-тесті: (а) #1 — переключення на UI_TOOLS-only зламало CRUD у всіх 6 чатах ("50 на каву" → AI давав текст замість JSON); (б) #2A — у Фінансах AI обирав switch_tab замість save_finance через застарілі JSON-приклади у промпті. Обидві виправлені у тій же сесії.
- Накопичилось 2 нових відкритих баги (B-84/B-85) — не встиг закрити через брак контексту, відкладено у наступну сесію.

### Метрики

- **Коміти:** 17 (`abbeafd → 5078a21`) + 2 build (`7d235a2`, `6555e9e`) + 1 docs (`bf16308`) + 1 hook (`a3721d5`)
- **Гілка:** `claude/start-session-EY55M`
- **Версія:** v309 (старт) → v312+ після мержу (з усіма фіксами)
- **CACHE_NAME:** `nm-20260419-1951` → `nm-20260420-0441` (3 bump'и)
- **Файли змінені:** `src/ai/prompts.js`, `src/tabs/me.js`, `src/tabs/habits.js`, `src/tabs/notes.js`, `src/tabs/finance-chat.js`, `src/tabs/health.js`, `src/tabs/projects.js`, `src/tabs/evening-chat.js`, `sw.js`. Нові експорти: `UI_TOOLS_RULES`, `_toolCallToUniversalAction`, `dispatchChatToolCalls`.
- **Build:** локально зелений після кожного коміту.
- **Тестування:** на iPhone v311-v312+ підтверджено UI навігація працює, CRUD у Фінансах/Задачах працює після фіксу. Регресії у Проектах/Здоров'ї → відкриті баги B-84/B-85.

### Наступні кроки

- **[Пріоритет 1]** Закрити B-85 (Проекти/Здоров'я switch_tab замість CRUD) — додати жорстке правило промпту як зроблено у Фінансах. ~20 хв, 2 коміти.
- **[Пріоритет 2]** Закрити B-84 (Здоров'я промпт OWL вибачається) — переписати тон, заборонити "Вибач/мені шкода/розумію". ~10 хв, 1 коміт.
- **[Пріоритет 3]** Перетестувати на iPhone v313+. Якщо все зелене — обрати наступний напрямок з ROADMAP (Я 65→100%, Проекти 65→100%, "Один мозок #2 B").

---

## 🔧 Сесія JvzDi — 3 фікси з iPhone-тесту v307 + виявлена проблема "Один мозок" (19.04.2026)

### Зроблено

**1. Жорстке правило `switch_tab` у промпті — коміт `240a0b5`.** iPhone-тест v307: "Відкрий задачі" у Inbox повертав "✓ Збережено" (AI інтерпретував як save_task з title="задачі"). Корінь — м'яке правило "відкрий X → switch_tab" без заборони альтернатив. Переписав правило у `prompts.js` UI TOOLS секція як ЖОРСТКЕ з прикладами: повідомлення що починається з "відкрий/покажи/перейди до/переключи на + назва вкладки" → ЗАВЖДИ switch_tab, НЕ save_task. Додано примітку "назва вкладки у командах 'відкрий X' — НЕ контент для збереження".

**2. Видалено плацебо `set_theme` — коміт `240a0b5` (той самий).** iPhone-тест: "Зроби темну тему" → сова "Темна тема." але нічого не змінилось. Причина: `applyTheme()` у `nav.js` лише фарбує колір таб-бару за поточною вкладкою, глобальної темної теми не існує. Tool був ілюзією. Прибрав: (а) визначення з `UI_TOOLS` масиву; (б) case з `handleUITool` dispatcher; (в) рядок з `INBOX_SYSTEM_PROMPT`; (г) невикористаний імпорт `applyTheme` з `ui-tools.js`. Повернемо коли буде реальна темна тема.

**3. Портовано парсер чіпів `_parseContentChips` у Inbox chat — коміт `0bf3d37`.** iPhone-тест: "Болить горло 2 дні" → сова "Коли саме почалося? ('Сьогодні' / 'Тиждень тому' / 'Давно')" — варіанти текстом у дужках, не кнопки. Причина: Health-інтерв'ю промпт (додано 6GoDe) обіцяв чіпи, але Inbox chat не мав парсера JSON блоку `{chips:[...]}` — тільки evening-chat мав. Зміни у `src/tabs/inbox.js`: (а) `addInboxChatMsg(role, text, chips = null)` — новий третій параметр; (б) імпорт `renderChips` з `owl/chips.js` + `extractJsonBlocks` з `core/utils.js`; (в) після бульбашки сови — `<div class="chat-chips-row">` з чіпами; (г) перед кожним agent-повідомленням видаляються застарілі чіпи попередніх (релевантні лише останньому питанню); (д) helper `_parseContentChips(content)` — повертає `{text, chips}` з content, text без JSON; (е) 3 точки показу `msg.content` (sendToAI без tool_calls, sendToAI з tool_calls, sendClarifyText) замінено на парсинг+рендер з чіпами. Промпт у `prompts.js`: Health-інтерв'ю приклад тепер показує `{"chips":[{"label":"Сьогодні","action":"chat"}, ...]}` формат, плюс загальне правило "ФОРМАТ ЧІПІВ" для всього Inbox чату (2-4 чіпи, label до 3 слів, action завжди "chat", не додавати при відкритих питаннях або коротких підтвердженнях).

**4. CACHE_NAME bump:** `nm-20260419-1912` → `nm-20260419-1951`.

### Обговорено (без виконання)

- **Архітектурний запит "Один мозок" (Роман чітко сформулював):** UI tools і tool calling підключені ТІЛЬКИ в Inbox. Решта 7 чатів (Вечір, Задачі, Нотатки, Фінанси, Звички, Я, Здоров'я, Проекти) — ізольовані локальні обробники. "Відкрий задачі" у чаті Здоров'я не перемикає вкладку, "Відкрий аналітику" у чаті Задач — сова шукає задачу з назвою "аналітику". Порушення принципу "один мозок" з CLAUDE.md. Роман: "В нас один мозок перевір усі вкладки". Два рівні вирішення:
  - **#1 Мінімум (наступна сесія, ~60 хв):** додати UI tools у всі 7 чатів. Голосом/текстом з будь-якого чату можна відкрити будь-яку вкладку, аналітику, пам'ять, налаштування.
  - **#2 Повний (окремо 2-3 сесії):** переробити 7 чатів на `callAIWithTools` з повним INBOX_TOOLS. Юзер з будь-якого чату може створити/редагувати/видалити будь-який тип запису. Це пункти 4.9+4.10 з ROADMAP ("розкатка чат-двигуна").
  - Додатковий баг глибший: чат Задач жорстко замкнений на задачну логіку в промпті ("Болить горло 3 дні" → "Що саме виснажило? Може переглянемо пріоритети?" — зовсім не про здоров'я). Потребує або переадресації на save_note/create_health_card, або м'якшого промпту.

- **Пост-MVP Вечора (4 напрямки з `EVENING_2.0_PLAN.md`):** посилення ритуалу (неділя/автомоменти/інтелект-карта/заспокоєння), розширення автосинхронізації (AI-роутинг щоденника/зворотний розпорядок/Google Calendar), інтеграція з вкладками (ліки у підсумку/календар-дзеркало/проекти), розкатка чат-двигуна. Роман обрав підхід "по порядку як запропонував" — спочатку iPhone-тест, потім розкатка двигуна. Тест виявив проблему "Один мозок" — вона стала пріоритетом над пост-MVP фічами.

- **Результати iPhone-тесту v307 крім 3 фіксів:** (а) лог діагностики 9/9 smoke зелені; (б) `sw.js load failed` не з'являється після v307 (6GoDe .catch() фікс працює); (в) `stale message detected` тепер як `log` не `warn` (6GoDe downgrade працює); (г) Auto-silence активний 12 хв очікувано — Роман читав/тестував без CRUD; (д) 1 fail запит з 12 — короткочасний glitch.

### Ключові рішення

- **Прибрати `set_theme` замість додати dark theme.** Плацебо-tool руйнує довіру гірше ніж відсутність фічі. Повертаємо коли буде реальна темна тема.
- **Портувати `_parseContentChips` з evening-chat, не винаходити новий.** Evening-chat перевірений патерн з 8 фаз MVP Вечора. Принцип "один двигун" — якщо закритий тест у Вечорі, має закривати і в Inbox.
- **Не чіпати "Один мозок" у цьому чаті через контекст 90%.** Роман: "треба зробити але так щоб не поламати". Правильне рішення — зафіксувати план у SESSION_STATE і почати свіжу сесію. 3 фікси цієї сесії вже на проді і стабільні — добра точка зупинки.
- **Жорстке формулювання промпту > м'яке.** AI краще слухається категоричних правил з прикладами ("ЗАВЖДИ switch_tab, НЕ save_task") ніж ввічливих натяків. Це повторюється для всіх крайніх випадків.

### Інциденти

- Без reset/force push. 2 чистих коміти `240a0b5 → 0bf3d37`.
- Не обривалось (stream idle timeout не траплявся).
- Одна `npm install` перед локальним build (esbuild не був встановлений у робочій директорії).

### Метрики

- **Коміти:** `240a0b5 → 0bf3d37` (2 коміти)
- **Гілка:** `claude/start-session-JvzDi`
- **Версії:** v307 (старт) → v308+ після мержу
- **CACHE_NAME:** `nm-20260419-1912` → `nm-20260419-1951`
- **Файли змінені:** `src/ai/prompts.js`, `src/ai/ui-tools.js`, `src/tabs/inbox.js`, `sw.js`
- **Build:** локально зелений (node build.js без помилок після npm install)
- **Тестування:** на iPhone v307 — 3 баги виявлено і виправлено. Тест v308+ після мержу чекаємо.

### Наступні кроки

- **[Пріоритет 1]** Сесія "Один мозок #1 мінімум" — UI tools у всі 7 чатів. Починати з простіших (Нотатки, Я). Checkpoint-коміт після кожного чату.
- **[Пріоритет 2]** Тест на iPhone v308+ — підтвердити що "Відкрий задачі" перемикає вкладку і чіпи у Health-інтерв'ю клікабельні.
- **[Пріоритет 3]** Після "Один мозок #1" — вибір напрямку з ROADMAP (Я 65→100%, Проекти 65→100%, "Один мозок #2 повний").


---

## Проект

| | |
|--|--|
| **Версія** | v309 на проді. Після мержу EY55M — v312+ з усіма фіксами #1 + #2A |
| **URL** | owls68.github.io/NeverMind |
| **AI модель** | OpenAI GPT-4o-mini з Tool Calling (**46 tools:** 31 INBOX + 7 UI + 8 health/memory/cat). Після EY55M: усі 7 tab-чатів повний INBOX_TOOLS + спільний `dispatchChatToolCalls` |
| **Гілка** | `claude/start-session-EY55M` (17+2 комітів, push зелений) |
| **CACHE_NAME** | `nm-20260420-0441` |
| **Repo** | Public + LICENSE (All Rights Reserved) |

---

## 🗺️ Куди йде проект

**Дорожня карта — єдине місце:** [`ROADMAP.md`](../ROADMAP.md).

**🚀 Поточний Active:** **Вечір 2.0** — переробка концепції вкладки. 3 сесії, 8 фаз. Детальний план → [`docs/EVENING_2.0_PLAN.md`](../docs/EVENING_2.0_PLAN.md).

---

## 🎯 НАСТУПНИЙ КРОК — закрити B-84/B-85

**🚨 ПРІОРИТЕТ 1:** виправити 2 регресії з #2A які залишились відкриті:
- **B-85** Проекти/Здоров'я — додати жорстке правило промпту "якщо юзер каже 'створи задачу/подію/нотатку' → save_task/create_event/save_note ОБОВ'ЯЗКОВО, НЕ switch_tab" як зроблено у Фінансах коміт `5078a21`
- **B-84** Здоров'я — переписати тон OWL у промпті (заборонити "Вибач/мені шкода/розумію" — патернова шаблонність, не у стилі OWL)
- ~30 хв, 2 коміти

**Після закриття B-84/B-85:**
- **👤 Я 65→100%** (1-2 сесії) — теплова карта активності, автопатерни тижня, мікровізуалізація виконаних звичок
- **📁 Проекти 65→100%** (2 сесії) — глибоке інтерв'ю, детектор стагнації, синк Витрати проекту → Фінанси
- **"Один мозок #2 B повний"** (2-3 сесії) — додати у INBOX_TOOLS специфічні tools що зараз у text-JSON fallback (complete_step/undo_step/delete_transaction/set_budget/create_category/update_health_progress/add_medication/complete_project_step/add_project_step/update_project_progress)
- **Пост-MVP Вечора** — Ритуал неділі, інтелект-карта дня, AI-роутинг щоденника (див. `docs/EVENING_2.0_PLAN.md`)

**Перед будь-якою роботою — тест на iPhone v313+** (після мержу B-84/B-85) підтвердити що регресії закрилися.

---

## Відомі технічні проблеми (не вирішені)

1. **B-65** SW load failed — одноразова помилка, не відтворюється. Низький пріоритет.
2. **Tool calling тільки в Inbox** — решта чат-барів на JSON-форматі (але множинні JSON працюють через `extractJsonBlocks`)
3. **`src/core/nav.js` = 1236 рядків** — трохи більше 1200 порогу. Кандидат на розбиття (settings/theme/profile/storage)
4. **Monobank** — відкладено до Supabase
5. **~150 рядків закоментованого коду** по проекту — колись прибрати

---

## 📦 Попередні сесії

Детальні описи dIooU, QV1n2, NFtzw, uDZmz, rSTLV, w3ISi, VJF2M, Vydqm, FMykK, 14zLe, KTQZA, gHCOh, cnTkD, hHIlZ, W6MDn, VAP6z, acZEu, E5O3I, 3229b, 6v2eR, jMR6m → [`_archive/SESSION_STATE_archive.md`](../_archive/SESSION_STATE_archive.md).
