# Стан сесії

**Оновлено:** 2026-04-15 (сесія jMR6m — **Фаза 1 Здоров'я ВИКОНАНА (коміт `fab3865`) + 3 нові правила + 4.12 Антидублювання у плані**)

---

## Проект

| | |
|--|--|
| **Версія** | v170 (деплої через auto-merge) |
| **URL** | owls68.github.io/NeverMind |
| **AI модель** | OpenAI GPT-4o-mini з **Tool Calling** (26 tools) |
| **Гілка** | `claude/start-session-jMR6m` |
| **Repo** | Public + LICENSE (All Rights Reserved) |

---

## 🗺️ Куди йде проект

**Дорожня карта — єдине місце:** [`ROADMAP.md`](../ROADMAP.md) у корені репо.

**Поточний Active:** Блок 2 — Концепції вкладок (core UX). Аудит 6 вкладок проведений 15.04.2026 (сесія Lp0Ym). **Фаза 1 Здоров'я виконана 15.04.2026 сесія jMR6m коміт `fab3865`** — `getHealthContext()` тепер живе у `getAIContext()`, алергії видно скрізь. **Наступна робоча ціль — Фаза 1.5 Здоров'я** (двосторонній синк картка ↔ `nm_events` з архівацією прийомів). 6 фаз у `ROADMAP.md` секція Active.

---

## ⚠️ Для нового чату — де саме зупинились (15.04 сесія jMR6m)

**Що зроблено у цій сесії:**

**Код (Фаза 1 Здоров'я виконана, коміт `fab3865`):**
1. `src/tabs/health.js` — `nm_allergies` CRUD + UI-картка (коралова, фіксована зверху), lazy-міграція карток через прапор `nm_health_migrated_v2`, розширена структура `nm_health_cards` (`doctor`, `doctorRecommendations`, `doctorConclusion`, `startDate`, `nextAppointment:{date,time}`, `history:[{ts,type,text}]`), розширені `medications` (`dosage`, `schedule[]`, `courseDuration`, `log:[ts]`, `createTasks`), `openAddHealthCard` створює нову структуру, `renderHealthWorkspace` оновлено (нові медикаменти, блок "Лікування", Записи лікаря з `history.filter(type=doctor_visit)`), прибрано дубль-виклик у `sendHealthBarMessage`
2. `src/ai/core.js` — імпортовано `getHealthContext`, викликається у `getAIContext()` (паралельно `getFinanceContext`). "Один мозок" для здоров'я відновлений
3. `index.html` — новий anchor `#health-allergies-card` зверху `#health-scroll`
4. `sw.js` — `CACHE_NAME` оновлено `nm-20260413-1505` → `nm-20260415-1123`

**Документація та правила (5 комітів до коду):**
1. План Фази 1 деталізований + нова Фаза 1.5 + пункт 4.12 у Блок 4 + підсекція у Ideas (алергії розширення)
2. Правило **"🗺️ Робота з ROADMAP"** у CLAUDE.md — пріоритизація нових ідей
3. Правило **"🔍 Перевірка на внутрішню узгодженість"** у CLAUDE.md — два типи знахідок після точкової правки
4. Посилене правило **"Сперечайся і пропонуй альтернативи"** у CLAUDE.md — здорова критика вітається
5. Скіл `/obsidian` виправлений ("Запиши в daily" → "Запиши у brain") + Крок 3 під новий флоу + `/start` нагадувалка синхронізована

**Вкладка Здоров'я виросла ~45% → ~55%.**

**Наступний крок у новому чаті:** **Фаза 1.5** — двосторонній синк `nextAppointment` ↔ `nm_events` з архівацією минулих прийомів (`archived:true`). Повний опис у `ROADMAP.md` секція Active. Обсяг — 1 сесія коду. При `/start` запропонуй Фазу 1.5 як Active-ціль + альтернативи (скасувати і йти на Фінанси 85%, або тестувати Фазу 1 руками до переходу далі).

**Що варто протестити перед Фазою 1.5 (на реальному пристрої Романа):**
- Відкрити вкладку Здоров'я — чи бачиш коралову картку алергій зверху?
- Тапнути "+ Додати" алергію — чи додається? (prompt-based UI)
- Якщо є старі картки хвороб — відкрити workspace, чи всі дані збереглись після lazy-міграції (медикаменти, doctorNotes)?
- Написати у Inbox "купив арахіс" (якщо додав "горіхи" у алергії) — чи OWL попереджає?

---

## Останні сесії

- **15.04 (jMR6m)** — **Фаза 1 Здоров'я ВИКОНАНА** (коміт `fab3865`) + план до неї + 3 нові правила процесу. Код: `src/tabs/health.js` (алергії CRUD, lazy-міграція, розширена структура, новий `getHealthContext`, оновлений workspace рендер), `src/ai/core.js` (підключення health у `getAIContext`), `index.html` (anchor алергій), `sw.js` (CACHE_NAME). Документація: `CLAUDE.md` (3 нові правила — "🗺️ Робота з ROADMAP", "🔍 Перевірка на узгодженість", посилена "Сперечайся", + дані `nm_allergies`, `nm_health_migrated_v2`), `ROADMAP.md` (5→6 фаз Здоров'я, Фаза 1 позначена виконаною, пункт 4.12 у Блок 4, підсекція у Ideas), `CONCEPTS_ACTIVE.md`, скіли `/obsidian` і `/start`.
- **15.04 (Lp0Ym)** — Аудит 6 вкладок + концепція Здоров'я. Документаційні файли: `CONCEPTS_ACTIVE.md` (нова секція "🏥 Здоров'я"), `ROADMAP.md` (аудит + 5 фаз Здоров'я), `_ai-tools/SESSION_STATE.md`. Код не чіпали.

- **13.04 (PBybp)** — **Блок 1 повністю закритий** (7 коммітів):
  1. `d171dbc` — `set_reminder` фікси (тап на reminder-картку → календар; іконки ⏰/📅/📌)
  2. `3afac8d` — 4.5 Блокування табло поки чат відкритий + 10 сек cooldown
  3. `5965e96` — G9 Page Visibility (не палити API коли `document.hidden`)
  4. `381a3a8` — G11 Дія замість питань (chips завжди мін 1, макс 3)
  5. `adae757` — G12 Мікро-розмови (ліміт тем, історія 4 обміни)
  6. `bd283a7` — 4.40 Auto-silence (3 ігнори → 4 год тиші)
  7. `b9c9c8a` — cleanup (прибрано мертвий `_markChatClosedNow`)

  Детальна розгорнута історія змін + файли → `ROADMAP.md` секція `✅ Done` (13.04 PBybp).

- **13.04 (EdsNg)** — консолідація плану у `ROADMAP.md` (232→449 рядків), архівування `FEATURES_ROADMAP.md`
- **13.04 (gQ2Hl)** — Фаза 1.1 (об'єднання табло), 1.2 (чат бачить історію табло), 3.6/3.7/3.8/3.10/3.11/3.13, `fix(memory)` захист від сміття
- **12.04 (YbLTH)** — 4.3 Semantic cooldowns, правило перспективи, правило "глибина відповіді" в CLAUDE.md
- **11.04 (FPKOV + B29hd)** — Крок 1 уніфікація Judge Layer, Крок 2 Структурована пам'ять фактів (`nm_facts`), бізнес-модель freemium зафіксовано
- **10.04** — 4.1 Tool Calling (25 tools), Live Chat Replies Фаза 2.0
- **09.04** — B-23/B-24/B-25 закриті, Календар повна переробка

---

## 🎯 Старт для нового чату

**Поточний пріоритет:** `ROADMAP.md` → `🚀 Active` → **Блок 2 — Концепції вкладок**. Блок 1 повністю закритий 13.04.2026 (сесія PBybp).

**Поточна ціль — переробка вкладки Здоров'я** (зараз ~55% після Фази 1). 6 фаз у `ROADMAP.md` секція Active:
1. ~~**Фаза 1** — Фундамент структур даних + `getAIContext()`~~ ✅ **Виконано 15.04 jMR6m коміт `fab3865`**
2. **Фаза 1.5** — Синк картка ↔ `nm_events` (**наступна у черзі**)
3. **Фаза 2** — Створення через Inbox + tool calling + **4.12 Антидублювання**
4. **Фаза 3** — UI переробка
5. **Фаза 4** — Супровід OWL (пасивні нагадування ліків, моніторинг)
6. **Фаза 5** — Експорт медкартки + решта інтеграцій

**При `/start`:** запропонуй **Фазу 1.5 Здоров'я** (синк картка ↔ `nm_events`) як основний варіант + альтернативи: (а) спершу ручне тестування Фази 1 на пристрої Романа до переходу далі, (б) перемикнутись на іншу вкладку — Фінанси 85% або Проекти 65%, (в) конкретні баги якщо з'являться.

---

## ⚠️ Для нового чату — найважливіше

1. **ПРАВИЛО №1** — пояснення в дужках ВСЕ не-українською. Деталі → `CLAUDE.md` секція "ОБОВ'ЯЗКОВО В КОЖНОМУ ПОВІДОМЛЕННІ"
2. **РОЗМІР ВІДПОВІДЕЙ** — середній (5-15 рядків). Не простирадла.
3. **Без "Роби" — код не чіпати.**
4. **ГЛИБИНА ВІДПОВІДІ** — читай код перед оцінкою, не "на пам'ять". Урок з PBybp: пункт `set_reminder` у ROADMAP виглядав як TODO, а насправді вже був зроблений у коміті `f1ab11f` з 10.04 — прочитав код і зрозумів це замість того щоб реалізовувати з нуля.
5. **ОДИН МОЗОК НА ВСЕ** — OWL = єдиний Jarvis. Повний план → `ROADMAP.md`
6. **Бізнес-модель (11.04)** — freemium + API на сервері Supabase Edge Functions (не BYOK)

---

## 🆕 Нові ключі localStorage (після сесії PBybp)

| Ключ | Тип | Призначення | Модуль |
|------|-----|-------------|--------|
| `nm_owl_silence_until` | string (ts) | 4.40 Auto-silence — до якого часу OWL мовчить | `proactive.js`, `inbox-board.js` |
| `nm_owl_ignored_msgs` | string (count) | Лічильник проігнорованих повідомлень поспіль | `proactive.js`, `chips.js` |
| `nm_owl_last_board_ts` | string (ts) | Таймстемп останнього згенерованого повідомлення табло | `proactive.js` |
| `nm_owl_last_chip_click_ts` | string (ts) | Таймстемп останнього кліку будь-якого чіпа | `chips.js` |

Повну таблицю всіх ключів → `CLAUDE.md` секція "Дані (localStorage)".

---

## Ключові файли (швидке нагадування)

| Файл | Опис |
|------|------|
| `src/ai/core.js` | `getAIContext`, `callAI`, `callAIWithTools`, 26 `INBOX_TOOLS`, OWL особистість. **+13.04:** `lastChatClosedTs` для 4.5, правило G12 у `universal`, `callOwlChat` історія 4 обміни |
| `src/ai/memory.js` | Структурована пам'ять фактів (`nm_facts`) — CRUD, TTL, міграція |
| `src/owl/inbox-board.js` | Judge Layer (`shouldOwlSpeak`). **+13.04:** guard 4.40 на початку `_judgeBoard`, жорсткий блок чат-4.5, `document.hidden` guard у `tryOwlBoardUpdate` |
| `src/owl/proactive.js` | Генерація табло, `getBoardContext`. **+13.04:** детектор 4.40 у `generateBoardMessage`, `document.hidden` guards у `tryTabBoardUpdate` + data-changed listener |
| `src/owl/followups.js` | Live Chat Replies. **+13.04:** `document.hidden` guard на початку `checkFollowups` |
| `src/owl/chips.js` | Центральний модуль чіпів. **+13.04:** `trackChipClick` скидає 4.40 counter + записує click ts; CHIP_PROMPT_RULES починається з правила G11 |
| `src/tabs/inbox.js` | `sendToAI`, tool calling. **+13.04:** `navigateInboxItem` обробляє reminder (→ Calendar modal), `_renderUpcoming` розрізнює іконки |
| `src/tabs/calendar.js` | `nm_events`, Calendar/Routine/Day-schedule модалки |

Повна таблиця файлів → `CLAUDE.md` секція "Файлова структура".
