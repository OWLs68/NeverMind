# Стан сесії

**Оновлено:** 2026-04-13 (сесія EdsNg — консолідація плану в `ROADMAP.md`)

---

## Проект

| | |
|--|--|
| **Версія** | v156+ (деплої через auto-merge) |
| **URL** | owls68.github.io/NeverMind |
| **AI модель** | OpenAI GPT-4o-mini з **Tool Calling** (26 tools, з них `save_memory_fact` з 11.04) |
| **Гілка** | `claude/start-session-EdsNg` |
| **Repo** | Public + LICENSE (All Rights Reserved) |

---

## 🗺️ Куди йде проект

**Дорожня карта — єдине місце:** [`ROADMAP.md`](../ROADMAP.md) у корені репо.

Там:
- `🚀 Active` — поточний пріоритет
- `📋 Next` — 7 послідовних блоків робіт
- `✅ Done` — останні закриті пункти з датами
- `💡 Ideas` — для обговорення
- `🚫 Rejected` — відхилені з причиною
- `🔒 After Supabase` — окремий блок

**Історичні документи** (повні обговорення Grok/Gemini/DeepSeek, анти-патерни, архітектурні принципи) → `_archive/FEATURES_ROADMAP.md`.

---

## Останні сесії

- **13.04 (EdsNg)** — **консолідація плану** у `ROADMAP.md` (4 коміти):
  1. `d0412d9` — створено `ROADMAP.md` (232 рядки), архівовано `FEATURES_ROADMAP.md` + старий `SESSION_STATE.md`, оновлено посилання у 5 файлах
  2. `b7cbe9c` — розширення: автори біля пунктів (Grok/Gemini/GPT-4o/DeepSeek), складність, повні описи, 6 відкритих питань для "Динамічного розпорядку", ризики Блоку 7, нові секції (🧭 Архітектурні принципи, 🔬 Perplexity дослідження, 🔗 Посилання). ROADMAP виріс 232→449 рядків — стратегічний документ з повним контекстом. У Блок 2 додано "Календар — доробка (~80%)" (6-та вкладка на доробці)
  3. `ce5d077` — оновлено SESSION_STATE і CHANGES з підсумком сесії
  4. `b70d4fd` — додано 5 запозичень з добірки скілів (`agent-core`, `notebookLM-skill`, `theme-factory`): retry для tool calls + `prompts.js` в Блок 1, AI-декомпозиція Проектів + інтелект-карта тижня у Я в Блок 2, готові теми в Блок 6, AI-теми за промптом в After Supabase
- **13.04 (gQ2Hl)** — Фаза 1.1 (об'єднання табло), 1.2 (чат бачить історію табло), 3.6 (Smart Boot-up), 3.7 (емпатія), 3.8 (прокрастинація), 3.10 (кореляції), 3.11 (навігаційні чіпи), 3.13 (OWL питає темп), `fix(memory)` захист від сміття
- **12.04 (YbLTH)** — 4.3 Semantic cooldowns, правило перспективи, fallback "Запам'ятав ✓", правило "глибина відповіді" в CLAUDE.md
- **11.04 (FPKOV + B29hd)** — Крок 1 уніфікація Judge Layer, Крок 2 Структурована пам'ять фактів (`nm_facts`), бізнес-модель freemium зафіксовано
- **10.04** — 4.1 Tool Calling (25 tools), Live Chat Replies Фаза 2.0
- **09.04** — B-23/B-24/B-25 закриті, Календар повна переробка (day schedule, drum picker, event-edit modal)

---

## 🎯 Старт для нового чату

**Поточний пріоритет:** `ROADMAP.md` → `🚀 Active` → **Блок 1** (малі фікси з великою цінністю).

Блок 1 містить 8 пунктів (кожен ~1 сесія):
1. `set_reminder` фікси — картка у стрічці Inbox + синхронізація з `nm_events`
2. 4.5 Блокування табло коли чат відкритий (тривіально)
3. G9 Page Visibility fix — не спалювати API коли вкладка неактивна
4. G11 "Дія замість питань" — чіпи з конкретними діями
5. G12 Мікро-розмови — ліміт 2-3 повідомлення на тему
6. 4.40 Auto-silence — проігнорував 3 → тиша 4 години
7. **Retry/fallback для tool calls** (новий, з `agent-core`) — прибирає тихі втрати записів юзера
8. **Винесення промптів у `src/ai/prompts.js`** (новий, з `agent-core`) — рефакторинг, знижує технічний борг

При `/start` запропонуй варіанти з цього Блоку 1 + альтернативи (Блок 2 концепції, конкретні баги якщо з'являться).

---

## ⚠️ Для нового чату — найважливіше

1. **ПРАВИЛО №1** — пояснення в дужках ВСЕ не-українською. Деталі → `CLAUDE.md` секція "ОБОВ'ЯЗКОВО В КОЖНОМУ ПОВІДОМЛЕННІ"
2. **РОЗМІР ВІДПОВІДЕЙ** — середній (5-15 рядків). Не простирадла. Деталі → `CLAUDE.md` секція "📏 РОЗМІР ВІДПОВІДЕЙ"
3. **Без "Роби" — код не чіпати.**
4. **ГЛИБИНА ВІДПОВІДІ** — читай код перед оцінкою, не "на пам'ять". Деталі → `CLAUDE.md` секція "🧠 Глибина відповіді"
5. **ОДИН МОЗОК НА ВСЕ** — OWL = єдиний Jarvis. Зміна в одній вкладці = зміна в усіх. Повний план → `ROADMAP.md`
6. **Бізнес-модель (11.04)** — freemium + API на сервері Supabase Edge Functions (не BYOK). `nm_gemini_key` у localStorage — тимчасове рішення до Supabase

---

## Ключові файли (швидке нагадування)

| Файл | Опис |
|------|------|
| `src/ai/core.js` | `getAIContext`, `callAI`, `callAIWithTools`, 26 `INBOX_TOOLS`, OWL особистість |
| `src/ai/memory.js` | Структурована пам'ять фактів (`nm_facts`) — CRUD, TTL, міграція |
| `src/owl/inbox-board.js` | Judge Layer (`shouldOwlSpeak` з channel-aware routing), getOwlBoardContext |
| `src/owl/proactive.js` | Генерація табло, `getBoardContext` (єдина для всіх вкладок з 13.04) |
| `src/owl/followups.js` | Live Chat Replies (stuck-task, event-passed) — делегує Judge Layer |
| `src/owl/chips.js` | Центральний модуль чіпів — рендер, клік, fuzzy match ✔️ |
| `src/tabs/inbox.js` | `sendToAI` на tool calling, `processSaveAction`, `_detectEventFromTask` |
| `src/tabs/calendar.js` | `nm_events`, Calendar/Routine/Day-schedule модалки, drum picker, zoom |

Повна таблиця файлів → `CLAUDE.md` секція "Файлова структура".
