# Скіли Claude Code — тригери авто-активації

> **Правило:** Claude **сам** викликає скіл коли бачить у запиті Романа тригер-фразу зі списку нижче. Не чекає `/<name>`. Це інструменти розробки, не фічі NeverMind.
>
> **Як працює:** при старті сесії Claude читає цей файл. Коли Роман просить щось — Claude сканує запит на тригери. Якщо збіг → читає `.claude/commands/<name>.md` ДО початку роботи.
>
> **Статус 17.04.2026 (сесія gHCOh):** 7 скілів написані + 1 новий (`/refactor-large`) запланований. **Автоматична активація за тригерами нижче.**

---

## 🟢 Активні скіли (спрацьовують щосесії)

### `/ux-ui` — дизайн-система перед UI-змінами

**Файл:** `.claude/commands/ux-ui.md`

**Тригери (активуй якщо у запиті):**
- слова: `модалка`, `модалку`, `модалки`
- слова: `стиль`, `стилізація`, `колір`, `кольори`, `фон`, `padding`, `margin`
- слова: `компонент`, `картка` (у UI-контексті), `кнопка` (стилізація)
- слова: `дизайн`, `оформлення`, `візуально`
- фрази: `білий фон`, `прозорий фон`, `glass-ефект`, `фіолет`
- фрази: `не виглядає`, `виглядає погано`, `треба переробити стилі`

**Що робить:** читає `docs/DESIGN_SYSTEM.md`, блокує фіолет, білі модалки, padding не на outer panel. Перевіряє проти реальних компонентів.

---

### `/prompt-engineer` — зміна промптів OWL

**Файл:** `.claude/commands/prompt-engineer.md`

**Тригери:**
- слова: `промпт`, `промпти`
- слова: `галюцинує`, `галюцинація`, `вигадує` (про AI)
- фрази: `OWL дивно відповідає`, `AI не розуміє`
- фрази: `тон агента`, `антигалюцинації`
- фрази: `змінити промпт`, `переписати промпт`

**Що робить:** карта 12 промптів у коді + єдиний шаблон (Роль→Контекст→Правила→Антигалюцинації→Приклади→Формат).

---

### `/pwa-ios-fix` — iOS Safari баги

**Файл:** `.claude/commands/pwa-ios-fix.md`

**Тригери:**
- слова: `iOS`, `iPhone`, `Safari`, `PWA`
- слова: `bfcache`
- фрази: `не оновлюється на телефоні`, `застрягло на старій версії`
- фрази: `клавіатура перекриває`, `overscroll`, `bounce`
- фрази: `SW не реєструється`, `Service Worker`
- фрази: `home screen`, `standalone`

**Що робить:** перевіряє що вже є у `boot.js`, додає тільки відсутнє. Чекліст iOS: SKIP_WAITING, network-first, viewport-fit, tap delay.

---

### `/refactor-large` — рефакторинг великих файлів ⭐ НОВИЙ

**Файл:** `.claude/commands/refactor-large.md` (створити якщо ще немає)

**Тригери:**
- слова: `рефакторинг`, `розбити файл`, `винести у модулі`
- фрази: `занадто великий`, `split file`, `перенести функції`
- слова: `модульність`, `модуляризація`
- контекст: робота з файлом >500 рядків

**Що робить:** застосовує правило №1 з CLAUDE.md "🔧 Робочий процес" (skeleton+Edit для файлів >250 рядків) + правило №2 (checkpoint-коміти після кожної фази). Створює план фаз, виконує послідовно з комітами між ними.

---

## 🟡 Умовно активні (перед подією)

### `/owl-motion` — анімація сови

**Блокується:** потрібна багатошарова SVG від Романа.

**Тригери:** `анімація сови`, `OWL animation`, `motion`, `сова рухається`, `idle state`.

**Що робить:** 5 станів (idle/alert/thinking/error/greeting), чистий CSS keyframes без бібліотек.

---

### `/supabase-prep` — перед міграцією на Supabase

**Тригери:** `Supabase`, `міграція БД`, `backend`, `переносимо на бекенд`, `хмарне сховище`, `синк між пристроями`, `offline queue`, `retry`, `exponential backoff`.

**Що робить:** патерн міграції, retry з exp backoff, offline queue, error handling.

---

## 🔴 Планово активні (відкладені тригери)

### `/a11y-enforcer` — WCAG 2.1 AA

**Час активації:** перед публічним релізом (після стрес-тесту 20-30 юзерів).

**Тригери (не активувати до релізу):** `accessibility`, `доступність`, `a11y`, `WCAG`, `публічний реліз`, `screen reader`, `VoiceOver`, `aria-labels`.

---

### `/gamification-engine` — Блок 3 ROADMAP

**Час активації:** Блок 3 (після Блоку 2 — концепції вкладок).

**Тригери:** `стрік`, `streak`, `прогрес`, `ачівка`, `achievement`, `progress bar`, `мікро-анімація`.

**❌ Табу:** `бали`, `XP`, `рейтинги`, `leaderboard`, блокуючі "Вітаємо!".

---

## Самоперевірка перед виконанням скіла

**Згідно з "🛡️ Аудит скілів" з CLAUDE.md:**
1. Чи торкається пунктів "Що не можна змінювати без обговорення"?
2. Чи DOM-селектори/CSS-класи/функції реально існують у коді?
3. Чи не дублює існуючу архітектуру?
4. Чи нагадує про `CACHE_NAME` bump при зміні коду?
5. Чи делегує на `/new-file` при створенні нових JS-модулів у `src/`?

**Якщо порушення — зупинись і повідом Роману.** Не виконуй скіл самочинно.

---

## Якщо не впевнений чи тригер спрацював

**Краще викликати скіл** ніж проігнорувати. Ціна помилки: пара зайвих секунд читання `.claude/commands/<name>.md`. Ціна ігнору: баг який скіл би запобіг.

---

## Відкладено / відкинуто

- **Gemini SVG Creator** — об'єднати з B-56 (40 іконок категорій Фінансів)
- **Anime.js / Lottie / Rive** — відкинуто на користь чистого CSS keyframes (без +20KB)
- **Remotion Master**, **Social Carousel** — не наш продукт

---

## Ресурси

- [code.claude.com/docs/en/skills](https://code.claude.com/docs/en/skills) — офіційна дока формату
- [hesreallyhim/awesome-claude-code](https://github.com/hesreallyhim/awesome-claude-code)
- [sickn33/antigravity-awesome-skills](https://github.com/sickn33/antigravity-awesome-skills) — 1400+ готових скілів
