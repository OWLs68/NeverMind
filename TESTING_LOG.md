# Журнал тестування NeverMind

> **Що це:** список конкретних тестів які треба зробити на iPhone + архів того що вже протестили + повторювані баги.
>
> **Для кого:** Роман (тестує) + Claude (бачить що з минулого ще не закрито).
>
> **Правило:** не «треба протестити» абстрактно. Тут — конкретні дії: «тап ✓ задачі рукою», «свайп вліво на нотатці», «модалка категорії після 18:00».
>
> **Створено:** 29.04.2026 (lRnXU) — після 6 сесій підряд відкладеної iPhone-перевірки.

---

## ⏳ Чекає тесту (TODO)

> Тут живе те що треба перевірити **наступної iPhone-сесії**. Claude додає сюди після кожної міграції / нової фічі / зміни UI. Роман викреслює коли протестив (переносить у архів).

### v906+ (deploy 16.05 — DGH6F Event Delegation Phase 1д: projects.js (5 onclick))

**Контекст:** 5 onclick у `src/tabs/projects.js` мігровано. 5 нових actions у delegation.js (registry 18→23). Council Pre-mortem знайшов 1 pre-existing баг (`_syncProjectStepToTasks` fuzzy match) — НЕ цієї сесії, окремий ticket.

**Зміни:**
- Картка проекту → `data-action="open-project"`
- «← Назад» з workspace → `data-action="close-project-workspace"`
- «Розгорнути ↓» timeline → `data-action="toggle-project-timeline"`
- Чекбокс крока (всередині workspace) → `data-action="toggle-project-step"`
- «📝 Нотатки» (cross-tab з папкою) → `data-action="open-notes-folder"` data-folder (handler інкапсулює `switchTab('notes') + setTimeout(150, openNotesFolder)`).

**📁 Projects — workspace flow:**
- [ ] Projects-tab → бачу картки проектів.
- [ ] Тап на картку → відкривається workspace проекту з steps + Notes-блоком.
- [ ] У workspace «← Назад» → повертаємось до списку.
- [ ] «Розгорнути ↓» timeline → timeline expands.
- [ ] У workspace чекбокс крока → крок toggleиться (галочка). ⚠️ Перевір що не закрилася ЧУЖА задача через fuzzy match (`_syncProjectStepToTasks` — pre-existing баг ще до delegation, окремий ticket).
- [ ] Кнопка «📝 Нотатки» (cross-tab) → перехід на Notes-tab → через 150ms відкривається папка з ім'ям проекту.

**🔧 Регресія-чек:**
- [ ] DevTools Console → жодних `escapeJsArg is not defined` (видалив unused import).
- [ ] DevTools Console → жодних `Cannot read properties of undefined (reading 'id')`.

### v905+ (deploy 16.05 — DGH6F Event Delegation Phase 1г: board.js + evening.js (9 handler'ів))

**Контекст:** 2 файли мігровано одним коммітом (обидва без cross-file race). Council паралельно 3 агенти Sonnet (по файлу). 5 нових universal+specific actions у delegation.js (registry 13→18).

**board.js (3 onclick → 0):**
- `toggleOwlTabChat('${t}')` → `data-action="toggle-owl-collapsed"`
- `scrollOwlTabChips('${t}',-1)` → `data-action="scroll-owl-chips" data-dir="-1"`
- `scrollOwlTabChips('${t}',1)` → `data-action="scroll-owl-chips" data-dir="1"`
- ⚠️ `ontouchstart/move/end owlTabSwipe*` (3 події з координатами) — НЕ мігровано (Pre-mortem 🔴: triple-event state chain, потребує окремого swipe-helper. Phase 1+v окрема задача).

**evening.js (6 onclick → 0):**
- `openMomentView('${m.id}')` → `data-action="open-moment-view"`
- `event.stopPropagation();deleteMoment('${m.id}')` → `data-action="delete-moment"` (stopPropagation НЕ потрібен у delegation — closest повертає найближчий).
- `rescheduleTaskTomorrow/Week` → universal `data-action="reschedule-task" data-days="1|7"`
- `holdQuitHabit('${h.id}');renderEvening()` → `data-action="hold-quit-habit"` (handler сам викликає renderEvening)
- `confirmQuitRelapse('${h.id}');setTimeout(renderEvening,50)` → `data-action="confirm-quit-relapse"` (setTimeout збережено)

**🦉 OWL Board (всі 8 вкладок):**
- [ ] Inbox-tab → бачу OWL у згорнутому стані → тап на 🦉 (collapsed) → розгортається у speech bubble.
- [ ] OWL у tab-чаті → chips під ним → стрілки ‹ і › для прокрутки → працюють.
- [ ] Swipe сvгору на OWL avatar → згортає (не зачіпали — має працювати як раніше).

**🌙 Evening — moments:**
- [ ] Evening-tab → блок «Моменти дня» → ТАП на moment-картку → відкривається view-модалка з деталями.
- [ ] Тап на «✕» moment-картки → момент видаляється, view-модалка НЕ відкривається (стара проблема залишилась пофікшеною).
- [ ] Evening → блок «Завтра / На тиждень» → тап «Завтра» / «На тиждень» → задача перепланована.

**🚭 Evening — quit habits:**
- [ ] Якщо є quit-habit (звичка яку кидаю) → блок з кнопками «Тримаюсь» / «Зірвався».
- [ ] Тап «Тримаюсь» → стрік збільшується, картка перерендериться.
- [ ] Тап «Зірвався» → дзвонить confirmQuitRelapse → стрік скидається, через 50ms перерендериться.

**🔧 Регресія-чек:**
- [ ] DevTools Console на iPhone → жодних `Uncaught ReferenceError` чи `is not a function`.
- [ ] OWL chips arrows → клік не «провалюється» крізь до батьківського OWL bubble.

### v904+ (deploy 16.05 — DGH6F Event Delegation Phase 1в-a: tasks.js 4/5 handler'ів)

**Контекст:** Phase 1в-prep (комміт c8803c2) додав `touch-action: manipulation` у `style.css` для `[data-task-check]` + `[data-step-check]` — fast-tap забезпечує CSS, не JS preventDefault. Phase 1в-a мігрує 4 inline handler'и tasks.js на delegation:

- `toggleTempStep('${s.id}')` (рядок 150) → `data-action="toggle-temp-step"` (специфічний — edit-модалка)
- `removeTempStep('${s.id}')` (152) → `data-action="remove-temp-step"` (специфічний — edit-модалка)
- `taskCardClick('${task.id}', event)` (273) → `data-action="task-card-click"` (специфічний — передає event для guard)
- `ontouchend toggleTaskStatus` (276) → `data-action="toggle-entity-done" data-entity="task"` (UNIVERSAL — DRY Scanner: заміняє 8 onclick у habits.js + tasks.js при майбутній міграції)

Phase 1в-b ВIДКЛАДЕНО: 5-й handler (`[data-step-check]` рядок 290 з ontouchstart+ontouchend + координатна swipe-vs-tap) потребує окремий `src/ui/touch-detect.js` helper — окрема задача.

**📋 Tasks — core daily flow (ОБОВ'ЯЗКОВО на iPhone):**
- [ ] Tasks-tab → бачу список задач.
- [ ] Тап на ГАЛОЧКУ задачі (квадратик біля назви) → задача стає completed (зелена), БЕЗ затримки 300ms (fast-tap працює через `touch-action: manipulation`).
- [ ] Тап на ТIЛО задачі (не галочку) → відкривається edit-модалка.
- [ ] Натиснути «+» додати задачу → ввести текст + крок «зробити Х» → крок з'являється у списку (`renderTempSteps`).
- [ ] У edit-формі: тап на checkbox крока → крок toggleиться (done/not-done).
- [ ] У edit-формі: тап на «×» крока → крок видаляється з форми.

**🔧 Регресія-чек:**
- [ ] DevTools (Safari → iPhone) Console → жодних `Uncaught ReferenceError`.
- [ ] Швидко тапати галочку 5 разів поспіль → стан правильно змінюється кожен раз (без race).
- [ ] Тап на галочку НЕ повинен відкривати edit-модалку (guard у taskCardClick працює).

**ℹ️ Що ще ЗАЛИШИЛОСЬ як inline handler у tasks.js (Phase 1в-b, не зараз):**
- `[data-step-check]` рядок 290 — координатна swipe-vs-tap логіка (toggleTaskStep). Працює як раніше через ontouchstart+ontouchend.

### v903+ (deploy 16.05 — DGH6F Event Delegation Phase 1б3: inbox.js 4 onclick)

**Контекст:** 4 onclick у `src/tabs/inbox.js` переведено на event delegation:
- (#1+#2) upcoming-картка `switchTab('tasks')` / `openCalendarModal()` (рядок 269-271) → `data-action="switch-tab" data-tab="tasks"` / `data-action="open-calendar"`
- (#3) inbox-картка `navigateInboxItem('${item.id}')` (317) → `data-action="navigate-inbox-item"` (data-id уже на елементі)
- (#4) clarify-модалка `selectClarifyOption(${i})` (1104) → `data-action="select-clarify-option" data-idx="${i}"` (Pre-mortem 🔴 захист: `parseInt(data.idx)` у handler)

3 нові actions у delegation.js: `open-calendar`, `navigate-inbox-item`, `select-clarify-option`.

**📥 Inbox — основний потік (КОЖНОДЕННЕ використання):**
- [ ] Inbox-tab → пишу довільний текст «купив каву 3 євро» → AI створює фінансову транзакцію.
- [ ] Inbox-стрічка показує картку категорії → ТАП на неї → перехід у відповідну вкладку (Finance → транзакція виділена).
- [ ] Inbox upcoming-блок (зверху над чатом) → показує найближчі задачі/події.
- [ ] Тап на upcoming-картку **task** → перехід у Tasks.
- [ ] Тап на upcoming-картку **event/reminder** → відкривається модалка Календаря.

**🤔 Inbox — clarify модалка (edge case, перевір якщо буде):**
- [ ] Пишу неоднозначне «Лікар прописав парацетамол» у Inbox (без створеної картки Здоров'я).
- [ ] AI відкриває clarify-модалку «Що ти маєш на увазі?» з 2-3 опціями-кнопками.
- [ ] Тап на ПЕРШУ опцію → AI продовжує з вибраним варіантом.
- [ ] Тап на ДРУГУ опцію → AI йде іншою гілкою.
- [ ] DevTools Console → НЕ повинно бути `selectClarifyOption is not a function` чи `parseInt` помилок.

**🔧 Регресія-чек (DevTools на iPhone):**
- [ ] Шукай у Elements `onclick="navigateInboxItem"` → має повертати 0 результатів.
- [ ] Швидко тапати поспіль 5 inbox-карток → жодних дублікатів переходів / лагів.

### v902+ (deploy 16.05 — DGH6F Event Delegation Phase 1б2: onboarding fv-tip close)

**Контекст:** 1 onclick у `onboarding.js:616` (`this.closest('#fv-tip').remove()` для першого-візит tip) переведено на event delegation. Новий універсальний action `close-parent` у delegation.js (reuse для будь-якої кнопки `✕` що закриває модалку/tip через CSS selector батька — наприклад `health.js:1333`).

**👋 Onboarding — first-visit tips:**
- [ ] Очистити localStorage (Налаштування → Видалити всі дані) для імітації першого візиту.
- [ ] Перезавантажити NeverMind → з'являється підказка-tip (#fv-tip) з кнопкою ✕.
- [ ] Тап на ✕ → tip зникає одразу, без анімації-затримки.
- [ ] DevTools Console → жодних `Cannot read properties of null (reading 'remove')` чи `closest is not a function`.

### v901+ (deploy 16.05 — DGH6F Event Delegation Phase 1б1: me.js project card)

**Контекст:** 1 onclick у `me.js:185` (`renderProjects` рендер — картка проєкту з кліком що переходить на Projects-tab) переведено на event delegation. Використано новий універсальний action `switch-tab` у delegation.js (data-tab="X"). Reuse pattern для решти ~30 `switchTab(...)` onclick у проекті.

**🧠 Me — project cards:**
- [ ] Me-tab → блок «Активні проекти» → бачу свої проекти.
- [ ] Тап на картку будь-якого проекту → перехід на вкладку Projects, картка відкрита.
- [ ] DevTools Console → жодних `Uncaught ReferenceError: switchTab is not defined`.
- [ ] Швидкий тап двічі на ту саму картку → не лагне, не дублює перехід.

### v900+ (deploy 16.05 — DGH6F Event Delegation Phase 1а: header buttons + freeze hook)

**Контекст:** 16 inline onclick у `<header>` кожної вкладки (8 tabs × `openSettings`+`openHelp`) переведено на event delegation через новий модуль `src/core/delegation.js`. Strangler refactor — перший крок з ~334 onclick → 0 для strict CSP. Решта 318 onclick — наступні сесії. Pre-commit hook `pre-commit-onclick-freeze.js` (новий, 8-й сторож) блокує нові onclick через net-rachet.

**⚙️ Header buttons (всі 8 вкладок):**
- [ ] Inbox → тап на «⚙️» (шестерінка у правому верхньому куті) → модалка «Налаштування» відкривається.
- [ ] Inbox → тап на «?» → модалка «Допомога» відкривається з контентом для Inbox.
- [ ] Tasks → ⚙️ і ? — те саме (контент Help відрізняється — для Tasks).
- [ ] Notes → ⚙️ і ? — те саме.
- [ ] Me → ⚙️ і ? — те саме (виглядає трохи прозорим колір — це нормально, тема Me).
- [ ] Evening → ⚙️ і ? — те саме (колір приглушений — тема Evening).
- [ ] Finance → ⚙️ і ? — те саме.
- [ ] Health → ⚙️ і ? — те саме.
- [ ] Projects → ⚙️ і ? — те саме.

**🔧 Регресія-чек:**
- [ ] DevTools (Safari → Develop → iPhone) → Console → жодних `Uncaught ReferenceError: openSettings/openHelp is not defined` при тапі.
- [ ] Швидко натиснути ⚙️ потім зразу ? (race) → обидві модалки відкриваються коректно.

**ℹ️ Що НЕ змінилось (sanity-чек):**
- [ ] Кнопки виглядають візуально ідентично як до фічі (одна іконка, той самий розмір).
- [ ] Anti-bug: 16 onclick видалено з index.html — пошук `onclick="openSettings"` / `onclick="openHelp` у DevTools Elements має повертати 0.

### v568+ (deploy 03.05 — MIeXK Phase A: 6-статусна шкала Health + міграція legacy)

**Контекст:** замінили старі 3 статуси (active/controlled/done) на 6 нових (acute/treatment/improving/remission/chronic/done) з іконками. Всі існуючі картки автоматично мігрували: active→treatment, controlled→remission, done→done (одноразово при першому відкритті після деплою). Workspace картки тепер показує 6 кнопок-перемикачів замість 3. Інтерв'ю після створення (Phase C) і новий tool update_health_card_status (Phase B) — окремі коміти.

**🩺 Health — міграція + рендер списку:**
- [ ] Відкрити вкладку Здоров'я → старі картки відображаються з новими бейджами: «💊 Лікування» замість «Активне», «🟢 Контроль» замість «Під контролем», «✅ Завершено» без змін.
- [ ] Прогрес-бар і opacity сірих (Завершено) як раніше — без візуальних регресій.

**🩺 Health — workspace picker (тап на картку):**
- [ ] Тап на картку → відкривається workspace → під назвою новий бейдж + ряд з 6 кнопок (з flex-wrap якщо тісно): 🆕 Гостра / 💊 Лікування / 📈 Покращення / 🟢 Контроль / ♾️ Хронічна / ✅ Завершено.
- [ ] Тап на «📈 Покращення» → бейдж змінюється на «📈 Покращення», прогрес → 60%, у timeline (історії) додається запис «Лікування → Покращення» з іконкою тренду.
- [ ] Тап на «✅ Завершено» → бейдж + прогрес 100% + картка стає сірою у списку.
- [ ] Жодна кнопка не фіолетова (chronic — темно-індиго #1e1040, не violet).

**🩺 Health — створення нової картки:**
- [ ] «+» → модалка → ввести «Спина», зберегти → у списку з'являється з бейджем «💊 Лікування» (default після прибраного блоку Статус з 4xJ7n).

**🩺 Health — новий tool `update_health_card_status` (Phase B):**
- [ ] У чат-барі Здоров'я вибрати картку → написати «закрив, повністю одужав» → AI має викликати `update_health_card_status(card_id, 'done')` → бейдж «✅ Завершено», прогрес 100%, у timeline запис «Лікування → Завершено».
- [ ] Написати «у ремісії» → статус «🟢 Контроль», прогрес 80%.
- [ ] Написати «це хронічна» → статус «♾️ Хронічна» (темно-індиго бейдж, не фіолет).

**🩺 Health — AI-інтерв'ю після створення картки (Phase C):**
- [ ] Відкрити Здоров'я → «+» → ввести «Спина», зберегти → автоматично у Health-чаті з'являється повідомлення «Створив картку "Спина". Що зараз?» + 4 чіпи [🆕 Щойно з'явилось] [💊 Лікую] [♾️ Хронічна] [Пропустити].
- [ ] Тап «🆕 Щойно з'явилось» → попереднє питання + чіпи замінюються на «Лікар призначив лікування?» + чіпи [Так] [Не був у лікаря] [Сам лікую] [Пропустити].
- [ ] Тап «Так» → «Симптоми зараз?» + [Сильні] [Помірні] [Майже нема] [Пропустити].
- [ ] Тап «Сильні» → «Записав. Статус "Спина": 💊 Лікування.» Картка отримує статус treatment, прогрес 40%.
- [ ] Альтернативний шлях: створення через Inbox AI («у мене вже тиждень болить спина») → AI створює картку → у Inbox чаті повідомлення «Створив картку "Спина" у Здоровʼї. Пройди опитування там...» → у вкладці Здоров'я над кнопкою Send червона крапка → відкрив Health-чат → бачить повне інтерв'ю з чіпами.
- [ ] Якщо юзер ігнорить (закрив чат, перейшов на іншу вкладку, повернувся через день) — інтерв'ю чекає у Health-чаті, чіпи активні. Тап чіпа продовжує з тієї ж точки.
- [ ] Тап «Пропустити» на step 1 → «Гаразд, без опитування. Статус можна змінити з картки.» Картка лишається з default статусом «💊 Лікування».
- [ ] Не фіолетові кольори ніде (chronic темно-індиго).

**Контекст:** запобігання B-115 регресії через soft safety net. Коли AI пропонує create_project / create_event / save_task / save_moment / save_note для повідомлення з минулим часом дієслова АБО голим іменником без числа і команди — guard перехоплює і показує 3 інлайн-чіпи: «У щоденник» (save_note до папки Особисте) / «Як момент» (save_moment) / «Не зберігати». Клік чіпа виконує save локально через диспетчер, БЕЗ повторного запиту до AI. Працює тільки в Inbox у Phase 1; Phase 2 розкатить на Notes/Health/Finance/Evening/Projects/Me. CACHE_NAME `nm-20260502-1810`.

**🦉 Inbox — clarify-guard для минулого часу:**
- [ ] Юзер пише «Відкрив автомийку» → НЕ створюється проект/подія. Замість того сова пише «"Відкрив автомийку" — куди це записати?» + 3 чіпи [У щоденник] [Як момент] [Не зберігати]. Тап «У щоденник» → нотатка з текстом «Відкрив автомийку» з'являється у папці Особисте. Тап «Як момент» → запис у вкладці Вечір (моменти дня). Тап «Не зберігати» → нічого не зберігається, сова пише «Не зберігаю.».
- [ ] «Запустив сайт» → той самий механізм (3 чіпи). Тап «Як момент» → момент створюється.
- [ ] «Хімчистка» (голий іменник, без дієслова) → той самий механізм 3 чіпи.
- [ ] Тап чіпа НЕ викликає AI повторно (швидкий, без spinner).

**🦉 Inbox — НЕ ламає звичайні випадки (виключення працюють):**
- [ ] «Купив каву на 50» → одразу save_finance, БЕЗ чіпів. Сума → guard пропускає.
- [ ] «кава 60» → save_finance, БЕЗ чіпів.
- [ ] «Купити хліб завтра» → save_task (інфінітив + дата), БЕЗ чіпів.
- [ ] «Зустріч з Андрієм 15 травня» → create_event (дата у тексті), БЕЗ чіпів.
- [ ] «Створи проект Хімчистка» → create_project (явна команда), БЕЗ чіпів.
- [ ] «Запам'ятай що я не їм глютен» → save_memory_fact, БЕЗ чіпів.

**🦉 Inbox — повторне виявлення B-115:**
- [ ] «Відкрив автомийку» (тестування рукою) → guard ловить і показує чіпи. На відміну від вчорашнього теста (де AI створив дубль проекту з невірною назвою «Хімчистка») — тепер AI просто не виконує tool, юзер сам тапає правильний варіант.

**🦉 Phase 2 — guard у 6 чатах (Notes, Health, Finance, Evening, Projects, Me):**
- [ ] Відкрити чат у вкладці Нотатки → написати «Купив костюм» → 3 чіпи [У щоденник] [Як момент] [Не зберігати]. Тап «У щоденник» → нотатка створюється (не задача).
- [ ] Чат у вкладці Здоров'я → «Прийняв таблетку» → guard НЕ спрацьовує (це log_medication через існуючу логіку Health). Звичайна поведінка лишається.
- [ ] Чат у Фінанси → «кава 60» → save_finance, БЕЗ чіпів (число → виключення).
- [ ] Чат у Вечір (вранішній чат-бар) → «Запустив сайт» → 3 чіпи. Тап «Як момент» → момент дня створюється.
- [ ] Чат у Проекти → «Закрив угоду» → 3 чіпи (минулий час без числа).
- [ ] Чат у Я → «Хімчистка» (голий іменник) → 3 чіпи.

### v530+ (deploy 01.05 — LW3j8: HOT_RULES + i18n обгортки finance-modals/notes + рефакторинг категорій нотаток)

**Контекст:** сесія LW3j8 01.05 — створено `_ai-tools/HOT_RULES.md` + самотест-хук для активного засвоєння правил на старті, обгорнуто 24 рядки у `finance-modals.js` (модалки дати/бюджету/категорії повністю), 30 рядків у `notes.js` (запасна назва папки + написи модалки + повідомлення чату + назви кольорів папок), рефакторинг категорій папок нотаток через канонічний довідник `src/data/notes-categories.js` (видалено 2 старі мапи з українськими ключами, переписано 3 функції-споживачі).

**📝 Нотатки — категорії папок (рефакторинг через `src/data/notes-categories.js`)**
- [ ] Папка `'Харчування'` — іконка тарілки/їжі і крапка 🥑 на місці
- [ ] Папка `'Робота'` — іконка валізки і крапка 🎯
- [ ] Папка `"Здоров'я"` (з апострофом) — іконка серця і крапка 💪
- [ ] Стара папка `'Здоровя'` (БЕЗ апострофа, legacy від тестерів) — теж іконка серця і крапка 💪 (працює через нормалізацію)
- [ ] Папка `'Ідеї'` — іконка лампочки і крапка 💡
- [ ] Нова папка з невідомою назвою (наприклад `'Кулінарія'` або `'Хобі'`) — fallback на іконку-блокнот, крапка 📝
- [ ] Папка з кастомною іконкою (раніше через UI вибрав свою) — лишається з вибраною іконкою (логіка `getFolderMeta` збережена)
- [ ] Авто-присвоєння іконки: створити нову папку з ім'ям категорії (наприклад `'Спорт'`) — автоматично береться іконка `sport`
- [ ] AI-команда «створи нотатку про каву у папку Кава» — папка створюється з іконкою чашки

**💰 Фінанси — модалка дати/бюджету/категорії (24 обгортки)**
- [ ] Тап на дату транзакції → модалка з кнопками «Сьогодні · 1 трав», «Вчора · 30 квіт», «Позавчора · 29 квіт», «Тиждень тому · 24 квіт»
- [ ] Лейбл «Виберіть день» + календар + кнопка «Закрити» — все на місці
- [ ] Тап на «Бюджет на місяць» → модалка з заголовком, лейблом «Загальний ліміт», placeholder «€ 0 — без ліміту», списком категорій, кнопками «Скасувати»/«Зберегти»
- [ ] Placeholder у полях категорій — «без ліміту»
- [ ] Тап на категорію (режим редагування) → модалка з заголовком «Редагувати категорію» (або «Нова категорія» якщо нова), тогли «Витрата»/«Дохід» (для нової), лейбл «Назва», placeholder «напр. Подорожі»
- [ ] Лейбли «Вигляд» (іконка+колір) і «Підкатегорії» з кнопкою «+ підкатегорія»
- [ ] Тап на іконку → модалка «Обери іконку» з сіткою; на колір → «Обери колір»
- [ ] Тогл «Архівувати» з підказкою «Сховати з сітки, дані зберігаються»
- [ ] Кнопки знизу: «Видалити», «Скасувати», «Зберегти» (або «Створити» для нової)
- [ ] Спроба зберегти без імені → toast «Введи назву»
- [ ] Спроба видалити → confirm «Видалити категорію? Транзакції збережуться, але без візуального кружечка.»

**🎨 Нотатки — кольори у picker'і папки**
- [ ] Тап «···» на картці папки → modal редагування папки → picker кольорів показує 8 варіантів з підписами «Пісок», «Блакитний», «Зелений», «Жовтий», «Рожевий», «Фіолетовий», «Оранжевий», «Сірий»

**💬 Нотатки — повідомлення чату нотатки**
- [ ] Без OpenAI ключа → агент пише «Введи OpenAI ключ в налаштуваннях…»
- [ ] AI оновив нотатку через чат → toast «✓ Нотатку оновлено.»
- [ ] AI не знайшов нотатку → «Не вдалося знайти нотатку.»
- [ ] Втрата інтернету → «Мережева помилка.»
- [ ] Кнопка «+ Зберегти як нотатку» під відповіддю агента — на місці

**💬 Нотатки — нижня AI-плашка вкладки Нотатки**
- [ ] AI не знайшов нічого → «Нічого не знайдено по запиту "X".»
- [ ] AI знайшов кілька → «Знайдено N:»
- [ ] AI відкрив папку → «Відкрив папку "X".»
- [ ] AI не знайшов папку → «Папку "X" не знайдено. Доступні: ...»
- [ ] AI відкрив нотатку → «Відкрив нотатку.»
- [ ] AI не знайшов нотатку → «Нотатку не знайдено.»
- [ ] Будь-яка дія через AI → коротке «Зроблено ✓»

**🎓 HOT_RULES + самотест-хук (`/start` оновлено)**
- (Це для Claude, не для тебе) — у наступній сесії після `/start` я мушу видати блок `📋 Самотест:` з 3 відповідями своїми словами ПЕРЕД першою реплікою. Якщо я скочуся одразу до «Прочитав, проект…» — самотест пропущений, нагадай мені.

**⚠️ Якщо знайдеш баг:**
- Скрін + опис у чат
- Якщо іконка папки зникла після рефакторингу — критично, відкочу `446f848` і `03f70c7`
- Якщо кнопка/лейбл на іншій мові показує неправильно — це не баг рефакторингу, а недопрацьовка fallback тексту в `t()`

---

### v504+ (deploy 30.04 — xHQfi: Silence UI badge + cancel_quiet + Typed Cooldowns + Silent Reply + блокувальний Stop-хук)

**Контекст:** велика сесія xHQfi 30.04 — Фаза 1 OWL Silence добита (cancel + UI), Фаза 4 OWL V3 (typed cooldowns + silent reply на короткі дії), полагоджено `pre-push-check` + блокувальний Stop-хук для повідомлень. Усі зміни код-фронту чекають реальної перевірки.

**🤫 OWL Silence — UI плашка і скасування (xHQfi 30.04 — коміти 44bf7fe, da057ae)**
- [ ] У будь-якому чаті написати «дай спокій до вечора» — сова відповідає «🤫 Зрозуміла. Мовчу до 22:00»
- [ ] Зверху board зʼявилась бурштинова плашка «🤫 Сова мовчить до 22:00. Тапни щоб скасувати»
- [ ] Перейти на іншу вкладку (Задачі → Фінанси → Я → Інбокс) — плашка є СКРІЗЬ
- [ ] Тап на плашку → плашка зникає миттєво на цій вкладці
- [ ] Перейти на іншу вкладку — там плашки теж немає
- [ ] Голосове скасування: знов «дай спокій на годину», потім «можеш говорити» / «повертайся» — сова: «🦉 Окей, повернулась»
- [ ] Інші тригер-фрази тиші: «не доставай» / «помовчи» / «не зайобуй» — кожна включає тишу
- [ ] Чат під час тиші — спитати «що я зробив сьогодні?» — сова відповідає на питання, але НЕ пропонує нових дій («давай зробимо X»)

**🔇 Typed Cooldowns — похвала і тривога окремо (xHQfi 30.04 — коміт ce9ab58)**
- [ ] Виконати звичку зранку → сова на табло Звичок похвалила за стрик
- [ ] Зробити велику витрату що пробиває бюджет (через AI «витратив 1000 на ресторан» якщо бюджет на цій категорії) — сова має попередити про бюджет ПОПРИ ранкову похвалу. Раніше глушило на 1 годину
- [ ] У `localStorage.nm_owl_cooldowns` (DevTools) — структура `{praise:{...}, concern:{...}, overview:{...}, info:{...}}` замість плоскої

**📊 Lazy Profile Builder (Фаза 6 OWL V3 xHQfi 30.04)**
- [ ] Через 24 год активного використання — у `localStorage.nm_user_patterns` (DevTools) масив 5-7 тенденцій українською (наприклад «Найактивніший період — ранок 6-12, закриває 18 з 24 задач»)
- [ ] У `localStorage.nm_user_patterns_ts` — timestamp оновлення
- [ ] AI у чатах спирається на ці патерни — наприклад знає що ти більше витрачаєш на каву і згадує це коли пишеш «200 кава» (один раз без нав'язування)
- [ ] Перші ~24 год від встановлення — патернів немає (норма)
- [ ] Якщо застосунок не відкривався 5+ днів — після відкриття за хвилину фоном генерує новий профіль

**🧠 Brain Pulse розумніший за станом (Фаза 2 OWL V3 xHQfi 30.04 — коміт чекає)**
- [ ] Записати у моменти «дуже втомився, сили нема» вранці → впродовж дня Brain Pulse не повинен показувати на табло stuck-task / streak-risk / budget-warn. Тільки критичні події <3 год лишаються
- [ ] Поставити вечірній настрій 😩 і дочекатись 22:00+ → Brain Pulse повністю мовчить (тільки критичні події з таймером <2 год)
- [ ] Не виконати звички 3+ дні поспіль → streak-risk сигнали від Brain Pulse не зʼявляються
- [ ] Закрити 3+ задачі за день → впродовж 1 год після останньої — Brain Pulse не пропонує нову задачу
- [ ] Звичайний день без маркерів → Brain Pulse працює як раніше

**🦉 Характер сови по стану (Фаза 5 OWL V3 xHQfi 30.04)**
- [ ] Записати у моментах ввечері «дуже втомився сьогодні» → у наступній відповіді сови (чат Я / Інбокс) тон м'який, без оклику, без «давай зробимо», запитує «як ти?» замість пропонувати дії
- [ ] Перевірити стрик звички 7+ днів поспіль → сова згадує коротко («7 днів. Працює.») без надмірного захвату «вау, нереально»
- [ ] Закрити 3+ задачі за день рукою → у відповіді коротка щира реакція без розтягування і без негайної пропозиції нової задачі
- [ ] Не виконати ВСІ заплановані звички 3 дні поспіль → сова рубаними реченнями пропонує мікро-дію (5 присідань / 2 хв читання), без «треба», без «ти молодець»
- [ ] Звичайний стан (нема втоми, нема стрика, нема пропусків) → стандартний тон persona-режиму без додаткового стилю

**🤐 Silent Reply на короткі дії (xHQfi 30.04 — коміт 7b9ecb0)**
- [ ] У чаті Inbox: «200 кава» → у відповіді ТІЛЬКИ коротке «✓ 200₴ Кафе» від обробника, БЕЗ «Як справи з фінансами цього тижня?»
- [ ] «додай хліб у задачі» → ТІЛЬКИ «✓ Задача» без «Класно що плануєш!»
- [ ] «виконав біг» → ТІЛЬКИ «✓ Біг» без «Молодець!»
- [ ] АЛЕ: «що я витратив сьогодні?» → нормальна текстова відповідь (це питання, не дія) — НЕ має бути ✓
- [ ] «додай задачу подзвонити сестрі завтра увечері о 18 щоб обговорити подарунок» (8+ слів, складний контекст) → нормальна відповідь з коментом

**🛡 Хук pre-push для документації (xHQfi 30.04 — коміт 5b7743e)**
- (Це для Claude, не для тебе) — після цього коміту я повинен пушити документацію без bypass-фрази «pre-push: ok». Перевірка пройшла на 06d111d.

**🚫 Блокувальний Stop-хук (xHQfi 30.04 — коміт 050ce4a)**
- (Це для Claude) — мої повідомлення тобі з технічним сміттям без пояснень тепер блокуються до перепису. Anti-loop 3 блоки за 30 хв. Тобі помітно якщо отримуєш чисті повідомлення без `setOwlCd`/`getNewBoard` тощо.

**⚠️ Якщо знайдеш баг:**
- Скрін + опис у чат
- Сесія xHQfi усе ще активна — Claude одразу зафіксує

### v472 (deploy 29.04 16:48 — 7PQ1a auto-merge)

**Контекст:** не тестовано 6 сесій підряд (kGX6g → UG1Fr → m4Q1o → oknnM → SK6E2 → 7PQ1a). Накопичились зміни Календаря, тривалості подій, Я→95%, фікс AI контексту, Usage Meter, інверсія детектор-хука.

- [ ] **Календар тривалість:** створити подію через AI «зустріч з 14 до 16» — зʼявляється з кінцем 16:00, не звичайна точка
- [ ] **Календар без вигаданого часу:** AI «щотижня пн ср пт» (без явної години) — НЕ створює події з вигаданим часом
- [ ] **Календар попередження конфлікту:** створити подію на час де вже є інша — AI попереджає «⚠️ На цей час вже є X»
- [ ] **«Я» теплова карта 14 днів:** видно квадратики з активністю по днях
- [ ] **«Я» місячна стат звичок:** тренд + цифри
- [ ] **«Я» тиждень vs минулий:** показує реальні дані (не «не виконано» коли виконано)
- [ ] **«Я» AI блок «OWL знає тебе»:** генерується раз на тиждень, не пустий
- [ ] **Usage Meter:** Налаштування → блок «📊 Споживання OpenAI» — є цифри, експорт працює
- [ ] **Тап ✓ на задачі рукою** (не через AI чат) — закриває нормально
- [ ] **Свайп вліво на задачі** — видалення у кошик з undo

### Cleanup (накопилось)

- [ ] **36 «привидів» з `recurringId`** у localStorage — видалити вручну (з kGX6g відкочення Календар Phase 2). Tool/UI ще не зроблений.

---

## ✅ Архів (протестовано)

> Те що вже перевірили з результатом і датою. Повторювані баги переносимо у секцію нижче.

_Поки порожньо — почнемо заповнювати з першої iPhone-сесії._

**Формат запису:**
```
### v[версія] — [дата]
- ✅ [що тестив] — [результат] — [нотатка]
- ❌ [що тестив] — [що не вийшло] → перенесено у баг B-XX
```

---

## 🔁 Повторювані проблеми (баги що повертаються)

> Якщо одна і та сама проблема знаходиться 2+ рази у різних версіях — записуємо сюди. Це сигнал що фікс був симптомом, а не коренем.

_Поки порожньо._

**Формат запису:**
```
### Проблема: [короткий опис]
- v[версія1] [дата1]: знайшов → виправив у комміті XXX
- v[версія2] [дата2]: повернувся → виправив у комміті YYY
- v[версія3] [дата3]: знову → треба архітектурний fix
```

---

## 📋 Як користуватись

**Роман:**
1. Сідаєш тестувати → дивишся секцію «⏳ Чекає тесту»
2. Після кожного тесту: викреслюєш чекбокс або переносиш у «✅ Архів» з результатом
3. Якщо знайшов баг — у `NEVERMIND_BUGS.md` як B-XX, тут залишаєш посилання

**Claude:**
1. Після кожної міграції / нової фічі / зміни UI — додаю конкретні тести у «⏳ Чекає тесту»
2. На старті сесії читаю цей файл — бачу скільки відкладено
3. Якщо одна проблема повернулась 2+ разів → переношу у «🔁 Повторювані» і ставлю питання архітектурного фіксу

---

## 🎯 Чому цей файл існує

**Проблема:** «iPhone-перевірка» 6 сесій поспіль відкладалась бо це було абстрактне «треба протестити». Без конкретного списку — нема відчуття обʼєму, нема відстеження прогресу, нема видимості повторюваних багів.

**Рішення:** тут конкретний короткий список ЩО саме треба клікнути / свайпнути / перевірити. Видно скільки лишилось. Видно що повторюється.
