# TESTS TODO — після MPVly 06.05 (4 баги + i18n 110 рядків)

> Що перевірити вручну на iPhone (PWA) і десктопі після `node build.js` і деплою.

---

## 🔴 Bug #1 — Барабан дати/часу не повинен стискати модалку (iOS)

**Що змінилось:** видалено `mask-image` з `.drum-col` у `style.css` (той самий клас бага що Settings UvEHE 03.05 — composite glitch у backdrop-filter контексті).

### Тест 1.1 — Редагування події у календарі
1. Календар → тап на існуючу подію
2. Відкривається модалка «Редагувати подію»
3. Крути барабан **Дата** (день/місяць/рік) — швидко вгору-вниз
4. Крути барабан **Час «З»** (години/хвилини)
5. Крути барабан **Час «До»** (години/хвилини)

**Очікувано:** модалка стоїть нерухомо, лише цифри прокручуються всередині барабана. Картка не «зменшується».
**Регрес:** якщо картка стискається при scroll — баг повернувся.

### Тест 1.2 — Health date/time picker
1. Здоров'я → «+» → новій картці виставити дату початку
2. Drum picker зверху health card modal
3. Крути обидва барабана (день/місяць/рік + години/хвилини)

**Очікувано:** picker стоїть нерухомо. Health card під ним не дрижить.

### Тест 1.3 — Косметика fade на барабані
**Раніше:** верх/низ барабана мали fade-out до прозорого (mask-image).
**Тепер:** fade прибрано — цифри обрізаються чітко по краю барабана. Це **очікувано** і прийнятно (border + центральна синя/жовта смуга все одно виділяють активну позицію).

---

## 🟡 Bug #2 — set_reminder повідомлення

**Що змінилось:** `habits.js:1452` тепер обгорнуто у `t('habits.reminder.set.ok', '⏰ Нагадаю о {time}: "{text}"', ...)`.

### Тест 2.1
1. Будь-який чат-бар (Inbox, Tasks, Me) → «нагадай о 18:00 купити хліб»
2. AI має створити reminder (видно у nm_reminders + у Inbox-стрічці картка ⏰)

**Очікувано у відповіді чату:** `⏰ Нагадаю о 18:00: "купити хліб"`
**Регрес:** якщо `{time}` чи `{text}` показується дослівно (без підстановки) — params поламані.

---

## 🟡 Bug #3 — reminder cross-tab sync

**Що змінилось:** `boot.js` — додано `'reminder': 'nm_reminders'` у `DETAIL_TO_KEY`.

### Тест 3.1 (десктоп Chrome — 2 таби)
1. Відкрий NeverMind у двох табах одного браузера
2. Таб A: «нагадай о 17:00 завдання» → reminder створений
3. Таб B: переключися (БЕЗ reload) → відкрий Inbox

**Очікувано:** картка ⏰ з reminder з'являється у Tab B автоматично (через nm-data-changed → DETAIL_TO_KEY → handleSyncKey).
**Раніше:** картка з'являлась тільки після hard reload Tab B.

### Тест 3.2 (один девайс — sanity)
1. Будь-яка вкладка → reminder створений → bezperedluk видно у Inbox
2. Тапнути по картці reminder → видалити (свайп)

**Очікувано:** картка зникла, з nm_reminders теж видалена. (Це і раніше працювало, перевіряємо що нічого не зламано.)

---

## 🟡 Bug #4 — sendClarifyText без AI race

**Що змінилось:** `inbox.js:1048` — додано `if (aiLoading) return; aiLoading = true; ... finally { aiLoading = false; }` у `sendClarifyText`.

### Тест 4.1 — складно відтворити, sanity-перевірка
1. Inbox → щось амбівалентне («зустріч завтра») → AI відкриває clarify
2. У clarify-вікні введи уточнення → Send
3. Поки чекаєш відповідь, **спробуй швидко двічі тапнути Send у головному Inbox**

**Очікувано:** друга відправка блокується (поки aiLoading). Жодних дублів типу `addInboxChatMsg('agent', ...)` через паралельний AI-call.
**Раніше:** теоретично могло проявлятися як дублі повідомлень або interleaved відповіді.

---

## 🌐 i18n — 110 нових `t()` обгорток

**Файли:** `habits.js`, `health.js`, `nav.js`, `finance-analytics.js`.

### Тест i.1 — habits AI replies (українською fallback)
1. Inbox чат → «створи задачу купити хліб» → AI має написати `✅ Задачу "купити хліб" створено`
2. «створи звичку медитація» → `🌱 Звичку "медитація" створено`
3. «видали задачу хліб» → `🗑️ Задачу "хліб" видалено`
4. «нагадай о 19:00 ...» → `⏰ Нагадаю о 19:00: "..."`
5. **Кожне повідомлення** має правильно показувати title/name/time БЕЗ дослівного `{title}`.

### Тест i.2 — Health export
1. Здоров'я → меню → Експорт картки → текст внизу

**Очікувано:** «МЕДИЧНА КАРТКА», «Дата експорту: ...», «🚨 АЛЕРГІЇ: ...», «АКТИВНІ СТАНИ (N):», «ВСІ ПРЕПАРАТИ (N):», «ВІЗИТИ ДО ЛІКАРЯ (за рік, N):», «Згенеровано у застосунку...» — все українською без `{n}`/`{date}`.

### Тест i.3 — Tab order list
1. Налаштування → Активні вкладки → Порядок вкладок
2. Перетягни — кнопки/підписи: «Продуктив.», «Нотатки», «Я», «Вечір», «Фінанси», «Здоров'я», «Проекти»

### Тест i.4 — Memory cards
1. Налаштування → Пам'ять
2. Якщо є факти з різних source: написи `фон`, `вручну`, `стара пам'ять`, `онбординг` мають з'явитися біля дати.

### Тест i.5 — Finance analytics
1. Фінанси → 📊 Аналітика
2. Метрики (3 блоки) перебираю стрілками

**Очікувано:** «Максимум за день — найдорожчий з N активних днів» / «Найбільша операція — з N операцій» / «Прогноз місяця — за поточним темпом до DD.MM» / «Доходи місяця — N надходжень» — все по-укр., {n} підставляється.

3. Розподіл доходу → редагувати (олівець) → кнопка «Готово» / «Скинути до 50/30/20» / «(ціль 50%)» / «⚠️ Сума XX% — рекомендовано 100%».

---

## ✅ Smoke (5 хв, після кожного деплою — універсально)

1. Inbox → коротке повідомлення → AI обробив
2. Перемикання вкладок (нижні таби) — без помилок у консолі
3. Створити задачу/звичку/нотатку/подію → відображається
4. Закрити PWA → відкрити знову — стан зберігся
5. iOS Safari ConsoleApp / desktop DevTools — НЕ повинно бути ReferenceError / TypeError

---

## ⚠️ Силент-баги що **НЕ** в скоупі цієї сесії (на наступну)

- **nav.js openTabSelector** (line 202): `ALL_TABS_CONFIG.map(function(t)` — параметр `t` shadowing import `t` з `utils.js`. Виклик `t('nav.tabsel.always', 'завжди')` всередині цього map (line 212) спробує викликати об'єкт як функцію → TypeError при відкритті селектора активних вкладок. Не помічено бо: модалка рідко відкривається. **Fix:** перейменувати параметр на `cfg`, замінити `t.id`/`t.label`/`t.svg`/`t.bg`/`t.accent` на `cfg.*`. ~15 хв з тестом.
- **calendar.js setupModalSwipeClose двічі на routine-panel** (silent-bug-scout #5): зараз працює завдяки `_swipeClose` guard, але fragile при будь-якому refactor `routine-panel`. **Fix:** винести в одну init-функцію. ~10 хв.
