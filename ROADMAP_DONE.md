# NeverMind — Виконано (історія)

> Журнал виконаних робіт. Консолідовано з `ROADMAP.md` 20.04.2026 (сесія g05tu).
>
> **Що тут:** завершені Active-блоки, повністю закритий Блок 1 малих фіксів, 6 фаз переробки Здоров'я, хронологія `✅ Done`, аудит вкладок 15.04.2026.
>
> **Де деталі сесій:** `docs/CHANGES.md` — хронологічний журнал з повним описом кожного коміту.
>
> **Архів більш ранніх робіт:** `_archive/FEATURES_ROADMAP.md` — "Що мозок вже вміє" з посиланнями на файли і рядки коду.

---

## 🏆 Завершені Active-блоки

### ✅ Council механізм — 5 паралельних агентів через `Agent` tool (сесія bOqdI 02.05.2026)

24-рядкова секція `## 🧠 Council` у `CLAUDE.md` (коміт `8cc73af`) — інфраструктура для великих задач. Один меседж з 5 паралельних `Agent` викликів `general-purpose` з ролями: 🕵️ Критик / 🌀 Стратег / 🚀 Оптиміст / 💎 Свіжий погляд / 🛠 Виконавець. Кожен сам читає код через Read/Grep — реальні якорі, не галюцинації.

**Тригер автоматичний:** слова Романа («дивись ширше / глибше / перевір уважно / порадься / використай агентів») АБО об'єктивні маркери (зміна `src/core/*`, payload `nm-data-changed`, функція з >3 викликами, рефакторинг файлу >500 рядків, >3 файли одразу).

**Чого НЕМАЄ свідомо** — логу, метрик 👍/👎, граматичних рамок, тріад незворотності. Чесний урок 4 ітерацій з Gemini де проста ідея «5 агентів-радників» розрослась у бюрократичну машину з Тріадою Незворотності, граматичною рамкою і метриками смерті. Роман зупинив на 5-й ітерації: «Ми ускладнили лишнє». Концепт `_archive/COUNCIL_CONCEPT.md` (171 рядок) лишився у архіві як ілюстрація принципу «менше = більше».

**Перший реальний запуск (bOqdI 02.05):** знайшов 3 архітектурні борги — `evening.js / _rescheduleTask` (`19e112f`), `health.js / _syncMedicationToTask` (`25e60da`), `proactive.js` (`8c3fe8d`) — гігієна перед Pre-Migration Підсесією 3.

**Другий запуск (bOqdI):** глобальний cleanup-аудит документації — знайшов 33+ протиріч → план з 9 фаз у `_ai-tools/CLEANUP_PLAN_bOqdI.md`, виконаний у наступній сесії BqTWF 02.05 (коміти `b953825`→).

---

### ✅ OWL Silence Engine + Pruning Engine (сесії UVKL1 26.04 → C8uQD 27.04 → xHQfi 30.04.2026)

3 фази архітектурного плану `docs/OWL_SILENCE_PRUNING_PLAN.md`. Закриває «stale board» (сова повторює про вже закриті задачі) і дає юзеру структурний важіль «дай спокій».

- **Фаза 1 — Silence Engine.** Tool `request_quiet(hours)` пише у `nm_owl_silence_until`, чек у `shouldOwlSpeak()` блокує всі 4 канали сови (Inbox-табло, Tab-табло, Brain Pulse, Followups) одним рядком. **xHQfi 30.04 розширив:** tool `cancel_quiet` для дострокового скасування (коміт `44bf7fe`) + UI-плашка «🤫 Сова мовчить до HH:MM» з тапом для скасування на 8 вкладках + Inbox (коміт `da057ae`).
- **Фаза 2 — Pruning Engine** (C8uQD 27.04). Сова заповнює `entityRefs:[task_123,event_456]` у JSON-схемі генерації. `isMessageRelevant()` фільтр у `src/owl/board-utils.js` викидає повідомлення про неактивні сутності з UI і `boardHistory`. Wipe старої історії при rollout (`nm_pruning_wipe_v1_done`).
- **Фаза 3 — Розділення Контексту** (C8uQD 27.04, коміти `baf91bc` + `68a2674`). Блок `[ВАЖЛИВО — РЕЖИМ ТИШІ]` у `getAIContext()` `src/ai/core.js:56` — у чаті сова відповідає на питання, але не пропонує нового. Блок `[ФАКТ] Нещодавно ЗАКРИТІ` видалено з `getTabBoardContext()` `src/owl/proactive.js:44-47`.

**Закриті баги структурно:** B-100 (тригер-слова «не доставай»), B-102 (USER_STATE сигнал у Brain Pulse — заміняється централізованим silence через `shouldOwlSpeak`).

**Ключові файли:** `src/ai/ui-tools.js` (request_quiet + cancel_quiet), `src/ai/prompts.js` (UI_TOOLS_RULES тригер-фрази), `src/ai/core.js` (силenсе flag у getAIContext), `src/owl/inbox-board.js` (silence-чек у shouldOwlSpeak), `src/owl/proactive.js` (entityRefs генерація + збереження), `src/owl/board.js` (UI badge + перерендер на nm-data-changed), `src/owl/board-utils.js` (isEntityRelevant + isMessageRelevant), `src/core/boot.js` (wipe історії).

---

### ✅ Рефакторинг документації + «мозок» Claude (сесія g05tu 20.04.2026)

5 фаз, 6 комітів. Навести порядок у документації через переміщення блоків у спеціалізовані файли + створити "мозок" Claude (lessons, INDEX, хуки) щоб у новому чаті швидше орієнтуватись.

- **Фаза 0 `d8ecab1`** — `_ai-tools/REFACTOR_PLAN.md` + snapshot `e64eb58`
- **Фаза 1 `f579492`** — 5 нових файлів (`ROADMAP_DONE`, `docs/FILE_STRUCTURE`, `docs/GIT_EMERGENCY`, `docs/DO_NOT_TOUCH`, `docs/FINANCE_V2_PLAN`) + скорочено CLAUDE 521→416, ROADMAP 715→539, SESSION_STATE 293→103, START_HERE 114→56 + додано "Один мозок на все" у NEVERMIND_LOGIC
- **Фаза 2 `80f9846`** — `lessons.md` з 3 секціями (🔄 патерни / ❌ анти-патерни / 📋 журнал рішень)
- **Фаза 3 `90e0f7f`** — 4 автоматичні хуки у `.claude/hooks/`: `cache-name-reminder`, `md-index-reminder`, `ai-tools-sync`, `skill-triggers` (+"Роби" детекція)
- **Фаза 4 `004e299`** — `_ai-tools/INDEX.md` семантичний індекс "куди йти за чим"
- **Фінал `13b843d`** — hash'і фаз у REFACTOR_PLAN + оновлення SESSION_STATE

**Результат:** стартове читання 2164 → 1593 рядки (−26%). З JIT-читанням через INDEX ще −60-80% на глибоких відповідях. Автоматичні нагадування ловлять CACHE_NAME bump, нові .md у INDEX, оновлення AI_TOOLS, тригери скілів, слово "Роби".

### ✅ Рефакторинг `finance.js` (сесія gHCOh 17.04.2026)

Розбиття `finance.js` на 6 модулів з backward-compat через re-exports. Файли після рефакторингу:
- `src/tabs/finance.js` (~700 рядків) — ядро: renderFinance, state, getFinanceContext, processFinanceAction, getFinEditMode/setFinEditMode
- `src/tabs/finance-cats.js` — категорії (CRUD, 41 SVG-іконка, палітра 14 кольорів, міграція v2)
- `src/tabs/finance-modals.js` — модалки (транзакція з калькулятором, datepicker, бюджет, категорія)
- `src/tabs/finance-analytics.js` — Аналітика 📊 (3 режими графіка, 9 метрик, 50/30/20)
- `src/tabs/finance-insight.js` — Інсайт дня (AI з temperature 0.3, кеш 1 год)
- `src/tabs/finance-chat.js` — Chat bar Фінансів

### ✅ Фінанси v2 стабільні (сесії cnTkD + KTQZA 17.04.2026)

30+ багів закрито за 2 дні (B-42..B-79), всі 🔴 і 🟡 закриті. Поточний стан **~98%** (лишився 1 🟢 B-65 SW load, одноразово).

Детальний план імплементації (що зроблено по фазах) → `docs/FINANCE_V2_PLAN.md`.

### ✅ Блок 1 "Малі фікси" закритий

Перевірено у KTQZA: `set_reminder`, G9 Page Visibility, G11/G12/4.5/4.40, retry/fallback — **все в коді**. Детально нижче у секції "Блок 1".

### ✅ Винесення промптів у `src/ai/prompts.js` (сесія 14zLe 17.04.2026)

`prompts.js` 251 рядок (getOWLPersonality + INBOX_SYSTEM_PROMPT + INBOX_TOOLS 31 tool + getOwlChatSystemPrompt), `core.js` зменшився 749→623 рядки.

### ✅ Вечір 2.0 MVP ПОВНІСТЮ ВИКОНАНО (19.04.2026 — сесії QV1n2 Ф0 + dIooU Ф1-8, 14 комітів)

Усі 8 фаз з `docs/EVENING_2.0_PLAN.md` закриті:
- Блокування матовим склом до 18:00 з автоанімацією
- `getEveningContext` у загальний AI-контекст
- Тригер `evening-prompt` з червоною крапкою
- Chips у діалозі
- Переробка вмісту (настрій/моменти/недороблені задачі/quit-звички)
- Три CTA (📅/📔/🌙)
- Tool calling через новий `dispatchEveningTool` (22 tools БЕЗ Inbox side-effects)
- Verify Loop + Memory Echo + G13 Brain Dump + 4.12 антидублювання
- Фінальний інсайт-підсумок + значок "✓ День закрито"

Пост-MVP фічі — `docs/EVENING_2.0_PLAN.md` секція "Після MVP".

### ✅ Один мозок #1 мінімум (сесія EY55M)

UI tools (`switch_tab`, `open_memory`, `open_settings` тощо) підключені у 4 чати на `INBOX_TOOLS` (Вечір, Задачі, Нотатки, частково інші). Виявлено на iPhone-тесті v308+ що це недостатньо — потрібен повний `INBOX_TOOLS`, а не тільки UI. Подальша робота — "Один мозок V2" у ROADMAP Active.

### ✅ Переробка Здоров'я (100%, сесії jMR6m + 6v2eR + 6GoDe 15-19.04.2026)

Повна переробка вкладки з ~45% до ~100%. 6 фаз — Фундамент, Синк календаря, Inbox + Tool Calling, UI переробка, Супровід OWL, Експорт. Деталі нижче у секції "Здоров'я 100%".

---

## 📋 Блок 1 — Малі фікси (повністю закритий 13.04.2026 сесія PBybp)

- **`set_reminder` фікси** — створити картку в стрічці Inbox з категорією "Нагадування" ⏰, автоствор запису у `nm_events` з часом, додати категорію "Нагадування" у мапи `CAT_DOT_BG` і `CAT_META` (`src/tabs/inbox.js`). **Пов'язане з планом "Динамічний розпорядок дня" (Блок 3) — це його передумова.** Зараз нагадування створюються, видно у стрічці і синхронізуються з календарем. Контекст у архіві → `_archive/FEATURES_ROADMAP.md` секція "Динамічний розпорядок дня".

- **4.5 Блокування табло поки чат відкритий** (автор: GPT-4o) — не генерувати нове повідомлення табло поки `activeChatBar` відкритий + 10 сек після закриття. Guard на початку `tryBoardUpdate`. Прибирає дратівне "ти пишеш у чаті — і раптом OWL перезаписує табло над чатом".

- **G9 Page Visibility fix** (автор: Gemini) — блокувати всі тригери (крім reminder-due) коли `document.hidden === true` (вкладка неактивна). Спалювання API зупинено.

- **G11 "Дія замість питань"** (автор: Gemini, правило промптів) — агент ЗАВЖДИ закінчує повідомлення чіпами з конкретними діями, не відкритими питаннями. Не "Як пройшла консультація?" а "Консультація пройшла — створити задачу з результатами?" [Створити] [Не треба]. Оновлено промпти табло + всіх чат-барів.

- **G12 Мікро-розмови** (автор: Gemini, ліміт діалогу) — жорсткий ліміт у промпті: максимум 2-3 повідомлення на одну тему. Після цього — мовчати або змінити тему. Передавати останні 3-4 репліки для контексту, не більше.

- **4.40 Auto-silence** (автор: Grok розширені) — `if (user_ignored_last_3_messages) { silence_for_4_hours + reduce_relevance_threshold_by_30% }`. Найпростіший анти-спам. Доповнює Judge Layer.

- **Retry/fallback для tool calls** (запозичено з `agent-core`) — зараз 5 місць у `src/ai/core.js` додано повторні спроби з exponential backoff (1с/2с/4с), у разі провалу — toast "Не вдалось зберегти, спробуй ще раз" + збереження у локальну чергу. Прибирає "тихі втрати" юзер-інпуту.

- **Винесення промптів у `src/ai/prompts.js`** — виконано 17.04.2026 (сесія 14zLe). Деталі вище у "Завершені Active-блоки".

---

## 🏥 Здоров'я 100% — 6 фаз переробки (15-19.04.2026)

### Фаза 1 — Фундамент структур даних і getAIContext (15.04.2026 jMR6m, коміт `fab3865`)

- `nm_allergies` з CRUD і UI-картка (коралова, фіксована зверху)
- Розширена структура `nm_health_cards` (`doctor`, `doctorRecommendations`, `doctorConclusion`, `startDate`, `nextAppointment`, `history[]`)
- Розширені `medications` (`dosage`, `schedule[]`, `courseDuration`, `log[]`, `createTasks`)
- Lazy-міграція через прапор `nm_health_migrated_v2` (конвертує старі `doctorNotes`→`history` + старі медикаменти у нову структуру)
- `getHealthContext()` переписано і підключено у `getAIContext()` — OWL бачить здоров'я/алергії у всіх чатах
- `renderHealthWorkspace` оновлено (блок "Лікування" з доктором/рекомендаціями/прийомом, Записи лікаря тепер з `history.filter(type=doctor_visit)`)
- `sendHealthBarMessage` прибрано локальний дубль-виклик `getHealthContext`
- Legacy `nm_health_log` не чіпали (живе до Фази 3 UI переробки)

### Фаза 1.5 — Синк картка ↔ календар (15.04.2026 6v2eR)

**Двосторонній синк реалізований:**
- Картка → подія: `_syncCardAppointmentToEvent` у `health.js` — при `saveHealthCardFromModal` створює/оновлює подію `nm_events` (`title: "Прийом: {назва}"`, `priority: 'important'`, `sourceCardId`, `archived: false`). Зберігає `eventId` у `card.nextAppointment.eventId`
- Картка видалена → подія у кошик через `addToTrash('event', ...)` (відновлюється 7 днів)
- **Зворотний синк (Календар → картка) — lazy у `renderHealth()`** (без circular dependency — не імпортуємо health.js у calendar.js):
  - `_syncEventDatesToCards()` — якщо подія була змінена у Календарі, оновлює `nextAppointment.date/time` при наступному відкритті Здоров'я
  - `_detectOrphanAppointments()` — якщо `eventId` вказує на видалену подію, прибирає `eventId`
- **Архівація минулих прийомів** — `_archivePastAppointments()` lazy у `renderHealth`:
  - Подія у `nm_events` помічається `archived: true` (не видаляється — залишається у Календарі як історія)
  - У картці — автозапис у `history` типу `doctor_visit` ("Прийом відбувся {date} о {time}")
  - `nextAppointment` очищається щоб звільнити слот
- **Edge case:** при реактивації архівованої події — `archived: false` повертається автоматично

### Фаза 2 — Створення через Inbox + Tool Calling + антидублювання (15.04.2026 6v2eR)

**9 нових tools у `INBOX_TOOLS`:** `create_health_card`, `edit_health_card`, `delete_health_card`, `add_medication`, `edit_medication`, `log_medication_dose`, `add_allergy`, `delete_allergy`, `add_health_history_entry`.

**Prompt-правила у `INBOX_SYSTEM_PROMPT` (секція ЗДОРОВ'Я):**
- Алергія → `add_allergy` (з 4.12 перевіркою існуючих)
- Симптом 3+ дні / діагноз → перевір "Активні стани" → якщо є — `add_health_history_entry`, якщо ні — `create_health_card`
- Разова скарга → `save_moment`/`save_note` (НЕ картка)
- Прийом ліків (з активних карток) → `log_medication_dose`
- Лікар прописав препарат → `add_medication` (існуючий стан) або `create_health_card` (новий)
- Візит до лікаря → `create_event` (НЕ окрема картка)
- Медичні питання ('що з моїм...') → текстова відповідь "я не лікар"

**4.12 Антидублювання реалізовано** — `getHealthContext()` віддає `[ID:X]` для карток, `[medID:X]` для медикаментів, `[ID:X]` для алергій. AI у промпті ОБОВ'ЯЗКОВО перевіряє контекст перед `create_health_card`/`add_allergy`.

### Фаза 3 — UI переробка вкладки (15.04.2026 6v2eR)

- **Картка стану з усіма полями:** Timeline історії з 5 типами іконок (manual/dose_log/status_change/doctor_visit/auto)
- Ліки з логом прийому — крапки прийомів за день, лічильник `X/Y`, кнопка "+ Прийняти"
- Кнопка "Запитати OWL про цей стан" — велика дія-картка вгорі workspace
- Чат-бар Здоров'я з preloaded контекстом — блок "🎯 ФОКУС РОЗМОВИ" на ВЕРХ контексту
- Прибрано блок "Відмітити сьогодні" (шкали 1-10) (B-31)
- Прибрано блок "Самопочуття тижня"
- **Індикатор прогресу курсу** — "Курс {pct}% · {тренд}" у workspace

### Фаза 4 — Супровід OWL (15.04.2026 6v2eR)

- **Пасивні нагадування ліків** — `_getMissedDoses()` у `health.js`: перебирає активні картки + `medications[]`. Вікно реакції: `now >= T+15хв` і `now <= T+6год`. Жовтий банер зверху вкладки з кнопками "✓ Прийняв" і "Пропущу". `setInterval` 5 хв перерендерить список + `document.hidden` guard щоб не палити ресурси у фоні.
- **Моніторинг суперечностей** — prompt-правила у `INBOX_SYSTEM_PROMPT` (секція "ЗДОРОВ'Я — МОНІТОРИНГ СУПЕРЕЧНОСТЕЙ"): коли юзер записує дію що суперечить `doctorRecommendations`. AI ПІСЛЯ primary tool call додає у text content м'яку згадку БЕЗ моралізаторства.
- **Класифікація тексту → автотренд** — `add_health_history_entry` з `entry_type='status_change'` і text що ЯВНО містить 'покращення'/'погіршення'/'стабільно'.
- **Жорсткий блок "OWL не лікар"** — у `sendHealthBarMessage` системний промпт розширено блоком з ❌/✅.

### Фаза 5 — Експорт і інтеграції (15.04.2026 6v2eR)

- **"Медична картка" fullscreen** — нова модалка `#health-export-modal` (повноекранна, зелений header, monospace pre). Функція `buildHealthExportText()` генерує plain-text: АЛЕРГІЇ → АКТИВНІ СТАНИ → ВСІ ПРЕПАРАТИ → ВІЗИТИ ДО ЛІКАРЯ → ЗАВЕРШЕНІ СТАНИ. Кнопка "📋 Скопіювати".
- **Синк ліків з `createTasks:true` → `nm_tasks`** — `_syncMedicationToTask(cardName, med)`.
- **Синк витрат на ліки/лікарів → `history` картки** — `syncHealthFinanceToHistory(amount, category, comment)`.
- **Інтеграція з Вечір** — промпт-правило: якщо є пропущені дози — м'яко згадати БЕЗ моралізаторства.
- **Інтеграція з Я** — промпт-правило: якщо є активні стани — коротка строка про дисципліну курсів.

### Фаза 6 — Інтерв'ю при створенні картки (19.04.2026 6GoDe)

Через промпт-правило у `INBOX_SYSTEM_PROMPT`: коли юзер описує симптом 3+ днів або діагноз, сова ставить 1-3 короткі питання (коли почалось, був у лікаря, які ліки) з чіпами-відповідями, а потім створює картку з повним вмістом. Не дотискає — якщо юзер проігнорував, створює з тим що є. Тільки при CREATE, не при edit/history.

### Legacy шкал cleanup (19.04.2026 6GoDe)

UI шкал 1-10 видалено у B-31 (15.04), але код що читав `nm_health_log` залишився у 4 місцях. Прибрано:
- Фаза 1 (`health.js`): `getHealthLog`/`saveHealthLog` експорти, legacy блок у `getHealthContext`, action `log_health` у chat-bar
- Фаза 2 (`proactive.js`): 2 мертві кореляції (сон↔звички, енергія↔задачі), health board контекст з порожніми плейсхолдерами
- Фаза 3 (`boot.js`): cross-tab listener + runMigrations v6 одноразово видаляє `nm_health_log` з localStorage. Net: -92 рядки мертвого коду.

---

## 📅 Хронологія ✅ Done (по датах)

### 19.04.2026 (сесія QV1n2 — Вечір 2.0 планування + Фаза 0 рефакторингу)

- **Концепція Вечора переписана на v2** — ритуал закриття дня з OWL замість dashboard'у з кільцем продуктивності: блокування до 18:00 матовим склом, OWL пише першою у чат Вечора з контекстом дня, дві CTA кнопки, чіпи у діалозі, автосинхронізація через tool calling, підсумок з інсайтом.
- **3-рівнева документація створена:** `docs/EVENING_2.0_PLAN.md` (544 рядки), `ROADMAP.md` Active-блок, `CONCEPTS_ACTIVE.md` секція v2.
- **Фаза 0 рефакторингу `src/tabs/evening.js`** (1054 рядки → 413): винесено у **5 модулів** — `me.js` (480, повна вкладка Я), `evening.js` (413, core Вечора), `evening-chat.js` (204, чат-бар + фуллскрін діалог), `evening-actions.js` (30, заготовка Фази 7 автосинх), `src/ui/unread-badge.js` (67, універсальна червона крапка непрочитаних). Імпорти оновлено у 6 файлах.

### 19.04.2026 (сесія rSTLV — відкат маскот-сови, повернення до емодзі 🦉)

- **Повне видалення OWL маскот-системи** (всі експерименти 18-19.04 — sprite-sheet, PNG станів, SVG-крило, priority state-machine, 5-frame flipbook махання, Nano Banana workflow). У Inbox повернуто простий `<div class="owl-speech-avatar">🦉</div>` як на всіх інших вкладках. Причина: анімація складна, час витрачений не виправдовує результат.
- **Видалено:** 11 PNG у `assets/owl/`, ~85 рядків CSS, ~45 рядків JS у `src/owl/board.js`, boot auto-trigger greeting у `src/core/boot.js`, 4 виклики `setOwlMascotState` у `src/ai/core.js`, папка `handoff/` з документами для інтеграції.
- **Повернемось до анімації коли:** буде готовий нормальний художній ассет (багатошарова SVG або Rive-файл) і ресурс на впровадження.

### 13.04.2026 (сесія PBybp — Блок 1 повністю закритий, 7 комітів)

- **`set_reminder` фікси** — тап на картку reminder у стрічці Inbox → відкриває Calendar modal; іконка ⏰ (reminder) / 📅 (event) / 📌 (task) у закріплених картках `_renderUpcoming`.
- **4.5 Блокування табло поки чат відкритий** — жорсткий `return { speak: false }` у `_judgeBoard` коли `activeChatBar` відкритий, +10 сек cooldown після закриття. Винятки: `hasCritical` і тригер `chat-closed` пробивають.
- **G9 Page Visibility fix** — `if (document.hidden) return;` у 4 точках. Виняток: `_checkReminders` продовжує працювати.
- **G11 "Дія замість питань"** — на початку `CHIP_PROMPT_RULES` правило G11: chips ≥ 1, ≤ 3; питання без чіпів = баг.
- **G12 Мікро-розмови** — у `getOWLPersonality() universal` секція "ПРАВИЛО G12": не повторювати тему >2-3 разів. У `callOwlChat` історія `slice(-12)` → `slice(-8)`.
- **4.40 Auto-silence** — 3 повідомлення поспіль проігноровано → 4 год тиші OWL. Ключі: `nm_owl_last_board_ts`, `nm_owl_last_chip_click_ts`, `nm_owl_ignored_msgs`, `nm_owl_silence_until`.
- **Аудит + cleanup** — 8-пунктний аудит пройдено, прибрано мертвий експорт `_markChatClosedNow`.

### 13.04.2026 (сесія gQ2Hl)

- `fix(memory)` — захист пам'яті від сміття (жорсткий промпт `save_memory_fact` + код-фільтр у `addFact` + прибрано фонове вгадування `_backgroundExtractFacts`)
- Фаза 1.1 — об'єднання табло Inbox і табло вкладок в один API (`getBoardContext`, `tryBoardUpdate`)
- Фаза 1.2 — чат бачить історію табло (3 останні повідомлення з часовими мітками)
- Фаза 3.11 — навігаційні чіпи на всі вкладки, підсилено `CHIP_PROMPT_RULES`
- Фаза 3.7 — правило емпатії в universal personality (усі чати + табло, 21 маркер замість 8)
- Фаза 3.13 — OWL запитує про комфортний темп + автоматичний розрахунок 20%
- Фаза 3.8 — прокрастинація detector (`rescheduleCount` при `edit_task`)
- Фаза 3.6 — Smart Boot-up (прострочені задачі + first-open-today тригер + SMART BOOT-UP правила в промпті)
- Фаза 3.10 — кореляції між вкладками (4 патерни, 3 джерела)

### 12.04.2026 (сесія YbLTH)

- 4.3 Semantic cooldowns — AI повертає `topic` у JSON табло, тема в cooldown 3 год
- Правило перспективи — OWL каже "ти/у тебе", ніколи "я/у мене"
- Fallback "Запам'ятав ✓" — коли AI повертає тільки `save_memory_fact`
- Історія чату без `tool_calls` — `inboxChatHistory` не зберігає `tool_calls`
- Правило "спочатку читай — потім говори" (глибина відповіді) — додано в `CLAUDE.md`

### 11.04.2026 (сесії FPKOV + B29hd)

- Крок 1 уніфікація — channel-aware Judge Layer (`shouldOwlSpeak(trigger, opts)`), `followups.js` делегує усі глобальні блокери у Judge Layer
- Крок 2 Структурована пам'ять фактів (4.2) — новий модуль `src/ai/memory.js`, `nm_facts` з часовими мітками, 6 категорій, TTL, новий tool `save_memory_fact`, переписаний `doRefreshMemory()`, UI у налаштуваннях "Що агент знає про мене", одноразова міграція legacy `nm_memory`. Архітектурно виправляє корінь бага "болить горло"
- Латка бага "горло" — "ПРАВИЛО ЧЕСНОСТІ" у `getOWLPersonality()`
- Правило "середній розмір відповідей" у `CLAUDE.md`
- `/start` скіл перероблено — читає правила взаємодії першим, потім стан проекту
- Бізнес-модель зафіксована — freemium + API через хмару Supabase (не BYOK)

### 10.04.2026

- 4.1 Tool Calling — перехід з текстового JSON на OpenAI function calling, 25 tools, prompt скорочений ~200→~30 рядків. 6 багів виправлено аудитом (1 critical, 4 medium, 1 low)
- Live Chat Replies Фаза 2.0 — `src/owl/followups.js`, 2 тригери (stuck-task, event-passed), `addMsgForTab` диспатчер

### 09.04.2026

- B-23, B-24, B-25 закриті (усі 🔴 критичні)
- Календар повна переробка — day schedule modal, all-day events зверху, drum picker, event-edit modal, zoom-анімації, SVG іконка з динамічною датою

### 06-08.04.2026

- Мозок OWL Фази 1-3 — єдина генерація, синхронізація табло↔чат, Welcome Back, крос-контекст, трекінг чіпів, `nm_insights`, емпатія, ранковий бриф
- Judge Layer — `shouldOwlSpeak()` з балами 0-15, поріг `SPEAK_THRESHOLD=3`
- B-16, B-17, B-18, B-19 закриті. Calendar modal + `nm_events`. Один мозок (всі чати = universal action). Динамічний денний скор. `dueDate`/`priority` у задачах

### 04-05.04.2026

- ES Modules рефакторинг — 22 модулі, 162 `window` handlers. Табло read-only, чіпи через чат-бар, `owlChipToChat`
- B-13, B-14, B-15 закриті

_Повна історична таблиця "Що мозок вже вміє" з посиланнями на файли і рядки коду → `_archive/FEATURES_ROADMAP.md`._

---

## 📊 Аудит вкладок 15.04.2026 (статус %, історичний знімок)

| Вкладка | Статус (15.04) | Прогалини |
|---|---|---|
| 💰 Фінанси | **85%** (ROADMAP казав 70%) | база знань OWL у промпті коуча, історичний графік по тижнях, візуал 50/30/20 |
| 🌙 Вечір | **80%** (станом на 15.04) | підсумок не згадує проекти; `autoEveningSummary` по кнопці а не фоново. **Оновлено 19.04.2026 (QV1n2 + dIooU):** переписано на Вечір 2.0, MVP закрито повністю (100% MVP) |
| 👤 Я | **65%** | патерни OWL тільки по кнопці, немає автогенерації раз на тиждень; немає візуальної шкали під оглядом тижня |
| 📁 Проекти | **65%** | поле `status` відсутнє у коді; Витрати проекту не синк з Фінансами; немає автодетекції стагнації |
| 🏥 Здоров'я | **100%** (Фази 1-5 закрито 15.04 у jMR6m + 6v2eR; Фаза 6 "Інтерв'ю" додана 19.04 у 6GoDe; legacy шкали прибрано у 6GoDe) | Лишилась одна ідея "розширення алергій з severity" — у `💡 Ideas` |
| 📆 Календар | **80%** | drum picker треба ручний тест; немає тривалості подій, рекурентності, нагадувань |

**Примітка:** цей знімок — історичний. Поточний статус вкладок → `ROADMAP.md` секція `🚀 Active` + `📋 Next`.

---

_Цей файл поповнюється при виклику `/finish` — завершені пункти з ROADMAP переносяться сюди. При переповненні (>600 рядків) — найстаріші секції у `_archive/ROADMAP_DONE_archive.md`._
