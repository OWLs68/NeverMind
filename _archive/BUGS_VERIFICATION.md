# Верифікація відкритих багів проти реального коду

> **Створено:** нічна сесія qG4fj, 25.04.2026 (~01:00). Автономна робота поки Роман спить.
>
> **Мета:** перевірити чи 4 відкриті баги (B-100, B-101, B-102, B-103) ще актуальні після останніх комітів R5Ejr/nudNp, чи може щось випадково виправилось.
>
> **Метод:** автоматичний агент прочитав реальний код кожного файла з `NEVERMIND_BUGS.md`, перевірив рядки, подивився `git log` для впливу нових комітів.

---

## 📊 Результат — таблиця

| Баг | Файл | Статус | Обсяг фіксу | Зміни проти `NEVERMIND_BUGS.md` |
|-----|------|--------|-------------|--------------------------------|
| **B-103** 🔴 | inbox.js + habits.js + evening-actions.js | **АКТУАЛЬНИЙ** | ~30 хв | **6 місць** (не 5 як у файлі) |
| **B-100** 🟡 | prompts.js | **АКТУАЛЬНИЙ + частково покращено** | ~15 хв | «дістало» вже додано, 5 інших ще ні |
| **B-101** 🟢 | 9 чат-барів | **АКТУАЛЬНИЙ** | ~45 хв | **9 місць** (не «1 + можливо інші») |
| **B-102** 🟡 | brain-signals.js + brain-pulse.js | **АКТУАЛЬНИЙ (архітектурний)** | ~2-3 год | 8 сигналів зараз (не 9 як у CLAUDE.md) |

**Жоден баг випадково не закрився.** Але по B-100 і B-103 знайдено уточнення яких не було у `NEVERMIND_BUGS.md`.

---

## 🔴 B-103 — Дублі подій у Календарі

### Перевірка реального коду

Знайдено **6 місць** створення подій через `events.unshift()` без жодного dedup:

1. `src/tabs/inbox.js:582` — основна `create_event` дія в Inbox
   ```javascript
   events.unshift(ev);
   saveEvents(events);
   ```

2. `src/tabs/habits.js:936` — fallback detection (коли AI створив `create_task` але це насправді подія)
   ```javascript
   events.unshift(ev);
   saveEvents(events);
   ```

3. `src/tabs/habits.js:1096` — основна `create_event` у чаті Задач

4. `src/tabs/habits.js:1275-1284` — нагадання як подія
   ```javascript
   events.unshift({
     id: reminderId + 1,
     title: text,
     date, time,
     priority: 'normal',
     createdAt: Date.now(),
     source: 'reminder',
     reminderId
   });
   ```

5. `src/tabs/evening-actions.js:138` — вечірній чат, `create_event`

6. `src/tabs/evening-actions.js:162` — вечірній чат, нагадання

### Висновок

**🚨 У `NEVERMIND_BUGS.md` написано «5 місць» — насправді 6.** Пропущені два місця у `evening-actions.js` і одне у `habits.js:1275` (нагадання).

**Жодного `addEventDedup()` helper немає.** Останні коміти (`f01d4ac`, `44ae873`, `3e3892a`) не торкались логіки створення подій.

### Реалістичний план фіксу (~30 хв)

1. Створити `addEventDedup(ev)` у `src/tabs/calendar.js` — перевірка `date+time+fuzzy-title-match` за останні 60 сек
2. Замінити `events.unshift(ev); saveEvents(events)` на `addEventDedup(ev)` у всіх 6 місцях
3. Тест на iPhone: створити подію двічі підряд через AI — переконатись що друга не додалась

---

## 🟡 B-100 — Сова не реагує на «відступи»

### Перевірка реального коду

Файл: `src/ai/prompts.js`, функція `getOWLPersonality()`, рядки 62-66.

**Поточні маркери у ПРАВИЛІ ЕМПАТІЇ:**
> «втомився», «не можу», «забив», «погано», «зле», «хворію», «важко», **«дістало»**, «виснажений», «немає сил», «болить», «не висипаюсь», «сумую», «самотній», «стрес», «тривога», «злий», «розчарований», «здатися», «не встигаю», «не хочу»

**✅ Знайдено новий маркер «дістало»** — близький до того що описано у багу. Не зрозуміло коли саме додався (агент не зміг знайти конкретний коміт).

**❌ Відсутні прямі маркери:** «не доставай», «відчепись», «дай спокій», «вистачить», «досить»

### Висновок

Баг **частково покращено** — «дістало» працює як проксі для подібних емоційних станів. Але прямі команди-відступи («не доставай») сова досі може пропустити.

### Реалістичний план фіксу (~15 хв)

1. Додати у `prompts.js:63` 5 нових тригер-слів: «не доставай», «відчепись», «дай спокій», «вистачить», «досить»
2. Додати правило: «коли бачиш такий маркер → коротко підтверди ('Ок' / 'Зрозумів') і НЕ пропонуй нічого далі»
3. Тест на iPhone: написати «не доставай з задачами» у чаті Продуктивності → сова має промовчати або відповісти коротко без пропозицій

**Примітка:** це лише ~10% повного рішення. Архітектурний фікс (B-102 з USER_STATE сигналом) дасть решту 90%.

---

## 🟢 B-101 — Туманне «Щось пішло не так»

### Перевірка реального коду

**🚨 Знайдено 9 місць** з ідентичним повідомленням `'Щось пішло не так.'`:

1. `src/tabs/habits.js:1362` (чат Продуктивності)
2. `src/tabs/evening-chat.js:73`
3. `src/tabs/evening-chat.js:182`
4. `src/tabs/finance-chat.js:129`
5. `src/tabs/health.js:1566`
6. `src/tabs/notes.js:793`
7. `src/tabs/notes.js:1076`
8. `src/tabs/projects.js:515`
9. `src/tabs/tasks.js:517`

**У `src/ai/core.js` вже існує `_isNetworkError()` (рядки 333-335)** який детектує `AbortError`, `Load failed`, `Failed to fetch`. Але **жодна з 9 чат-функцій не використовує цей helper** — всі показують одне й те саме туманне повідомлення.

### Висновок

Баг масштабніший ніж описано у `NEVERMIND_BUGS.md` («1 + можливо інші»). Реально 9 ідентичних кодоблоків — це повна архітектурна проблема, а не один файл.

### Реалістичний план фіксу (~45 хв)

1. Створити helper `handleChatError(err, addMsg, retryFn)` у `src/ai/core.js`:
   - `AbortError` → тихо (нічого не показувати, це внутрішня скасовка)
   - Мережеві помилки → «Мережа не відповіла. Повтори повідомлення?» + чіп «Повторити» який викликає `retryFn`
   - API/сервер → залишити як є («Щось пішло не так»)
2. Замінити у 9 місцях `addMsg('agent', 'Щось пішло не так.')` на `handleChatError(err, addMsg, () => sendMessage(text))`
3. Тест: вимкнути Wi-Fi і написати у чаті — має зʼявитись зрозуміла помилка з кнопкою

---

## 🟡 B-102 — Табло не реагує на настрій у чаті

### Перевірка реального коду

Файли: `src/owl/brain-signals.js` (333 рядки), `src/owl/brain-pulse.js` (106 рядків).

**🚨 Знайдено 8 сигналів, не 9 як у CLAUDE.md і ROADMAP:**

1. `stuck-task`
2. `passed-event`
3. `upcoming-event`
4. `budget-warn`
5. `appointment-soon`
6. `streak-risk`
7. `project-stuck`
8. `weekly-review`

**Уточнення для документації:** ROADMAP пише «Brain Pulse engine з 9 сигналами», CLAUDE.md теж — насправді 8. Це не баг коду, але документ розходиться з реальністю.

**`USER_STATE` сигнал відсутній.** Brain Pulse не отримує жодного контексту про настрій юзера з чату — навіть якщо у чаті щойно було «не доставай».

### Висновок

Це **архітектурний фікс рівня V3 Фаза 2**, не точковий патч. Точкове додавання тригер-слів у B-100 проблему не розвʼяже бо це інша підсистема (чат vs Brain Pulse).

### Реалістичний план фіксу (~2-3 год — V3 Фаза 2)

1. Додати у `brain-signals.js` сигнал-збирач `_collectUserState()` — читає останні 5 повідомлень кожного чату (через `getRecentChatsAcrossTabs`), детектує маркери відступу/втоми/злості → повертає `{state, intensity, source_tab}`
2. Додати у `brain-pulse.js` правило пригнічення: коли `USER_STATE === 'push-back'` з intensity ≥ medium → ігноруй task/budget/streak сигнали поки `state` не зникне (1-2 години cooldown)
3. Інтегрувати у промпт `getBrainPulsePrompt()` — секція `[СТАН ЮЗЕРА]` з поточним станом
4. Тест: написати «не доставай» у будь-якому чаті → перевірити що Brain Pulse наступні 30 хв не згенерує проактивних повідомлень

---

## 🎯 Рекомендації для Романа на завтра

### Швидкі перемоги (сумарно ~90 хв):

1. **B-103** (~30 хв) — критичний, високий вплив на UX, простий фікс через helper
2. **B-100** (~15 хв) — точкове додавання слів, легко тестувати
3. **B-101** (~45 хв) — масштабна але механічна заміна, велика користь для відлагодження

### Залишити на пізніше:

- **B-102** (~2-3 год) — архітектурне, входить у V3 Фазу 2 — робити окремою сесією

### Документація — потребує оновлення (помилки знайдені під час верифікації):

- `NEVERMIND_BUGS.md` B-103: «5 місць» → **6 місць** (додати `evening-actions.js` 138, 162 + `habits.js` 1275)
- `NEVERMIND_BUGS.md` B-101: «1 + можливо інші» → **9 місць** (повний список вище)
- `CLAUDE.md` AI-логіка: «Brain Pulse engine з 9 сигналами» → **8 сигналів** (повний список вище)
- `ROADMAP.md` Test sprint: те саме «9 сигналів» → **8 сигналів**
