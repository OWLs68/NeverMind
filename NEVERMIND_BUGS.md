# NeverMind — Відомі баги

> **Правило ротації:** у файлі зберігаються **всі відкриті** баги + **закриті у 2 останніх активних сесіях** (згідно `_ai-tools/SESSION_STATE.md`).
> При виклику `/finish` у новій сесії — закриті з найстаршої з 2 активних переносяться у [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md).
> Формат ID: **B-XX** — номер, сортуються хронологічно.
> Використання: `/fix B-XX` — скіл Claude прочитає цей файл і знайде опис бага.

---

## 🔴 Критичні (зламана функціональність)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| B-84 | `src/tabs/health.js` (промпт чату Здоров'я, рядки 1556-1583) | Алергія не зберігається автоматично з чату | Юзер пише "алергія на пил" у чаті Здоров'я. Сова відповідає текстом ("Якщо у тебе алергія…"), але у розділ "Алергії" нічого не потрапляє. При прямому запитанні "Ти додав?" чесно відповідає "Ні, не зберіг". Корінь: у промпті чату Здоров'я **фізично немає** дій `add_allergy` / `delete_allergy` — вони існують лише в `INBOX_TOOLS` (Inbox чат). Порушення "Один мозок". Виявлено 20.04.2026 NRw8G. |
| B-85 | `src/tabs/health.js` (ЖОРСТКИЙ БЛОК рядки 1544-1551) | Опис симптому в чаті Здоров'я відбивається шаблоном "Я не лікар" | Юзер пише "Болить горло вже 3 дні" — констатація симптому, не запитання. Сова дає шаблон "Я не лікар. Це питання до твого лікаря". Нічого не записує у картку, не пропонує. Корінь: ЖОРСТКИЙ БЛОК у промпті не розрізняє **медичне питання** ("що зі мною?", "що робити?") і **опис симптому/факту** ("болить горло 3 дні"). Health-інтерв'ю з 6GoDe (1-3 уточнювальні питання → запис) існує тільки в Inbox. Виявлено 20.04.2026 NRw8G. |
| B-91 | `src/ai/prompts.js` (INBOX_SYSTEM_PROMPT) або `save_memory_fact` tool | "Запам'ятай що я не їм глютен" → сова створила задачу "покупка безглютенової піци" замість факту пам'яті | Юзер явно сказав "Запам'ятай" — ключовий тригер для `save_memory_fact`. AI зігнорував і галюцинував задачу з вигаданим об'єктом ("піцу" взагалі не згадувалось). Критична помилка семантики: (1) "Запам'ятай X" = факт у пам'ять, не задача; (2) "не їм Y" = обмеження/preference, не дія купити протилежне. Корінь — ймовірно: `save_memory_fact` слабко описаний у промпті порівняно з `save_task`, або AI зловживає задачами при будь-якому CRUD. Виявлено 20.04.2026 NRw8G. |
| B-86 | `src/tabs/health.js` (промпт чату Здоров'я) | "Новий прийом у лікаря" не створює подію — сова думає що помилився з часом існуючої | Юзер пише "Завтра прийом у лікаря на 14". В контексті вже є подія "Прийом" на 23 квітня 19:31. Сова: "Ти помилився з часом, наступний прийом 23 квітня". Замість `create_event` пропонує правку існуючої. Коли юзер уточнює "У мене **новий** прийом, завтра на 14" — сова викликає UI-інструмент перемикання періоду Фінансів ("Фінанси: місяць") замість створення події. Корінь: промпт слабко розрізняє CREATE vs EDIT event + UI-інструменти (switch_tab, set_finance_period) не відфільтровані від контексту про події/прийоми. Виявлено 20.04.2026 NRw8G. |

---

## 🟡 Середні (є обхідний шлях або рідко трапляється)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| B-87 | `src/tabs/inbox.js:121`, `src/tabs/evening-chat.js:170` | JSON-блок чіпів не повністю видаляється з тексту бульбашки | Регекс `/\{[\s\S]*?"chips"[\s\S]*?\}/g` у `_parseContentChips` жадібно-лазливий: ріже на **першому** `}` після `"chips"` (всередині першого чіп-об'єкта), лишає решту `{...}]}` як сміття у тексті повідомлення. Сам масив чіпів парситься правильно через `extractJsonBlocks` (depth-tracking). Видно на скріншоті Inbox: "Коли саме почалось? ,{"label":"Три дні тому","action":"chat"}, {"label":"Давно","action":"chat"}]}" — текст у бульбашці. Дубль коду в двох файлах — рекомендовано винести helper у `src/core/utils.js`. Виявлено 20.04.2026 NRw8G (JvzDi позначав B-83 закритим, але фікс не тестував локально). |
| B-88 | `src/tabs/health.js` (промпт рядок 1557) | Мертва дія `log_health` у промпті чату Здоров'я | Промпт пропонує сові дію `{"action":"log_health","energy":7,"sleep":8,"pain":2}`, але handler видалений у 6GoDe разом з legacy шкалами (`nm_health_log` прибрано). Якщо AI викличе — тихий no-op. Не проявилось на юзер-тестах бо AI рідко пропонує на повсякденних фразах. Виявлено 20.04.2026 NRw8G при розборі B-84/85/86. |

---

## 🟢 Дрібні (косметика, не ламає функціонал)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| B-80 | `src/tabs/notes.js` | Layout glitch при видаленні папки через свайп | Чіпи під OWL-баблом на частку секунди перекриваються першою папкою, потім само-виправляється (~250мс). Гіпотеза: layout race між анімацією закриття свайпу (transition 0.25s) і `renderNotes()`. Косметика, не ламає функціонал. Виявлено 18.04.2026 Vydqm. |
| B-89 | `src/owl/inbox-board.js` (swipe логіка чат-вікна) | Свайп закриття чату у вкладці Здоров'я працює криво — **потрібні деталі** | Юзер повідомив що свайп закриття чату у Здоров'ї поганий. Точний симптом не з'ясовано (не закривається / стрибає анімація / працює тільки з сірої ручки зверху / інше). У коді свайп на `.ai-bar-chat-handle` — тільки маленька смужка зверху. Чи проблема загально-чатова чи специфічна для Здоров'я — треба відтворити. Виявлено 20.04.2026 NRw8G. |
| B-90 | `src/ai/prompts.js` (INBOX_SYSTEM_PROMPT) або callAIWithTools | "Зроби темну тему" в Inbox — сова пояснює "Не можу змінювати налаштування теми" замість чесного "темної теми нема у застосунку" | B-82 у JvzDi прибрав `set_theme` tool. Але AI все одно натякає на існування налаштувань ("Ти можеш зробити це самостійно в налаштуваннях застосунку") — це галюцинація. Правильно: "Поки темної теми нема у застосунку, з'явиться пізніше". Косметика тону. Виявлено 20.04.2026 NRw8G. |
| B-92 | `src/core/nav.js:809` (`openMemoryModal`) або `index.html` розмітка `#memory-modal` | Модалка "Пам'ять агента" не закривається свайпом вниз — тільки хрестиком | Юзер відкриває Пам'ять (з Налаштувань або через tool `open_memory`), намагається закрити свайпом вниз — нічого. Тільки ✕ у правому верхньому куті. Інші панелі-модалки (задачі/звички/нотатки/Налаштування) закриваються свайпом. Корінь: `openMemoryModal` викликає лише `modal.style.display='flex'` і `renderMemoryCards()` — не підключає `setupModalSwipeClose`. Виявлено 20.04.2026 NRw8G. |

---

## ✅ Закриті (активні сесії)

_Зберігаються закриті у 2 останніх активних сесіях. Старіші → [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md)._

### Сесія JvzDi (19.04.2026)

| # | Файл | Опис | Як виправлено |
|---|------|------|----------------|
| B-81 | `src/ai/prompts.js` | "Відкрий задачі" у Inbox чаті інтерпретувалось як save_task з title="задачі" замість switch_tab | М'яке правило промпту "відкрий X → switch_tab" не заважало AI обрати save_task. Переписав UI TOOLS секцію як ЖОРСТКЕ правило з прикладами: повідомлення що починається з "відкрий/покажи/перейди до + назва вкладки" → ЗАВЖДИ switch_tab, НЕ save_task. Коміт `240a0b5`. |
| B-82 | `src/ai/ui-tools.js`, `src/ai/prompts.js` | Плацебо tool `set_theme` — сова "Темна тема." але у застосунку немає глобальної темної теми | Tool викликав `applyTheme()` яка лише фарбує таб-бар за поточною вкладкою. Прибрав визначення з UI_TOOLS, case з handleUITool, рядок з промпту, невикористаний імпорт. Повернемо коли буде реальна темна тема. Коміт `240a0b5`. |
| B-83 | `src/tabs/inbox.js`, `src/ai/prompts.js` | Чіпи у Health-інтерв'ю показувались як текст у дужках замість клікабельних кнопок | Health-інтерв'ю промпт з 6GoDe обіцяв чіпи, але Inbox chat не мав парсера JSON блоку `{chips:[...]}` — тільки evening-chat мав. Портував `_parseContentChips` з evening-chat.js, розширив `addInboxChatMsg` параметром `chips`, додав 3 точки показу msg.content з парсингом+рендером. Коміт `0bf3d37`. |

### Сесія 6GoDe (19.04.2026)

| # | Файл | Опис | Як виправлено |
|---|------|------|----------------|
| B-65 | `src/core/boot.js` | "SW load failed" у логах 7+ разів на добу | Корінь — `reg.update()` у `setupSW()` викликався 3 рази без `.catch()` (register, visibilitychange, pageshow). Коли iPhone офлайн/повільний — update() rejects з "Script load failed" → `window.unhandledrejection` → логер. Додав тихі `.catch(() => {})` на всі 3 виклики. Коміт `e634b12`. |

_У сесіях до 6GoDe (dIooU, QV1n2, NFtzw, rSTLV, w3ISi, uDZmz, VJF2M, Vydqm, FMykK, KTQZA, cnTkD, W6MDn, acZEu, 3229b, 6v2eR, jMR6m та старіші) — у [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md)._

---

## 📋 wontfix / Галюцинації AI

### "Синтаксична помилка `<msg.id>`" у `board.js:125` (05.04.2026)

Gemini під час аудиту повідомив про "критичну синтаксичну помилку" у `const ago = Date.now() - (msg.ts || msg.id || Date.now());`. Це **галюцинація** — у реальному коді кутових дужок немає, це валідний JS-ланцюжок з `||` fallback.

**Урок:** завжди перевіряти точну цитату Gemini проти реального коду перед виправленням.

---

## 📋 Як додавати новий баг

```markdown
| B-XX | `src/шлях/файл.js:рядок` | Короткий опис | Деталі якщо є |
```

Пріоритети:
- 🔴 **Критичний** — функціональність зламана повністю
- 🟡 **Середній** — є обхідний шлях або рідко трапляється
- 🟢 **Дрібний** — косметика, не заважає роботі

Після виправлення:
1. Перенести рядок у секцію "✅ Закриті (активні сесії)" з датою і ID сесії
2. Додати коротке пояснення як виправлено
3. При виклику `/finish` — старіші сесії автоматично ротуються у `_archive/BUGS_HISTORY.md`
