# NeverMind — Відомі баги

> **Правило ротації:** у файлі зберігаються **всі відкриті** баги + **закриті у 2 останніх активних сесіях** (згідно `_ai-tools/SESSION_STATE.md`).
> При виклику `/finish` у новій сесії — закриті з найстаршої з 2 активних переносяться у [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md).
> Формат ID: **B-XX** — номер, сортуються хронологічно.
> Використання: `/fix B-XX` — скіл Claude прочитає цей файл і знайде опис бага.

---

## 🔴 Критичні (зламана функціональність)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| B-103 | `src/tabs/inbox.js:582` + `src/tabs/habits.js:936/1096/1275` + `src/tabs/evening-actions.js:138/162` (**6 місць** — оновлено qG4fj 25.04 верифікацією) | Дублікати подій у Календарі. iPhone 24.04 v376: у Календарі зверху два «Концерт 30 квітня» + два у секції «Події · квітень». Юзер просив одну — створились дві. | **Корінь:** код створення події просто робить `events.unshift(ev); saveEvents(events);` без перевірки на схожу подію. Модель може повернути 2 `create_event` tool_calls у одній відповіді (або юзер двічі тапнув до відповіді, або AbortController + повторний запит). Правило 4.12 «антидублювання» у промпті є але прохальне — модель ігнорує свої ж попередні tool_calls у тій же відповіді. **Фікс:** винести у `calendar.js` helper `addEventDedup(ev)` який перевіряє: чи вже є подія з `date+time+fuzzy-title-match` за останні 60 сек → якщо так, не додавати і повернути «exists». Всі **6 call-sites** делегують сюди. Виявлено v2vYo 24.04.2026, верифіковано qG4fj 25.04 — насправді 6 місць (не 5). Деталі → `_ai-tools/BUGS_VERIFICATION.md`. |

---

## 🟡 Середні (є обхідний шлях або рідко трапляється)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| B-100 | `src/ai/prompts.js` (`getOWLPersonality` → ПРАВИЛО ЕМПАТІЇ) | Сова не реагує на пряме «відступи». iPhone 22-23.04 скрін (до v374): юзер у чаті Продуктивності написав «Не доставай з задачами» → сова відповіла «Давай зосередимось на вечері. Що ще можу запропонувати?». Має замовкнути або коротко ОК. | **Корінь:** у правилі емпатії є «втомився / не можу / забив / дістало» — але немає прямих тригерів «не доставай / відчепись / дай спокій / вистачить / досить». Плюс правило не забороняє пропонувати альтернативу після маркера. **Фікс:** додати 5 нових тригер-слів + правило «коли бачиш маркер відступу — коротко підтверди і НЕ пропонуй нічого». Потребує верифікації на v374+ — старий скрін був до фіксу B-97/98/99, можливо вже не відтворюється. Виявлено v2vYo 24.04.2026. |
| B-102 | `src/owl/brain-signals.js` + `src/owl/brain-pulse.js` | Табло не реагує на настрій у чаті. Коли юзер у чаті пише «не доставай / відчепись» — сова продовжує показувати на табло проактивні пропозиції про задачі і звички, наче нічого не сталось. | **Архітектурно:** Brain Pulse збирає 9 сигналів (stuck-task, budget-warn тощо) але серед них немає USER_STATE сигналу з настрою чату. Мозок фізично не знає що юзер просить «дай спокій». **Фікс:** рівня V3 Фаза 2 (USER_STATE у Brain Pulse) — уже у плані ROADMAP. Не точковий патч, бо треба новий тип сигналу + логіка пригнічення інших сигналів коли USER_STATE=«push-back». Виявлено v2vYo 24.04.2026. |

---

## 🟢 Дрібні (косметика, не ламає функціонал)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| B-101 | **9 чат-барів** (верифіковано qG4fj): `habits.js:1362`, `evening-chat.js:73/182`, `finance-chat.js:129`, `health.js:1566`, `notes.js:793/1076`, `projects.js:515`, `tasks.js:517` | Туманне повідомлення «Щось пішло не так» на будь-яку помилку запиту. iPhone 24.04 v376 після «Купити молоко» у чаті Продуктивності: у Діагностиці `FAIL 12283мс POST api.openai.com — Load failed` (мережа LTE впала), юзер бачить «Щось пішло не так» і не знає що робити — повторити чи це баг. | **Фікс:** розрізняти типи помилок у обробнику: (1) `AbortError` → тихо, не показувати — це внутрішня скасовка при новому тригері; (2) мережеві (`Load failed`, `Failed to fetch`, timeout) → «Мережа не відповіла. Повтори повідомлення?» + кнопка-чіп «Повторити» який повторює останній `text`; (3) API-помилки сервера (5xx/4xx) → лишити як є. **`_isNetworkError()` у `ai/core.js:333` вже існує — треба тільки використати його у 9 місцях** (помста qG4fj). Винести спільний обробник `handleChatError(err, addMsg, retryFn)` у `ai/core.js` і прокинути у всі 9. Виявлено v2vYo 24.04.2026, обсяг уточнено qG4fj 25.04. Деталі → `_ai-tools/BUGS_VERIFICATION.md`. |
| B-80 | `src/tabs/notes.js` | Layout glitch при видаленні папки через свайп | Чіпи під OWL-баблом на частку секунди перекриваються першою папкою, потім само-виправляється (~250мс). Гіпотеза: layout race між анімацією закриття свайпу (transition 0.25s) і `renderNotes()`. Косметика, не ламає функціонал. Виявлено 18.04.2026 Vydqm. |

---

## ✅ Закриті (активні сесії)

_Зберігаються закриті у 2 останніх активних сесіях. Старіші → [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md)._

### Сесія R5Ejr (24.04.2026)

| # | Файл | Опис | Як виправлено |
|---|------|------|----------------|
| B-104 | `src/tabs/tasks.js` + `src/tabs/habits.js` + `src/owl/proactive.js` | Stale OWL board на вкладці Задач — сова повторювала «закрий 3 задачі: X, Y, Z» попри те що юзер щойно їх закрив тапом ✓. Фікс B-98 (watchdog прапорця) НЕ допомагав — генерація проходила успішно, але з поганим контекстом. | **2 причини:** (1) у 4 з 8 місць де `status='done'` ставилось БЕЗ `completedAt` (tasks.js:171 toggleTaskStep, tasks.js:192 toggleTaskStatus, habits.js:1373 AI complete_step, habits.js:1383 AI complete_task) — `getAIContext` блок «Нещодавно ЗАКРИТІ задачі» фільтрує по `t.completedAt` і їх пропускав. (2) У `getTabBoardContext('tasks')` не було блоку «Нещодавно закриті» (тільки у `_getInboxBoardContext`). Фікс: усі 4 місця тепер ставлять `completedAt+updatedAt` при 'done', видаляють `completedAt` при reopen. Додано блок «Нещодавно ЗАКРИТІ задачі (НЕ повторюй з boardHistory)» у tab-контекст. Коміт `3e3892a`. |

### Сесія v2vYo (24.04.2026)

| # | Файл | Опис | Як виправлено |
|---|------|------|----------------|
| B-97 | `src/ai/prompts.js` (+ `src/tabs/habits.js`) | Чат Задач відмовляв «це подія, а не задача» на «Прийом у лікаря відміни» попри наявність `delete_event` у `INBOX_TOOLS` | Доданий `GLOBAL_TOOLS_RULE` — спільний блок «інструменти глобальні у всіх 8 чатах» + явний фузі-матч паттерн на скасування події («Прийом у лікаря відміни» + у контексті `[ID:123] "Прийом у лікаря"` → `delete_event(123)`). Підключений у чаті Задач (`habits.js` перед `REMINDER_RULES`). Готовий до пропаганди у 7 інших чатів якщо на iPhone спрацює. Глибший архітектурний фікс (Gemini-діагноз: Context Segmentation Failure) — через V3 Фазу 1 (`_reasoning_log`). Коміт `9e065a1`. |
| B-98 | `src/owl/proactive.js` (`generateBoardMessage`) | OWL табло не оновлювалось 8+ годин попри 5+ тригерів `[OWL board] stale message detected, forcing generation`. Прапорець `_boardGenerating[tab]` залипав `true` бо не було `try/finally` на все тіло функції — виключення у prompt-збірці (lines 624-745) або hung fetch без помилки лишали прапорець назавжди | Обгорнуто все тіло функції у `try { ... } finally { clearTimeout(watchdog); _boardGenerating[tab] = false; }` + додано watchdog `setTimeout(60s)` який примусово скидає прапорець і робить `abort()` якщо прапорець висить >60 сек. Коміт `5b25374`. |
| B-99 | `src/owl/brain-pulse.js:42` | У логах `[brain-pulse] skip:` іноді з пустою причиною після двокрапки — заважало діагностиці | Додано fallback `judge.reason \|\| 'unknown'` у `console.log`. Коміт `5b25374`. |

_Закриті у сесіях Gg3Fy / EWxjG / NRw8G / JvzDi / 6GoDe та старіші — у [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md)._

---

## 📋 wontfix / Галюцинації AI

### "Синтаксична помилка `<msg.id>`" у `board.js:125` (05.04.2026)

Gemini під час аудиту повідомив про "критичну синтаксичну помилку" у `const ago = Date.now() - (msg.ts || msg.id || Date.now());`. Це **галюцинація** — у реальному коді кутових дужок немає, це валідний JS-ланцюжок з `||` fallback.

**Урок:** завжди перевіряти точну цитату Gemini проти реального коду перед виправленням.

---

## 📋 Як додавати новий баг

```markdown
| B-XX | `src/шлях/файл.js:рядок` | Короткий опис | Деталі якщо є |
```

Пріоритети:
- 🔴 **Критичний** — функціональність зламана повністю
- 🟡 **Середній** — є обхідний шлях або рідко трапляється
- 🟢 **Дрібний** — косметика, не заважає роботі

Після виправлення:
1. Перенести рядок у секцію "✅ Закриті (активні сесії)" з датою і ID сесії
2. Додати коротке пояснення як виправлено
3. При виклику `/finish` — старіші сесії автоматично ротуються у `_archive/BUGS_HISTORY.md`
