# NeverMind — Відомі баги

> **Правило ротації:** у файлі зберігаються **всі відкриті** баги + **закриті у 2 останніх активних сесіях** (згідно `_ai-tools/SESSION_STATE.md`).
> При виклику `/finish` у новій сесії — закриті з найстаршої з 2 активних переносяться у [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md).
> Формат ID: **B-XX** — номер, сортуються хронологічно.
> Використання: `/fix B-XX` — скіл Claude прочитає цей файл і знайде опис бага.

---

## 🔴 Критичні (зламана функціональність)

| ID | Місце | Опис | Деталі |
|---|---|---|---|
| B-110 | OWL табло (Inbox) | Агент кидає 3 теми в одне повідомлення — питання про підсумок дня + список з 5 задач + пропозиція переформулювати/видалити | Порушує UX-принцип «одна думка за раз». Скрін v494 23:42: «Сьогодні виконано всі звички 💪 Але підсумок дня ще не записано. Як пройшов день? Чи актуальні ще ці задачі: 'Поприбирати в машині', 'Поміняти номер на склад', 'Заплатити за три штрафа', 'Заплатити за житло', 'Подати декларацію'? Можливо, варто їх видалити або переформулювати?». Розрізати на 3 окремих повідомлення з пріоритетом одного. |
| B-111 | OWL табло (вкладка Я) | Табло нагадує про події о 19:00 коли вже 23:40 — пише про минуле як майбутнє | Скрін v494 23:42-23:45: «Не забудь про два важливі події о 19:00 — Тренування та Кава з Вовою». О 23:42 ці події 4.5 години в минулому. Фільтрувати події що минули — або переформулювати на «пропустив», або взагалі не показувати у proactive nudges після часу події. |

---

## 🟡 Середні (є обхідний шлях або рідко трапляється)

| ID | Місце | Опис | Деталі |
|---|---|---|---|
| B-109 | OWL табло (Inbox + вкладка Я) | Табло занадто велике — займає півекрану, виштовхує контент | Скрін v494 23:42 (Inbox): сова + текст 6 рядків + чіпи дій займає ~50% висоти. На вкладці Я (23:42) — теж великий блок. Зменшити: менший аватар сови, обмеження тексту до 3 рядків з «...показати більше», або компактніший layout (горизонтальний замість вертикального). |
| B-112 | Я → Глибокий звіт | Незрозуміла метрика «14%» у тексті звіту — юзер не розуміє за який період і як рахується | Скрін v494 23:45: «Звичка 'Бігати щодня' виконана лише на 14%, а 'Ходити в зал' — 0%. Займався NeverMind 3 з 7 днів, що становить 43%». 14% = 1/7 днів за тиждень, але це не очевидно. Замінити на абсолютну форму «1 з 7 днів за тиждень», «0 з 4 днів» — як у бубликах активності. |
| B-113 | Я → блок «OWL знає тебе» | Блок не оновлюється автоматично коли юзер виконав задачу/звичку, додав нотатку — треба перезайти на вкладку | Очікувана поведінка: блок слухає подію `nm-data-changed` і ререндерить коли дані змінились. Зараз генерується раз при відкритті Я і кешується у `nm_me_weekly_insights`. Можливі рішення: (а) інвалідувати кеш при `nm-data-changed`, (б) debounced ререндер 30-60 сек після зміни (щоб не дзижчати при кожному кліку). |

---

## 🟢 Дрібні (косметика, не ламає функціонал)

_Немає відкритих дрібних багів._

---

## ✅ Закриті (активні сесії)

_Зберігаються закриті у 2 останніх активних сесіях (H0DxS + TdIqO). Старіші (lRnXU і раніше) → [`_archive/BUGS_HISTORY.md`](_archive/BUGS_HISTORY.md)._

_Сесія **H0DxS** (29.04.2026) — фікс-сесія: тижневий зріз звичок у `getAIContext` + bump `INSIGHTS_VERSION` (корінь «OWL знає тебе каже жодної звички за тиждень» при 3/4 виконаних сьогодні); правило «ЗОВНІШНІ ФАКТИ» у `getOWLPersonality()` проти галюцинацій (корінь: AI вигадав сюжет фільму «Кіллхаус» якого не знає); закрито 2 баги lRnXU — (1) «Відкрий звички» з чату Я тепер реально перемикає на підтаб Звички (`switchProdTab('habits')` після `switchTab('tasks')`), (2) блок «🦉 OWL знає тебе» — білий фон 0.85 + темніша рамка + тінь, чітко видно на бежевому фоні; видалено онбординг-модалку при першому вході (поля у Налаштуваннях). Деталі → `_ai-tools/SESSION_STATE.md`._

_Сесія **TdIqO** (29.04.2026) — повна переробка вкладки «Я»: 2 progress-кільця замість 4 графіків, тиждень-картки 7 днів зі шкалою заповнення, toast матове скло + зелена «Відновити», видалено ~25 ✓-підтверджень дій. Уніфікація базових кольорів сутностей по Inbox (Подія `#3b82f6`, Задача `#2fd0f9`, Звичка `#16a34a`) — календарну бірюзу замінено. Багів NeverMind не закрила. Перевірити у v489+: помилки `No identifiers...` з логів (з закешованої версії 27.04 до B-108 фіксу). Деталі → `_ai-tools/SESSION_STATE.md`._

---

## 📋 wontfix / Галюцинації AI

### "Синтаксична помилка `<msg.id>`" у `board.js:125` (05.04.2026)

Gemini під час аудиту повідомив про "критичну синтаксичну помилку" у `const ago = Date.now() - (msg.ts || msg.id || Date.now());`. Це **галюцинація** — у реальному коді кутових дужок немає, це валідний JS-ланцюжок з `||` fallback.

**Урок:** завжди перевіряти точну цитату Gemini проти реального коду перед виправленням.

---

## 📋 Як додавати новий баг

```markdown
| B-XX | `src/шлях/файл.js:рядок` | Короткий опис | Деталі якщо є |
```

Пріоритети:
- 🔴 **Критичний** — функціональність зламана повністю
- 🟡 **Середній** — є обхідний шлях або рідко трапляється
- 🟢 **Дрібний** — косметика, не заважає роботі

Після виправлення:
1. Перенести рядок у секцію "✅ Закриті (активні сесії)" з датою і ID сесії
2. Додати коротке пояснення як виправлено
3. При виклику `/finish` — старіші сесії автоматично ротуються у `_archive/BUGS_HISTORY.md`
