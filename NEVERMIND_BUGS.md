# NEVERMIND — Відомі баги

> Один баг за раз. Виправив — переміщуй в Закриті.
> Додавай нові баги одразу як знайдеш.
> Формат ID: **B-XX** — номер, сортуються хронологічно.
> Використання: `/fix B-XX` — скіл Claude прочитає цей файл і знайде опис бага.

---

## 🔴 Критичні (зламана функціональність)

_Наразі порожньо — всі 3 закрито у сесії cnTkD 17.04.2026._

---

## 🟡 Середні (є обхідний шлях або рідко трапляється)

_Наразі порожньо._

---

## 🟢 Дрібні (косметика, не ламає функціонал)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| B-58 | `src/tabs/finance.js`, `src/ai/core.js` | Автогенерація підкатегорій — обмежити до 3 | Зараз 5+. Максимум 3, решту юзер додасть сам |
| B-65 | `sw.js` | SW load failed — Service Worker не завантажився | Одноразова помилка, не критично |

---

## ✅ Закриті (для довідки)

| # | Коли | Опис |
|---|------|------|
| B-58 | 17.04.2026 (KTQZA) | **Автогенерація підкатегорій обмежена до 3.** Раніше дефолти у `FIN_DEFAULT_SUBCATS` містили по 5 для кожної категорії (Їжа: Продукти/Ресторан/Кафе/Доставка/Фастфуд тощо) — всі автоматично створювались, юзер мусив вручну видаляти зайві. Фікс у 3 місцях `src/tabs/finance-cats.js`: (1) `_makeCatObj` — `.slice(0, 3)` при створенні дефолтних категорій у свіжій установці. (2) `normalize` у міграції — для старих категорій з порожнім `subcategories` доставляє `.slice(0, 3)` дефолтних. (3) `createFinCategory` — якщо юзер/AI задав явно, беремо до 3; якщо порожньо — дефолтні до 3. (4) `src/ai/core.js` — оновлено опис AI-tool `create_finance_category.subcategories`: "Максимум 3 підкатегорії. Тільки якщо юзер прямо їх назвав". Існуючі категорії з 5 підкатегоріями не зачіпаються — обмеження тільки для нових створень. |
| B-57 | 17.04.2026 (KTQZA) | **Стрілки переміщення категорій у edit-режимі сітки.** Було: блок "Позиція в сітці" зі стрілками ⌃⌄ всередині модалки редагування — незручно (треба відкрити модалку щоб пересунути). Стало: у edit-режимі ✎ на кожному кружечку категорії з'являються дві 22px кнопки `‹` ліворуч і `›` праворуч. Тап → `moveFinCategory(id, ±1)` + `renderFinance()`. `event.stopPropagation()` щоб не відкривалась модалка. `moveFinCategory` і `renderFinance` додано у window-exports. Блок "Позиція в сітці" з модалки видалено. Файли: `src/tabs/finance.js`, `src/tabs/finance-modals.js`. |
| B-61 | 17.04.2026 (KTQZA) | **Тіні "левітації" на Hero кругу і кружечках категорій.** Раніше Hero donut мав ледь помітну тінь `rgba(30,16,64,0.08)` 1 шар; кружечки категорій — без тіні, виглядали плоскими. Стало: (1) Hero donut — `drop-shadow(0 8px 18px rgba(30,16,64,0.18)) drop-shadow(0 3px 6px rgba(30,16,64,0.08))` — два шари для глибини, 0.18 alpha для помітності. (2) Кожен кружечок категорії — `box-shadow: 0 6px 14px ${color}35, 0 2px 4px ${color}20` — **тінь кольором самої категорії**, натяк що інтерактивне. У edit-режимі той самий shadow + outline `0 0 0 2px ${color}55`. Файл: `src/tabs/finance.js`. |
| B-79 | 17.04.2026 (KTQZA) | **Safari/Chrome non-standalone застрягали на старій версії (v53 · 07:58) попри PWA v193+.** Корінь: на 07:53 Amsterdam був задеплоєний SW ще **до B-73 фіксу** (cache-first для ВСІХ файлів, включно з `sw.js` сам собою). Safari зловив цей деплой → закешував index.html/bundle.js/sw.js. Коли browser періодично перевіряв `sw.js` на оновлення, iOS Safari перехоплював цей запит через SW fetch listener (хоча W3C spec каже "bypass SW") — старий SW віддавав закешований `sw.js v1` → browser бачив "той самий SW" → no update. Zombie-lock: старий SW сам себе увічнював. PWA standalone обійшло це бо B-73 додав `skipWaiting()` + postMessage handler у новий SW, але постраждали ті хто зловили v53 деплой ДО B-73. Фікс: (1) meta-теги `Cache-Control: no-cache, no-store, must-revalidate` + `Pragma: no-cache` + `Expires: 0` у `<head>` — запобіжник на майбутнє. (2) Ручна інструкція: очистити Safari website data один раз (Налаштування → Safari → Дані сайтів → owls68.github.io → Видалити). Після цього новий SW network-first (B-73) → автоматичне оновлення назавжди. |
| B-78 | 17.04.2026 (KTQZA) | **Дефолтні категорії отримували сірий колір `#78716c` → виглядали як archived.** На скріні Підписки були сірі попри існування виразної іконки "subscription". Корінь: у legacy-версіях AI створював категорію через `createFinCategory` з `color: data.color || pickRandomCatColor(order)` де `pickRandomCatColor()` без seed викликав `Math.random` і міг випасти на `#78716c` (idx 12 у палітрі з 14 кольорів). У сітці сірий колір плутав як архівний. Фікс (`src/tabs/finance-cats.js`): (1) додано `FIN_DEFAULT_COLORS` — словник виразних кольорів для 30+ відомих назв (Їжа оранжева, Транспорт зелений, Підписки темний бурштин, Здоров'я рожеве, Житло червоне, Зарплата зелена тощо). (2) `_makeCatObj` і `createFinCategory` тепер пріоритизують `FIN_DEFAULT_COLORS[name]` ПЕРЕД `pickRandomCatColor`. (3) `normalize` у міграції: якщо категорія має name з `FIN_DEFAULT_COLORS` І поточний колір = `#78716c` (крім легальної категорії "Інше") — ремап на дефолтний. (4) `normalize` також доставляє `FIN_DEFAULT_ICONS[name]` якщо icon порожня/other і `FIN_DEFAULT_SUBCATS[name]` якщо subcategories порожній — для старих категорій створених до розширення дефолтів. (5) `createFinCategory` підхоплює дефолтні icon і subcategories якщо юзер/AI не задав їх явно. |
| B-75 | 17.04.2026 (KTQZA) | **Донат Фінансів фрагментований на багато сегментів + дубльована "Їжа" у сітці.** Корінь: у `nm_finance_cats` історично накопичились дублі категорій з однаковою назвою (різні `id`/`color`). `catMap` у `renderFinance` групує транзакції за NAME (не ID) → кожен дубль отримував той самий sum → донат малював 2 сегменти по 67% → переповнення кола = візуальна фрагментація. Також сітка рендерила обидва кружечки. Фікс у `src/tabs/finance-cats.js::_migrateFinCats`: після `normalize` додано `dedupe(list)` який за ключем `name.trim().toLowerCase()` залишає першу категорію, а підкатегорії дублів мержить у першу (не втратити). Спрацьовує автоматично на кожному `getFinCats()` — `needsSave` порівняння бачить що масив став коротший → запис у localStorage. Транзакції не потребують міграції (зберігаються як `{category:'Їжа'}` рядком — після дедупу автоматично приліплюються до єдиної категорії). |
| B-76 | 17.04.2026 (KTQZA) | **`formatMoney` показував крапку у дисплеї сум.** Список операцій виводив `-€52.20` замість `-€52,20` (європейський формат). Причина: `Math.abs(n).toFixed(2)` дає рядок з крапкою (JS default). Фікс у `src/tabs/finance.js::formatMoney` — `.replace('.', ',')` після `toFixed(2)`. Торкається всіх 11 місць у проекті де `formatMoney` викликається (список операцій, hero-круг, AI-контекст, бюджет warnings, інсайт дня). |
| B-77 | 17.04.2026 (KTQZA) | **Кнопка "+додати" у блоці Останні операції завжди створювала витрату.** `openAddTransaction()` без `prefill.type` → `_showTransactionModal` за замовчуванням `type='expense'` → модалка "Нова витрата" без можливості перемкнути на дохід. Фікс у `src/tabs/finance-modals.js::_renderTransactionModalBody`: додано toggle "Витрата / Дохід" (pill-style, активний варіант білий з тінню) одразу після `title`. Показується тільки при створенні (`!isEdit`) — у режимі редагування тип зафіксований. Нова експортована функція `setFinTxType(type)` перемикає `_finTxCurrentType` і скидає `_finTxCategory/_finTxSubcategory` (бо категорії у expense/income списках різні). Додано у window-exports. |
| B-44 | 17.04.2026 (KTQZA) | **Кома на калькуляторі ставила крапку замість коми.** Кнопка `,` передавала `'.'` у `finCalcAppend`, яка зберігала `.` у `_finTxExpression`. Дисплей показував `600.50` замість європейського `600,50`. Фікс (3 дрібні правки у `src/tabs/finance-modals.js`): (1) кнопка тепер передає `','` у `finCalcAppend(',')`. (2) `finCalcAppend` обробляє `','` і `'.'` однаково — зберігає як `,`, блокує другу кому/крапку у числі. (3) При редагуванні існуючої операції `_finTxExpression` = `String(data.amount).replace('.', ',')` щоб дисплей показував кому. `calcEvaluate` на рядку 55 вже робив `.replace(/,/g, '.')` перед обчисленням — парсинг суми при збереженні працює без змін. |
| B-50 | 17.04.2026 (KTQZA) | **"Транзакції" → "Операції" у всьому UI.** Користувацький термін застарів. Замінено 18 місць у видимих текстах і AI-промптах (AI-промпти — бо AI потім переказує юзеру): `src/tabs/finance.js` (6 рендер-рядків: пустий стан, список "Останні операції", "Всі операції", toast "Немає операцій за цей період", AI-контекст "Останні операції" з ID), `src/tabs/inbox.js` (4 agent-повідомлення у чаті + typeLabel кошика), `src/tabs/finance-chat.js` (системний промпт + toast видалення), `src/owl/proactive.js` (fallback-текст табло Фінансів), `src/ai/core.js` (labels кошика + 3 tool descriptions: `update_transaction`, `delete_finance_category`, `merge_finance_categories`), `index.html` (UPPERCASE-заголовок "Операції" рядок 671). Коментарі коду `// транзакції...` і внутрішні ідентифікатори (`update_transaction`, `nm_finance`, `_finTxExpression`) НЕ чіпались — у правилі B-50 явно вказано "тільки видимі тексти". |
| B-70 | 17.04.2026 (cnTkD) | **Сітка категорій + Hero donut зникли з вкладки Фінанси.** Діагностика дала точку: `_finCatsGrid error: TypeError: undefined is not an object (evaluating 's.replace')` — `escapeHtml(undefined)` у `utils.js:57`. Корінь проблеми: у `processUniversalAction` (`habits.js:1226`) було `catList.includes(category)` — але `catList` це масив ОБ'ЄКТІВ (не рядків), тому `.includes('Їжа')` завжди `false` → `catList.push('Їжа')` додавав **рядок у масив об'єктів** → "биті" категорії без `id`/`name` → `_finCatsGrid` падав на `escapeHtml(cat.id)`. Фікс (3 шари захисту): (1) `escapeHtml(s)` → `String(s ?? '').replace(...)` у `utils.js` — захищає ВСЕ застосунок. (2) `_migrateFinCats` у `finance.js` перевіряє КОЖНУ категорію (а не тільки першу), нормалізує биті об'єкти + рядки у валідні через `_makeCatObj`. (3) `_finCatsGrid` фільтрує `c.id && c.name && !c.archived`. (4) `processUniversalAction` переписано: `catList.some(c => c.name === category)` + виклик `createFinCategory` замість `.push()` — більше не створює биті дані. Файли: `src/core/utils.js`, `src/tabs/finance.js`, `src/tabs/habits.js`. |
| B-71 | 17.04.2026 (cnTkD) | **Чат-бар Фінансів не створював картку в Inbox.** Раніше B-48 фікс у `sendFinanceBarMessage` (finance.js) для `save_expense`/`save_income` додавав Inbox запис, але `processUniversalAction` обробляв action першим (finance.js handler ніколи не виконувався). Результат: транзакція створювалась, але у стрічці Inbox — порожньо. Фікс: перенесено логіку Inbox-запису у `processUniversalAction` (`habits.js:1219`) — тепер ВСІ шляхи створення транзакції з чатів (Inbox, Finance, Task chat, Me chat, Evening, Health, Projects) створюють картку в Inbox з тим самим `id`/`ts` що й транзакція. Файл: `src/tabs/habits.js`. |
| B-72 | 17.04.2026 (cnTkD) | **Інсайт дня вигадував неточні числа** (€761 замість реальних €750 при €50+€700 на Їжу). AI "творчив" арифметику. Фікс у `_refreshFinInsight` промпті: (1) додано **жорстке правило точності чисел** зверху — "якщо число — воно МАЄ БУТИ ТОЧНО ТАКИМ ЯК У ДАНИХ, не округляй, не додавай, не вигадуй". (2) Шаблони переписано з явними формулами: "річна проекція = X × 12 (без округлень)", "економія за рік = X × 52". (3) Дані передаються у списковому форматі (кожна категорія з нового рядка `- Категорія: ${formatMoney(a)}`) — AI важче "змішувати" цифри. (4) `temperature` 0.7 → 0.3 — менше творчості, більше точності. (5) Якщо не впевнений у числі — давати пораду без цифри. Файл: `src/tabs/finance.js`. |
| B-74 | 17.04.2026 (cnTkD) | **Лічильник версій скинувся: очікувалось 100+, показувало v53.** Розслідування: на 13.04.2026 було v131+ (підтверджено `_archive/SESSION_STATE_2026-04-13.md`), між 13.04 і 15.04 хтось зробив `git reset --hard` + `git push --force` — це відкотило `index.html` у старий стан з низькою версією, `auto-merge.yml` читав номер з бейджа і продовжив інкрементувати від низької цифри. Первинні v54-v130 garbage collected з git. Фікс: новий файл `deploy-counter.txt` — абсолютний лічильник, ініціалізовано `184` (оцінка 131 + ~53 = ~184). `auto-merge.yml` тепер читає/пише цей файл (MAX до/після merge як захист від `-X theirs`). Наступний деплой = v185. Попередження у `CLAUDE.md` → "Екстрений скид" про оновлення лічильника перед force-push. Повна хронологія → `docs/CHANGES.md` 17.04 запис. Файли: `deploy-counter.txt`, `.github/workflows/auto-merge.yml`, `CLAUDE.md`, `docs/CHANGES.md`. |
| B-73 | 17.04.2026 (cnTkD) | **PWA не оновлювалось на iOS standalone** після деплою. Браузер Chrome і Safari показували v53, але PWA (з home screen) застрягав на v52 навіть після закриття-відкриття. Причина: `fetch` handler у `sw.js` мав cache-first стратегію для нашого коду (HTML/JS/CSS) без перевірки мережі — онлайн завжди повертався старий файл з кешу. Плюс 3 деплоя підряд за 5 хв не дали SW пройти цикл install→wait→activate. Фікс: (1) `sw.js` — network-first для `index.html`/`bundle.js`/`style.css`/`sw.js`/`manifest.json`, cache як fallback для офлайну. Інші ресурси (картинки, шрифти) лишаються cache-first. (2) `sw.js` — додано `SKIP_WAITING` message handler. (3) `boot.js setupSW` — при statechange `'installed'` + є controller → postMessage SKIP_WAITING у новий SW (форсує активацію без чекання). При register().then() — якщо вже є `reg.waiting` → теж SKIP_WAITING. (4) `doReload` додає `?_v=<timestamp>` query-параметр — iOS PWA не бере з pages cache при такому URL. Підтверджено Романом після деплою. Файли: `sw.js`, `src/core/boot.js`. |
| B-45 | 17.04.2026 (cnTkD) | **Нотатки: шапка OWL табло не прозора.** Контент не проглядав під табло при скролі. Причина: `#page-notes` використовував `sticky` header з непрозорим фоном `#fed7aa` замість flex+absolute overlay як у Фінансах. Фікс: переписано структуру на `flex-column` + `#notes-fixed-top` + `#notes-scroll`, додано у `applyBoardOverlays` у `boot.js` (робить header абсолютним `pointer-events:none`, scroll розтягується на всю висоту). Тепер контент плавно заходить під табло однаково з Фінансами. Файли: `index.html`, `src/core/boot.js`. |
| B-46 | 17.04.2026 (cnTkD) | **Інсайт дня не оновлювався + шаблонний контент.** Кеш 12 год = один текст весь день. Контент — загальні фрази. Фікс: (1) кеш 12 год → 1 год. (2) Додано hash-інвалідацію — хеш даних (кількість+суми+топ-3 категорії) зберігається у кеші, при зміні будь-якого параметра кеш недійсний і промпт виконується заново. (3) Переписаний промпт з явними заборонами (не повторювати загальні суми, без "стеж за витратами", без "в цілому") + 5 конкретних шаблонів (річна проекція, відхилення, економія, перевищення ліміту, тренд). `temperature` 0.5→0.7 для різноманітності. Файл: `src/tabs/finance.js`. |
| B-47 | 17.04.2026 (cnTkD) | **Дублікат папки "Здоров'я" у Нотатках.** AI створював різні варіанти апострофів: `'` (U+0027), `ʼ` (U+02BC), `'` (U+2019) — створювались як окремі папки. Фікс: нова функція `normalizeFolderName()` у `notes.js` зводить всі варіанти апострофів до ASCII `'`. `addNoteFromInbox` тепер перевіряє нормалізовану назву проти існуючих папок (case-insensitive) — використовує існуючу якщо є match. Плюс одноразова міграція `_migrateFoldersApostrophes()` прогонить всі існуючі нотатки і об'єднає папки з однаковою нормалізованою назвою. Прапор `nm_folders_apostrophe_migrated`. Файл: `src/tabs/notes.js`. |
| B-48 | 17.04.2026 (cnTkD) | **Операція через чат-бар Фінансів не створювала картку в Inbox.** Через Inbox → картка є, через чат-бар Фінансів — тільки операція без запису у стрічку. Фікс: у handler `save_expense`/`save_income` (`sendFinanceBarMessage` у `finance.js`) додано створення запису у `nm_inbox` з тим самим `id`/`ts` що й транзакція + `category: 'finance'` + `processed: true`. Тепер будь-яка операція видима у Inbox стрічці незалежно від точки введення. Файл: `src/tabs/finance.js`. |
| B-52 | 17.04.2026 (cnTkD) | **Категорії і підкатегорії у модалці транзакції виглядали однаково.** Фікс: у `_renderTransactionModalBody` розділено візуально. Категорії: `padding:7px 14px`, `font-size:13.5px`, `font-weight:800`, `border:2px` — **primary** вигляд. Підкатегорії: `padding:3px 9px`, `font-size:11.5px`, `font-weight:500`, `border:1px` + лівий border `2px solid rgba(194,65,12,0.18)` як індикатор ієрархії + лейбл "Підкатегорія" дрібним текстом. Файл: `src/tabs/finance.js`. |
| B-53 | 17.04.2026 (cnTkD) | **Список операцій без підкатегорії.** Показувалось "Транспорт −€17" замість "Транспорт · Парковка −€17". Фікс: у `_finTxsBlock` і `openAllTransactions` додано рендер `t.subcategory` після категорії через `·` розділювач, стилем `font-size:11px; font-weight:500; color:rgba(30,16,64,0.4)` — категорія жирна, підкатегорія легша і сіріша. Файл: `src/tabs/finance.js`. |
| B-54 | 17.04.2026 (cnTkD) | **Свайп видалення операцій — переробка механіки.** Раніше: свайп вліво >90px = миттєве видалення. Тепер: свайп вліво >135px (+50% зона) → картка зупиняється на -80px → справа з'являється SVG-кнопка кошика. Тап на кошик = видалення з undo. Свайп вправо >30px = скасування (повернення). Тап на картку у відкритому стані = закрити свайп (замість відкрити редагування). Нові константи `SWIPE_BIN_WIDTH=80`, `SWIPE_BIN_THRESHOLD=135`. Кнопка кошика — SVG Lucide trash, червоний `#ef4444`, не емодзі. Файл: `src/tabs/finance.js`. |
| B-56 | 17.04.2026 (cnTkD) | **40 якісних SVG-іконок категорій.** Було 21 базова. Додано +20 (стиль Lucide/Heroicons, stroke-based): pet, baby, gym, music, book, camera, gaming, bus, flight, hotel, restaurant, pharmacy, doctor, investment, savings, tax, bill, alcohol, tech, movies, hair, charity, bank, dumbbell, crown, briefcase2. Picker у модалці редагування категорії автоматично показує всі 41 (через `FIN_CAT_ICON_NAMES = Object.keys(FIN_CAT_ICONS)`). Файл: `src/tabs/finance.js`. |
| B-59 | 17.04.2026 (cnTkD) | **Модалка редагування категорії скролила на початок при тапі на колір/Х підкатегорії.** Причина: `_refreshCatEditModal()` робив `modal.innerHTML = ...` — повний re-render, scrollTop = 0. Фікс: (1) `_refreshCatEditModal` тепер зберігає `scrollTop` + `activeElement` + `selectionRange` перед re-render і відновлює після. (2) Для частих дій (вибір кольору / іконки) — нові точкові функції `_updateCatModalColorHighlight()` і `_updateCatModalIconHighlight()` які змінюють тільки стилі існуючих кнопок через `data-cat-color`/`data-cat-icon` атрибути, БЕЗ re-render. Файл: `src/tabs/finance.js`. |
| B-60 | 17.04.2026 (cnTkD) | **Круг Hero — товстий donut chart з сегментами по категоріях.** Раніше білий круг з сумою. Тепер: SVG donut з `stroke-width:9`, кожна категорія = окремий `<circle>` з `stroke-dasharray` пропорційним до `сума/total`, кольори з категорій, `rotate(-90)` щоб починати з 12 години, обертається послідовно через `stroke-dashoffset`. Центральний білий круг `r=37` відкриває сам donut як "обід". Над ним — лейбл + велика сума з drop-shadow. Файл: `src/tabs/finance.js`. |
| B-62 | 17.04.2026 (cnTkD) | **Аналітика Фінансів — повний редизайн.** Було: 1 графік 8 тижнів, 3 інсайт-картки, 50/30/20 бар. Стало: (1) **Головний графік з 3 режимами** — toggle "Капітал / Витрати / Доходи". Капітал = polyline накопичувального балансу (з початковим зі всіх попередніх операцій), Витрати = стовпчики поточної палітри, Доходи = двоколірні стовпчики оранжевий+зелений. (2) **3 міні-блоки з 9 метриками** — кожен блок перемикається стрілками `‹ ›`, показує N/9. Метрики: Витрати місяця (vs минулий %), В середньому/день, Топ категорія (+ %), Заощадження %, Операцій (+ середня), Максимум за день, Найбільша операція, Прогноз місяця (по темпу), Доходи місяця. Велике число + короткий опис. (3) **50/30/20 з кастомними % і назвами** — кнопка ✎ активує режим редагування (input'и для назв і %), зберігається у `nm_fin_benchmark`. "Скинути до 50/30/20". State: `_analyticsChartMode`, `_analyticsMiniIdx[3]`, `_analyticsBenchmarkEdit`. Файл: `src/tabs/finance.js`. |
| B-64 | 17.04.2026 (cnTkD) | **OWL Auto-silence спрацьовував надто агресивно.** Раніше 3 повідомлення поспіль без кліку = silence 4 год. Проблема: лічильник зростав навіть якщо табло приходили за хвилини (юзер не встиг побачити). Фікс: (1) поріг 3 → 5 повідомлень. (2) Додано `MIN_VISIBLE_MS=10хв` — ignored рахується тільки якщо попереднє табло прожило >10хв без кліку. Новіші повідомлення не враховуються (юзер не встиг відреагувати). Файл: `src/owl/proactive.js`. |
| B-68 | 16.04.2026 (W6MDn) | **Агент не бачив анкету налаштувань.** `getAIContext()` читав `nm_routine` (блоки часу Календаря), але НЕ читав `nm_settings.schedule` (базовий розклад з налаштувань). OWL у чаті питав "о котрій прокидаєшся" попри те що розклад заданий. Додатково: `getSchedule()` парсила години без хвилин (06:30 → 6). Фікс: (1) новий блок "Розклад дня (з налаштувань)" у `getAIContext()` + явне правило "НЕ питай". (2) `getSchedule()` повертає `wakeUpStr/workStartStr/workEndStr/bedTimeStr` з HH:MM. (3) `proactive.js` промпт табло використовує точні рядки. (4) `getProfile()` включає currency/language. Файли: `src/ai/core.js`, `src/core/nav.js`, `src/owl/inbox-board.js`, `src/owl/proactive.js`. |
| B-69 | 16.04.2026 (W6MDn) | **Застарілі повідомлення табло OWL зі вчора.** AI природно пише "завтра/вчора". Коли день змінюється — ці слова стають неправдою, а кеш `nm_owl_board`/`nm_owl_tab_*` живе у localStorage без прив'язки до дати. Приклад: табло Нотаток показувало "Завтра мама приїжджає" а приїзд через 8 днів. Фікс: функція `clearStaleBoards()` у `src/owl/inbox-board.js` перевіряє `toDateString()` першого повідомлення, якщо не сьогодні — очищує кеш і скидає `*_ts` у 0. Виклик у `bootApp()` перед рендером табло. Рішення Романа: залишити природну мову "завтра/вчора", фіксувати кеш не промпт. Файли: `src/owl/inbox-board.js`, `src/core/boot.js`. |
| B-43+B-51 | 16.04.2026 (W6MDn) | **Модалка операції — біле тіло + неправильна padding-архітектура.** Два баги = одна функція `_renderTransactionModalBody` (створення + редагування). Корінь: `background:rgba(255,255,255,0.96)` без `overflow:hidden` outer + горизонтальний і вертикальний padding на одному елементі. Фікс: переписано за `docs/DESIGN_SYSTEM.md` — outer panel `rgba(255,255,255,0.30)` + `blur(32px)` + `overflow:hidden` + `padding:0 20px`, scroll container з `padding:28px 0 calc(env(safe-area-inset-bottom)+28px)`, overlay `padding:0 16px 16px`. Файли: `src/tabs/finance.js`. |
| B-49 | 16.04.2026 (W6MDn) | **Модалка "Дата операції" — біле тіло.** Той самий glass-патерн як B-43. Додатково: підключено `setupModalSwipeClose` якого раніше не було (свайп вниз закриває). Файл: `src/tabs/finance.js`. |
| B-55 | 16.04.2026 (W6MDn) | **Модалка редагування категорії — біле тіло.** Glass-патерн як B-43. Input'и назви і підкатегорій отримали `rgba(255,255,255,0.7)` фон (раніше прозорий — на glass-тлі не читались). Файл: `src/tabs/finance.js`. |
| B-67 | 16.04.2026 (acZEu) | **Система автодіагностики — 4 фази за одну сесію.** Новий модуль `src/core/diagnostics.js` + розширення `src/core/logger.js`. **Ф1 Error Boundary+логер:** stack trace у записах помилок, ring buffer останніх 10 юзер-дій (`trackUserAction`), автолистенер `nm-data-changed`, UI розгортання записів з stack+actions. **Ф2 Health Check:** 9 перевірок (сховище доступне, обсяг <5MB, API ключ, SW, онбординг, цілісність JSON 10 масивів/6 об'єктів, Auto-silence, застаріле табло, критичні модулі) з підказками як фіксити. **Ф3 Smoke Tests:** 9 авто-тестів (storage write/read, JSON цілісність, формат дат, DOM структура, глобальні функції, CSS-змінна, event система, clipboard) з ізольованим ключем `__nm_smoke_test__`. **Ф4 Performance:** автомоніторинг startup time (Navigation Timing), long tasks (PerformanceObserver, Safari не підтримує — показуємо "не підтримується" замість фалсепоз), fetch calls (monkey-patch window.fetch з буфером 30 останніх URL+method+duration+status). Діагностична панель: 4 блоки зверху (Стан систем / Smoke / Performance / Логи помилок) з розгортанням. Кнопка "Копіювати" тягне повний текстовий звіт з усіма 4 секціями. Рендер на світлому тлі #f5f0e8 під дизайн-систему NeverMind. |
| B-42+B-63 | 16.04.2026 (acZEu) | **Один баг, не два.** У `generateBoardMessage()` (`src/owl/proactive.js:725`) використовувалась змінна `sc` без оголошення — `const sc = getSchedule()` існував тільки у сусідній `_getInboxBoardContext()` (інший scope). ReferenceError `sc is not defined` (Safari: `Can't find variable: sc`) вилітав ДО `try/catch` блоку (try починався перед `fetch` на рядку 766, а `sc` використовувався на 725). Прапорець `_boardGenerating[tab] = true` (рядок 672) ніколи не скидався → наступні виклики одразу return на рядку 665 → табло застрягало на всіх вкладках (B-42). На Фінансах 31 помилка в консолі (B-63) — тригерів генерації багато (data-changed, timer, welcome-back, chat-closed). Походження: сесія 3229b додала "розклад юзера у промпт табло" але забула `const sc = getSchedule()` у функцію. Фікс: `const sc = getSchedule();` у `generateBoardMessage()` + `sc.sleep` → `sc.bedTime` (бо `getSchedule()` не має `sleep`). |
| B-32 | 16.04.2026 (3229b) | OWL галюцинував "€824 на їжу" при реальних €58. Причина: `getFinanceContext()` не мав явних маркерів чисел — AI додумував. Фікс: маркери `[MONTH_EXPENSES:€X]`, `[TODAY_EXPENSES:0]` + рядок "сьогодні витрат не було" + міграція v3 (очищення кешу табло). |
| B-33 | 16.04.2026 (3229b) | Граматика заголовка модалки "Редагувати витрата" / "Нова дохід". Фікс: повністю переписана модалка (Фаза 3 — калькулятор). Окремі шаблони для expense/income. |
| B-34 | 16.04.2026 (3229b) | Немає вибору дати транзакції — ts завжди Date.now(). Фікс: датапікер у модалці (Сьогодні/Вчора/Позавчора/Тиждень тому + input type=date). |
| B-35 | 16.04.2026 (3229b) | "Або своя категорія" input всередині блоку категорій — UX плутаний. Фікс: прибрано, категорія передається з сітки (K-01). |
| B-36 | 16.04.2026 (3229b) | Кнопка видалення транзакції 🗑 без підпису. Фікс: "Видалити" з SVG-іконкою + текст. |
| B-37 | 16.04.2026 (3229b) | Транзакція не видалялась свайпом. Фікс: touch handlers на `.fin-tx-swipe-wrap` з applySwipeTrail + addToTrash + undo toast. |
| B-40 | 16.04.2026 (3229b) | Nav-чіп "Перевір фінанси" на вкладці Фінансів — безглуздий. Фікс: `if (target === currentTab) return` у `handleChipClick`. |
| B-41 | 16.04.2026 (3229b) | Smart fallback на Фінансах казав про звички ("Всі звички закриті"). Фікс: `_tryTabLocalFallback` з релевантним текстом по вкладках (finance→суми, tasks→задачі, health→самопочуття). |
| B-27 | 15.04.2026 (6v2eR) | Картка всередині не показувала блоки Фази 1 (Лікування/Препарати/Записи лікаря) для старих/нових карток — поля порожні, UI редагування не було. Фікс: створено стилізовану модалку `health-card-modal` у двох режимах (create/edit). У заголовку воркспейсу додано кнопку "Ред." яка відкриває модалку з усіма полями: назва, опис, лікар, рекомендації, висновок, дата початку, наступний прийом (date+time), препарати (динамічний список з name/dosage/schedule/courseDuration), статус. Зберегти/видалити — повний CRUD. Логи `medications[].log[]` зберігаються при редагуванні (match по name). |
| B-28 | 15.04.2026 (6v2eR) | Кнопка "Назад" у воркспейсі картки не реагувала на клік. Корінь: `renderHealthWorkspace` перезаписував `#health-scroll` innerHTML → статичні елементи `#health-cards-list`, `#health-empty`, `#health-allergies-card` (які були у `index.html`) зникали з DOM. `closeHealthCard()` → `renderHealthList()` шукав `#health-cards-list`, отримував `null` → виходив через `if (!listEl) return`. Воркспейс лишався на екрані. Фікс: `renderHealthList` переписано на повну генерацію `#health-scroll.innerHTML` зсередини (не покладається на статичний DOM). `index.html` — `#health-scroll` тепер пустий контейнер. `_renderAllergiesCard` перероблено на `_buildAllergiesCardHtml` (повертає HTML-рядок). |
| B-29 | 15.04.2026 (6v2eR) | Блок "Нотатки · {назва}" у картці Здоров'я не відкривав папку — юзер лишався на вкладці Здоров'я. Корінь: `openNotesFolder` лише міняв стан і рендерив — але НЕ викликав `switchTab('notes')`. Фікс: додано wrapper `openHealthNotesFolder(folderName)` який викликає `switchTab('notes')` + `setTimeout(openNotesFolder, 150)` — аналог як у Проектах (`bundle.js:1720`). |
| B-30 | 15.04.2026 (6v2eR) | "+" у Здоров'ї відкривало системний `prompt()` браузера (два послідовні промпти — "Назва:" і "Підзаголовок:"), що виглядало чужорідно. Фікс: модалку з B-27 використано як create-режим — натискання "+" відкриває ту саму стилізовану модалку з пустими полями. Єдина UI точка створення+редагування станів. |
| B-31 | 15.04.2026 (6v2eR) | Legacy шкали 1-10 (Енергія/Сон/Біль) жили усупереч новій концепції Здоров'я (`CONCEPTS_ACTIVE.md`: "Стан сьогодні — без шкали. OWL класифікує текст у тренд"). 3 місця прибрано: (1) блок "САМОПОЧУТТЯ ТИЖНЯ" на головному, (2) блок "ВІДМІТИТИ СЬОГОДНІ" на головному, (3) блок "Самопочуття сьогодні" всередині картки. Видалено функції `_renderHealthWeekBars`, `_renderHealthTodayScales`, `setHealthScale`. Дані `nm_health_log` збережено у localStorage як fallback (AI action `log_health` лишився, але без рендеру). Тренд OWL-классифікації з'явиться у Фазі 4. |
| B-26 | 15.04.2026 (jMR6m) | Дві pre-existing проблеми Inbox board layout, експоновані Фазою 1 Здоров'я (ширший AI контекст → довші повідомлення табло). (1) `#inbox-scroll` мав `margin-top:-10px` — фіксований overlap що ставав видимим коли OWL board ріс у висоту, чіпи перекривали першу картку. Фікс: прибрано negative margin, flex layout тепер натурально стискає scroll коли board росте. (2) `.owl-speech-chips` мав `mask-image` linear-gradient fade на краях шириною 14px — кнопка "Поговорити" візуально "обрізана" (пів-кнопки) коли потрапляла у зону fade (14px з'їдало ~13% ширини кнопки). Підтверджено timing-залежністю: при re-mount застосунку (foreground повернення) кнопка "домалювалась". Фікс: маска зменшена 14px → 4px — естетичне розмиття на самих краях лишилось, але вмісту не "обрізає". Стрілки `.owl-chips-arrow` залишаються основним індикатором скролу. Файли: `index.html`, `style.css`, `sw.js`. |
| B-25 | 09.04.2026 | Тап на подію в календарі не працював. Фікс: додано модалку редагування події (назва, дата, час, пріоритет, видалення). Тап працює в 3 місцях: список подій місяця, список дня, блок "Найближче". Видалена подія йде в кошик з undo toast. |
| B-24 | 09.04.2026 | "Нагадай ввечері" створювало задачу замість нагадування. Причина: `INBOX_SYSTEM_PROMPT` не мав правила "НАГАДАЙ = set_reminder" і маркерів часу. Фікс: додано правило + маркери (вранці=08:00, ввечері=18:00 тощо) в Inbox промпт, а також у Notes і Health chat bars де `set_reminder` був пропущений. |
| B-23 | 09.04.2026 | Табло Inbox показувало fallback замість AI. Причина: `_isTooSimilar()` відкидала ВСІ нові повідомлення (overlap >60% через ті самі задачі/звички) + fallback оновлював `nm_owl_board_ts` → Judge Layer штрафував наступну спробу → замкнене коло. Фікс: 1) видалено `_isTooSimilar()` повністю (антиповтор забезпечує `_extractBannedWords` у промпті), 2) виправлено граматику fallback ("1 задач" → "1 задача"), 3) fallback більше не оновлює `nm_owl_board_ts`. |
| B-15 | 08.04.2026 | `setTimeout(100)` у `sendChipToChat` замінено на подвійний `requestAnimationFrame`. Прив'язано до рендер-циклу браузера замість магічного числа — працює надійно на будь-якій швидкості пристрою. |
| B-22 | 07.04.2026 | AI плутав подію з задачею. Фікс: `_detectEventFromTask()` — regex-детекція слів-маркерів подій (приїзд, зустріч, концерт тощо) + паттернів дат ("20го", "15 травня") у заголовку задачі. Якщо збіг — автоконвертація task→event. Працює як fallback у `processSaveAction` (inbox.js) і `processUniversalAction` (habits.js). |
| B-21 | 07.04.2026 | Табло маловаріативне — OWL зациклювався на тих самих задачах. Фікс: 1) `_extractBannedWords()` — витягує ключові слова з останніх 3 повідомлень і явно забороняє їх у промпті. 2) `_isTooSimilar()` — кодовий фільтр після генерації: якщо overlap ключових слів > 60% з останніми повідомленнями — відкидає. |
| B-20 | 07.04.2026 | Табло не реагувало на відповіді юзера в чаті. Фікс: додано dispatch `nm-data-changed` з detail `'chat'` у `saveChatMsg()` (`src/ai/core.js`) — тільки для `role === 'user'`. Debounce-listener у `proactive.js` підхоплює і перегенерує табло через 3 сек. |
| B-09 | 06.04.2026 | Міні-календар з вкладки Я прибрано. Замість нього — повноцінна Calendar modal з кнопки 📅 у Продуктивність. Місячний календар з навігацією, маркери днів з задачами, тап на день показує список задач. Додано dueDate/priority в структуру задач. |
| B-03 | 06.04.2026 | Верифікація: `create_project` action працює в `inbox.js`, промпт розрізняє задачі/проекти. Не відтворюється. |
| B-04 | 06.04.2026 | Верифікація: календаря в `nav.js` немає, баг неактуальний (N/A). |
| B-05 | 06.04.2026 | Верифікація: `overflow-x:auto` є, картки не обрізаються. Не відтворюється. |
| B-06 | 06.04.2026 | Верифікація: поле вводу Inbox працює коректно, blur/fade не потрібен. |
| B-11 | 06.04.2026 | Верифікація: модалка звичок має правильний `padding:0 20px` на outer panel. Виправлено 31.03. |
| B-12 | 06.04.2026 | Верифікація: модалка задач — аналогічно B-11, padding правильний. Виправлено 31.03. |
| B-19 | 06.04.2026 | Смайлик настрою не впливав на скор і OWL не реагував. Рішення: скор = чисто об'єктивний (задачі + звички + проекти), настрій окремо — передається в OWL prompt для адаптації тону. Динамічна формула скору: кожне джерело 0..1, середнє. doneAt на кроках проектів. setEveningMood → renderEvening. 3.12 закрито. |
| B-18 | 06.04.2026 | Тап на момент дня не відкривав повний текст. Фікс: модалка `moment-view-modal` в `index.html` + `openMomentView()` в `evening.js`. При тапі на момент відкривається модалка з повним текстом, часом, настроєм. Дизайн як habit-modal. |
| B-17 | 06.04.2026 | Табло не оновлювалось миттєво після дій. Фікс: event-система `nm-data-changed` — saveTasks/saveHabitLog/saveMoments dispatch'ать подію, debounce-listeners в proactive.js і inbox-board.js тригерять генерацію нового повідомлення через 3 сек. |
| B-16 | 06.04.2026 | Чіпи-привиди: ✔️-чіпи залишались на табло після ручного закриття задачі/звички. Фікс: `filterStaleChips()` у `chips.js` — при кожному рендері перевіряє fuzzy match чи задача вже done або звичка відмічена, і прибирає застарілі чіпи. |
| — | 05.04.2026 | Оновлення табло проактивно відкривало чат-бар на tasks/notes/me/evening/finance. Корінь: `add*BarMsg` беззастережно викликали `openChatBar` на кожне повідомлення, включно з тихим відновленням історії через `restoreChatUI`. Фікс: обгорнуто `openChatBar` у guard `if (!_noSave)` у 5 файлах. |
| B-13 | 05.04.2026 | Апостроф у `onclick` чіпів ламав клік на словах з `'` — переведено на `data-chip-text` + делегований click-обробник у `src/owl/board.js` і `src/owl/inbox-board.js`. `&#39;` у `onclick` декодується HTML-парсером назад у `'` до виконання JS → SyntaxError. |
| B-14 | 05.04.2026 | `includes()` у `navMap` давав false positives. Перший фікс (видалення navMap) був невірний — втрачена навігація, AI міг сам закривати задачі не знаючи які юзер реально виконав. Правильний фікс: типізовані чіпи `{label, action:'nav'\|'chat', target?}` — AI явно вказує тип у JSON. Оновлено промпти в `src/owl/proactive.js` і `src/owl/inbox-board.js`, рендер у `board.js`/`inbox-board.js` пише `data-chip-action`/`data-chip-target`, `chips.js` розгалужує по action. Fallback для старих string-чіпів → chat. |
| B-14 (старий) | 03.04.2026 | Task chat показував сирий JSON замість обробки дії — заборонено action-формат в промпті, додано fallback |
| B-07 | 31.03.2026 | Свайп по кроках задачі ставив галочку — виправлено: тільки тап < 10px |
| B-08 | 31.03.2026 | Зняття галочки з кроку не відновлювало карточку — виправлено |
| B-10 | 31.03.2026 | При переході на Inbox чат відкривався на весь екран — видалено авто-відкриття |
| B-01 | 26.03.2026 | Undo нотатки повертала в кінець — виправлено через predecessor ID |
| B-02 | 26.03.2026 | `==` замість `===` при порівнянні habit_id — виправлено |
| — | 31.03.2026 | GitHub Pages не оновлювався — `GITHUB_TOKEN` блокує запуск `deploy.yml`. Фікс: деплой в `auto-merge.yml` |
| — | 25.03.2026 | SW кеш `nm-v1` ніколи не скидався → деплой не оновлювався у браузері |
| — | 25.03.2026 | Мітка деплою в Inbox була захардкоджена, не оновлювалась |
| — | 25.03.2026 | Quit-звички: один зрив обнуляв весь прогрес |

> ⚠️ Примітка: старий `B-14` (Task chat JSON) і новий `B-14` (`includes()` в chips.js) — різні баги, той самий ID повторно використаний. При потребі буде перейменовано на `B-14-old`.

---

## 📋 Детальні описи останніх знахідок (через `/gemini`)

### B-13 — Апостроф у `onclick` чіпів ламає клік

**Пріоритет:** 🔴 HIGH
**Файл:** `src/owl/board.js:137-140` (і потенційно `src/owl/inbox-board.js` — перевірити)
**Знайдено:** 2026-04-05, Gemini 3 Pro (аудит через `/gemini`), підтверджено Claude Opus 4.6

**Опис:** У `renderTabBoard()` чіпи рендеряться з інлайн `onclick` що містить текст чіпа:
```javascript
const s = escapeHtml(c).replace(/'/g, '&#39;');
return `<div class="owl-chip" onclick="owlChipToChat('${tab}','${s}')">${escapeHtml(c)}</div>`;
```

Спроба екранування апострофа через `&#39;` **не працює** — HTML-парсер браузера декодує entity назад у `'` **перед** тим як JS-парсер побачить код атрибута.

**Чому це баг:** Якщо текст чіпа містить апостроф, браузер генерує невалідний JS:
```
onclick="owlChipToChat('tasks','п'ять задач')"
                                  ↑
                         SyntaxError тут
```

**Конкретні кейси які гарантовано зламаються:**
- Українські слова з апострофом: **п'ять, м'ясо, сім'я, здоров'я, ім'я, об'єм**
- Англійські фрази: **I'm, don't, can't**

**Пропоноване рішення:** Перенести текст чіпа в `data-*` атрибути і повісити один делегований обробник:

```javascript
// Рендер у renderTabBoard():
const chipsHTML = (msg.chips || []).map(c =>
  `<div class="owl-chip" data-chip-tab="${escapeHtml(tab)}" data-chip-text="${escapeHtml(c)}">${escapeHtml(c)}</div>`
).join('');

// Один делегований обробник (після присвоєння innerHTML):
chipsEl.addEventListener('click', (e) => {
  const chip = e.target.closest('.owl-chip');
  if (!chip) return;
  const t = chip.dataset.chipTab;
  const txt = chip.dataset.chipText;
  if (chip.classList.contains('owl-chip-speak')) {
    openChatBar(t);
  } else {
    owlChipToChat(t, txt);
  }
});
```

`dataset` безпечно зберігає будь-який текст — HTML-парсер не виконує його як JS.

**Перевірити також:** `src/owl/inbox-board.js` — можливо той самий паттерн використовується для чіпів Інбоксу.

**Тест-кейс ручний (після фіксу):**
1. Створити чіп з текстом `п'ять задач` через консоль: `saveTabBoardMsg('tasks', {text:'тест', chips:["п'ять задач"], ts:Date.now()})`
2. Тапнути на чіп
3. ✅ Має: відкрити чат-бар вкладки tasks з текстом "п'ять задач"
4. ❌ До фіксу: `SyntaxError: missing ) after argument list` у консолі, клік не спрацьовує

---

### B-14 — `includes()` в chips.js дає false positives

**Пріоритет:** 🟡 MEDIUM (дизайн-smell, не критично поки AI дотримується правила "3 слова")
**Файл:** `src/owl/chips.js:26-33`
**Знайдено:** 2026-04-05, Gemini 3 Pro

**Опис:** `owlChipToChat()` розрізняє навігаційні чіпи від текстових через `String.prototype.includes()`:
```javascript
const navMap = [
  { patterns: ['задач', ...], tab: 'tasks' },
  ...
];
for (const nav of navMap) {
  if (nav.patterns.some(p => lower.includes(p))) {
    switchTab(nav.tab);
    return;
  }
}
```

**Чому це баг:** Будь-який чіп що **містить** підрядок `"задач"` перекине користувача на вкладку замість відправки в чат:
- `"обговорити задачі"` — користувач очікує чат, отримує навігацію
- `"нотатки по проекту"` — хибний кидок на notes замість projects

**Поки працює** бо промпт до AI обмежує чіпи до 3 слів. Але фундаментально ненадійно.

**Пропоноване рішення:** Явна типізація чіпів у відповіді AI:
```json
{
  "chips": [
    {"label": "Закрити задачі", "action": "nav", "target": "tasks"},
    {"label": "Пізніше", "action": "chat", "text": "нагадай завтра"}
  ]
}
```

**Що треба змінити:**
1. Промпт у `src/owl/proactive.js` (`generateTabBoardMessage`) — оновити формат JSON
2. Промпт у `src/owl/inbox-board.js` — той самий
3. `saveTabBoardMsg()` — зберігати новий формат
4. `renderTabBoard()` у `board.js` — передавати `action` в dataset
5. `owlChipToChat()` у `chips.js` — розгалуження по `action`, прибрати `navMap`
6. **Міграція:** fallback для старих `chips:[string]` у localStorage

---

### B-16 — Система чіпів порушена (концептуально + технічно)

**Пріоритет:** 🔴 CRITICAL
**Файли:** `src/owl/proactive.js`, `src/owl/inbox-board.js`, `src/owl/chips.js`, `src/tabs/inbox.js`
**Знайдено:** 2026-04-05

**Головна проблема:** Чіпи = швидкі ВІДПОВІДІ юзера агенту. НЕ заклики до дії. AI цього не дотримується.

**Що зламано (5 підпроблем):**

1. **AI генерує чіпи-імперативи** — "Купи продукти", "Покурити" замість "Купив продукти ✔️". Промпти оновлювались тричі — AI ігнорує.
2. **Клік на чіп викликає AI → AI робить дію** — юзер натиснув "Купив продукти ✔️" → `sendToAI()` → AI вирішив це нова задача → `processSaveAction()` → дублікат картки. Або AI вирішив закрити звичку/задачу без підтвердження.
3. **Чіпи-привиди** — задачу закрито вручну, але чіп "Закрив задачу ✔️" залишається на табло до наступної генерації (3 хв).
4. **Орфографія AI** — "постараю одяг" замість "постирав одяг". Промпт не допомагає.
5. **fromChip працює тільки для Inbox** — на інших вкладках (tasks, habits, notes) клік на чіп все одно проходить через AI.

**Рішення — переписати систему чіпів:**

1. **Промпти** — переформулювати концепт: чіпи = варіанти відповіді юзера ("Зробив ✔️", "Пізніше", "Розкажи більше"), НЕ команди.
2. **✔️ чіпи обробляти ЛОКАЛЬНО** — без виклику AI. Чіп з ✔️ → fuzzy match (нечіткий пошук) по першим 4 літерам кожного слова → знайти задачу/звичку → закрити локально → показати toast (спливне повідомлення "Готово!").
3. **Чат-чіпи** — надсилати в чат з позначкою `fromChip=true` для ВСІХ вкладок, не тільки Inbox.
4. **Навігаційні чіпи** — залишити як є (action='nav').

---

### B-17 — Табло не оновлюється миттєво

**Пріоритет:** 🟡 MEDIUM
**Файли:** `src/owl/proactive.js`, `src/owl/inbox-board.js`, всі `src/tabs/*.js`
**Знайдено:** 2026-04-05

**Опис:** Табло оновлюється тільки за таймером (`OWL_BOARD_INTERVAL` = 3 хв для Inbox) і при переключенні вкладок. Якщо юзер закрив задачу — табло мовчить 3 хвилини.

**Що має бути:** При ключових подіях (закриття/створення задачі, виконання звички, нова нотатка, нова витрата, новий момент) → табло оновлюється через 2-3 секунди з реакцією типу "Красава, ще 2 лишилось".

**Рішення:**
1. Додати debounce-функцію (затримка що об'єднує кілька подій в один виклик) `requestBoardUpdate(tab)` — якщо подія відбулась, чекає 3 сек і запускає `generateTabBoardMessage()` або `generateOwlBoardMessage()`.
2. Викликати `requestBoardUpdate` з ключових місць: `completeTask`, `saveHabitLog`, `addNote`, `addFinanceRecord`, `addMoment`.
3. Debounce захищає від спаму — 5 подій за 2 сек = один виклик.

---

### B-15 — `setTimeout(100)` у chips.js — тендітна синхронізація

**Пріоритет:** 🟢 LOW
**Файл:** `src/owl/chips.js:43-52`
**Знайдено:** 2026-04-05, Gemini 3 Pro (частково підтверджено)

**Опис:** Після `openChatBar(barTab)` відправка повідомлення робиться через жорсткий `setTimeout(..., 100)`:
```javascript
openChatBar(barTab);
// ... input.value = text
setTimeout(() => { sendToAI(); }, 100);
```

**Чому це (потенційно) баг:** 100 мс — магічне число без гарантій. На повільному iPhone з анімацією може не вистачити.

> **Примітка Claude:** Gemini помилково описав що `getElementById` повертає null — насправді він викликається синхронно до setTimeout, і в NeverMind input'и живуть у статичному `index.html`. Реальна потенційна проблема — race з пересіканням рендерів OWL.

**Рішення:** зробити `openChatBar()` асинхронним (Promise) і використати `await` замість `setTimeout`. **Робити тільки в рамках окремого рефакторингу чат-бара** — зачіпає всі caller'и.

---

## 📋 Як додавати новий баг

```markdown
| B-XX | `src/шлях/файл.js:рядок` | Короткий опис | Деталі якщо є |
```

Пріоритети:
- 🔴 **Критичний** — функціональність зламана повністю
- 🟡 **Середній** — є обхідний шлях або рідко трапляється
- 🟢 **Дрібний** — косметика, не заважає роботі

Після виправлення:
1. Перенести рядок з відкритих у секцію "✅ Закриті (для довідки)"
2. Додати дату і коротке пояснення як виправлено
3. Якщо баг потребує подальшого моніторингу — залишити коментар

## 📋 wontfix / Галюцинації AI

### "Синтаксична помилка `<msg.id>`" у `board.js:125` (05.04.2026)

Gemini під час аудиту повідомив про "критичну синтаксичну помилку" у конструкції:
```javascript
const ago = Date.now() - (msg.ts || msg.id || Date.now());
```
Стверджуючи що там є `<msg.id>` з кутовими дужками. **Це галюцинація** — у реальному коді дужок немає, це валідний JS-ланцюжок з `||` fallback.

**Урок:** завжди перевіряти точну цитату Gemini проти реального коду перед виправленням. Не довіряти "критичним синтаксичним помилкам" без верифікації.
