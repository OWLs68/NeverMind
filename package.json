{
  "name": "nevermind",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "@playwright/test": "^1.61.0"
  }
}
